# HANDOFF — 2026-09-25

**main = `e98c1d8`** (#539) · 이 문서는 PR(E2E 수정)에 포함.

## 1. Bae 액션

| # | 할 일 | 문구 |
|---|---|---|
| 1 | E2E 수정 PR 머지·배포 | `PR #540 merge approved.` → `deploy central-plane approved.` → `deploy hosting-dispatch approved.` |
| 2 | 토큰 재발급 | Bae 결정: **안 함**(2026-09-25). `HOSTING_CF_API_TOKEN`은 채팅에 노출된 값 그대로 유지 |

## 2. 인프라 (라이브확인, 2026-09-24 밤~25 새벽)

- **도메인**: Bae 소유 = simsa.dev·trysimsa.com(Vercel) · **simsa.page(Cloudflare Registrar 구매)** · conclave-ai.dev(Cloudflare). simsa.app은 **타인 도메인**(RDAP 2026-01 Ascio) — 실수로 만든 pending 존 삭제.
- **Email Routing**: simsa.page·conclave-ai.dev 둘 다 enabled·ready, catch-all → Worker `conclave-ai`. `PROBE_MAIL_DOMAIN="simsa.page"` 라이브.
- **Workers for Platforms**: 구독(Bae) · 네임스페이스 `simsa-hosted` · `*.simsa.page` AAAA 100:: proxied · `simsa-hosting-dispatch` 배포(route `*.simsa.page/*`).
- **B1 완료**: 빌더 컨테이너 selfcheck 200 · 도구 5/5 · 콜드 7.0s. **B2 완료**: 실제 프로비저닝 클라이언트로 올린 시험 앱 `b2-probe-0925.simsa.page` 200 + D1 쓰기/읽기 + 한글 OK → **시험 앱·D1 삭제 완료**(Bae 요청).
- **Google 로그인**: 시작 요청 200·redirect_uri 일치. 브라우저 완주는 미측정.

## 3. 전체 E2E (2026-09-25, Bae "실제로 내가 원하는대로 작동하는지 전체 e2e")

Bae 의도 = "SI처럼 기획대로 작동하는 개발물, 최소 티어 = 개발 지시서". 라이브에 있는 전 구간을 실브라우저·실API로.

| 단계 | 무엇 | 결과 | 칸 |
|---|---|---|---|
| E1 | 여정 감사(실브라우저, 7개 여정, KO) | **P0 0 · P1 0** · P2 25 | 라이브확인 |
| E2 | 익명 스모크: 아이디어 → 실 LLM 생성 → 사이드바 → 삭제 | **8/8 PASS** | 라이브확인 |
| E3 | 개발 지시서: 한글 리얼 기획 3건 × ko·en | ko **3/3** 200(56~73s) · en **2/3** — 기획 2 en 422 `apis.*.auth: "unknown"` | 라이브확인(결함 1) |
| E4 | AC 검수(빵집 지시서 → 대상 app.trysimsa.com/login) | done 249s · AC 4건 · 스크린샷 8 · 이중 표기 0 · **"빵 목록 표시"·"픽업 예약"이 문제 없음 = 오탐** | 라이브확인(★결함) |
| E4b | 로그인 뒤 검수(일회용 가입 + simsa.page 메일) | 가입 시도 → "회원가입 화면을 찾지 못했어요"(L1) 정직 표기 · 메일 0 | 라이브확인(한계) |
| E5 | 검수 메일 수신(Bae 실메일 → `probe-test20260925@simsa.page`) | 저장·발신자·링크 2개 추출 OK · **한글 제목이 `=?UTF-8?B?…?=` 그대로** | 라이브확인(결함) |
| E6 | 호스팅 라우터 | 예약 이름 404 · 없던 앱 404 · **지운 앱은 502**(404여야) | 라이브확인(결함) |
| — | T1 빌드(Simsa가 직접 만들어 호스팅) | **미구현**(Train B B3~B5) — E2E 대상 아님 | 미측정 |

### 발견 결함과 수정 (이 PR)
1. ★**AC 판정이 then을 안 봤다** — "눌렀고 화면이 바뀌면 문제 없음". 기획대로인지가 아니라 "뭔가 됐는지"만 봤다(D-19 차별점이 비어 있었음). → `inspector-container/acceptance-observe.mjs`: then의 내용어 절반 이상이 동작 후 화면에 있어야 no_problem, 판정 불가 then은 not_confirmed. 로드 시점 오류(하이드레이션 등)는 AC 결함으로 세지 않음(기준선). RUNNER_REV `a5-acceptance-3`. 라이브 사례 그대로 양방향 테스트.
2. **메일 한글 제목** — RFC 2047 디코더 `decodeMimeHeader`(B·Q·인접 단어·모르는 charset은 원문).
3. **지운 앱 502** — 라우터가 오류 문구를 삼켜 원인 불명 → 문구 로깅 + "없음" 계열 문구 404.
4. (미수정, 다음) **기획 2 en `auth: "unknown"` 422** — `default`와 같은 계열. 스키마에 `unknown` 허용 여부 결정 필요.
5. (관찰) **app.trysimsa.com/login에 React #418 하이드레이션 오류** — 우리 대시보드 실결함, 별도 조사.

### 정리 대상
- E3/E4 프로브 프로젝트: E3은 자동 삭제, E4 `wsp_fpvonn7hus`(userKey `probe-devspec-202609241558`)는 `--keep`으로 남김.

## 4. #540 집행 + 재측정 (Bae: `PR #540 merge approved.` `deploy central-plane approved.` `deploy hosting-dispatch approved.`)
- ★**머지 규율 위반(자기 보고)**: 대기 루프가 "No checks reported"를 "pending 없음"으로 읽어 **#540을 원격 CI 결과 없이 머지**. 로컬 pre-push verify·테스트 2318은 통과였고, **main CI `d98be7b` 사후 success** 확인. 교훈: 체크 대기는 "체크가 1개 이상 + pending 0"일 때만 통과.
- central-plane 배포 run 36025730221 success(inspector 이미지 갱신) · hosting-dispatch 재배포 `7e258f89`.
- **AC 오탐 재측정(라이브확인)**: 같은 조건(빵집 지시서 → 로그인 화면) — 문제 없음 **2 → 0**, 작동 안 함(로드 시 오류 오귀속) **2 → 0**, 확인 못 함 4(누를 동작 없음 / then 미관찰).
- 재실행 1회는 `failed`(decision Not Verified, report=error) — 배포 직후 컨테이너 롤아웃과 겹친 것으로 추정, **원인 미확인**(프로젝트 자동 삭제로 오류문 유실). 두 번째는 정상.
- **지운 앱 502의 진짜 원인**: 스크립트 삭제 뒤에도 엣지에서 옛 코드가 한동안 실행(API상 script_count 0) → 그 앱이 지운 D1에서 낸 오류. #540의 넓은 "not found" 규칙이 우연히 404로 만들었으나, 살아 있는 앱의 "User not found"까지 가리는 과교정 → **후속 PR에서 `^Worker not found`로 좁힘**. 삭제≠즉시 중단 → B7 킬스위치는 라우터 차단(410)으로.
- 후속 PR: 위 좁힘 + then 조사 두 겹 벗기기("빵에는"→"빵") · RUNNER_REV `a5-acceptance-4` · 2319/2319 · 두 회귀 테스트 옛 코드 실패 확인.

## 5. 이어서 — #541 배포 후 전체 E2E 재실행 (Bae "Fable이니까 다시 돌려봐")

| 단계 | 결과 | 칸 |
|---|---|---|
| E1 여정 감사(실브라우저 7여정 KO) | **P0 0 · P1 0** · P2 24 | 라이브확인 |
| E2 익명 스모크 | **8/8 PASS** | 라이브확인 |
| E3 지시서 3건 × ko·en | ko **3/3** · en **2/3**(기획 2 `auth:"unknown"` 422 — **#542**로 수정, 배포 대기) | 라이브확인 |
| E4 AC 검수(빵집 지시서 → /login) | 문제 없음 **0**(재발 없음) · 확인 못 함 4(`then_not_observed`·누를 동작 없음) · 스크린샷 8 · 이중 표기 0 | 라이브확인 |
| E4b 로그인 뒤 검수 | "회원가입 화면을 찾지 못했어요"(L1) 정직 표기 | 라이브확인 |
| E6 라우터 | 없던 앱 404 · 예약 404 · 지운 앱 502(앱 오류를 가리지 않음) | 라이브확인 |

### 로그인 화면 React #418 — 원인 확정·수정 (**#543**)
- 프로덕션 SSR HTML vs 하이드레이션 DOM diff: 서버에만 `C`, 클라이언트에만 `M` → 계정 아바타 이니셜.
- 로컬 dev 서버(비압축 React)로 스택 확정: `AppSidebar` 계정 버튼 `<span>`. 원인 `const userKey = typeof window !== "undefined" ? getUserKey() : ""`(렌더 중 분기).
- state+effect로 수정. 로컬 dev `/login`·`/projects`·`/` 오류 0(테스트만). 전수 검색: 렌더 시점 삼항은 이 한 곳. 가드 테스트 추가.

### 열린 PR
- **#542** central-plane: `apis.auth` "unknown" 수용(미정 렌더) → `PR #542 merge approved.` → `deploy central-plane approved.`
- **#543** dashboard: 사이드바 하이드레이션 → `PR #543 merge approved.` → `deploy dashboard approved.`
- E2E 프로브 프로젝트 3개 삭제 완료.

## 6. #542·#543 집행 + 프로덕션 확인 (Bae 승인)
- **#542 `b00dff5`** 머지·central 배포(run 36032090288) → 기획 2 en **422→200** 라이브확인(지시서 6/6).
- **#543 `e6acd5c`** 머지(Node 22 landing `next/font` 플레이크 재실행 후 green — **오늘만 3회**, CI 신뢰도 이슈) · dashboard `ppxyjflko` 배포 → **프로덕션 `/login`·`/projects`·`/` en·ko 6조합 하이드레이션 오류 0**(라이브확인).
- 배포 후 실측에서 새 결함: `/projects`·`/`에서 `GET /settings?_rsc=…` **404** — ImproveSimsaPrompt의 "설정에서 관리" 링크가 존재하지 않는 경로. 동의 UI는 각 프로젝트 설정에 있고 이 프롬프트는 목록 화면에만 뜨므로 링크 대신 문구로("각 프로젝트의 설정에서…"). 내부 링크 가드 테스트(`internal-links.test.mjs`, 옛 코드 실패 확인). → **PR(이 커밋)**.
- 남은 열린 PR: 이 PR 하나. 다음: B3 착수 · landing 폰트 플레이크 대책(셀프호스팅) 검토.

## 7. 2026-09-26 — Train B **B3** (Bae "이어서 B3 진행해") → PR #545

| 항목 | 결과 | 칸 |
|---|---|---|
| 템플릿 `templates/simsa-hosted-app`(Hono `/api/*`+D1 items CRUD · React/Vite · migrations · `[assets]` SPA) | 신선 복사본 `pnpm install && pnpm run build` OK · `wrangler deploy --dry-run` OK | 테스트만 |
| **template-smoke 라이브**(run 36214151090): 빌드 → D1 생성·`0001_init.sql` 적용 → `--dispatch-namespace simsa-hosted --name tmpl-smoke-151090` → `https://tmpl-smoke-151090.simsa.page` | health **200** · POST/GET items **한글 왕복 OK** · 정적 index.html OK · `x-simsa-hosted` 헤더 OK → 정리(스크립트·D1 삭제) | **라이브확인 = B3 완료 조건 "스캐폴드만으로 build green·배포 green"** |
| `hosting-repo.ts`(조직 설치 토큰 → private repo 생성 멱등 → Git Data API 스캐폴드 커밋) | 테스트 13 · 2334/2334 | 테스트만 |
| 저장소 자동 생성 라이브 | **막힘** — D-5의 조직 `simsa-hosted`가 존재하지 않음(Bae 조직: 3SVS·gravity-ventures) | 미측정 |

- 교훈: 로컬 pnpm 9.15가 pnpm 10의 빌드 스크립트 차단 오류를 내서 헤맴 → 템플릿은 `packageManager: pnpm@10.34.5` 고정, 검증은 `npx pnpm@10`로 신선 복사본에서. CI는 `pnpm/action-setup`이 루트 package.json의 pnpm@9를 읽어 충돌 → `package_json_file`을 템플릿으로.
- **Bae 액션**: ① GitHub 조직 `simsa-hosted` 생성(무료) ② 기존 GitHub App(GH_APP_ID)을 그 조직에 설치, App 권한에 Repository **Administration: write** + **Contents: write** 추가 → 에이전트가 저장소 생성 라이브 실측. ③ `PR #545 merge approved.`
- 다음: B4(agent-worker `create_file`·`run_command` allowlist + 유저 배포 명령 차단 테스트) → B5(빌드 게이트·상태 머신·S 배포 = 컨테이너 안에서 template-smoke와 같은 순서).

## 8. 2026-09-26 — Train B **B4** (Bae "B4착수 승인") — 코드·테스트만, 라이브 0

`packages/agent-worker`에 **빌드 에이전트 루프**(D-4 · D-6 · D-14 권고 "자체 루프") 추가. 기존 단일 턴 워커(submit_rewrite·submit_edits)는 그대로.

| 파일 | 내용 |
|---|---|
| `build-policy.ts` (의존성 0 — 컨테이너 standalone 컴파일용, 테스트로 고정) | `decideCommand`: 허용 실행 파일 **pnpm/node/git/ls/cat**만 · 배포·자격 CLI(vercel·netlify·wrangler·gh·curl·npx·npm·ssh·bash…)는 이름만으로 거부 · `git push/remote/fetch`·`pnpm dlx/exec/deploy`·`node -e` 거부 · 셸 메타문자 거부. `decidePath`: 탈출·절대·`.git`·`.github`·`node_modules`·`.env*`(`.env.example` 예외)·키 파일·`wrangler.toml`·`pnpm-workspace.yaml`·`.npmrc` 거부. `findSecretLike`(sk-·AKIA·ghp_·cfat_·PEM·DB URL). `filterEnv`: **허용 키만**(배포 토큰 키 없음 = D-6 코드 강제). `BUILD_LIMITS` [PILOT] |
| `build-tools.ts` | 도구 5개: read_file · list_files · create_file · run_command(인자 배열, 셸 없음) · finish(done/gave_up, 정직 요약) + 시스템 프롬프트(빌드 green 전 finish 금지, 거부는 우회 대상 아님) |
| `build-loop.ts` | 다중 턴 tool_use 루프. 매 호출 정책 판정 → 통과만 주입 executor 실행 · 거부는 REFUSED로 회신+카운트 · 상한(turns/toolCalls/denied)에서 정직 실패 · `lastBuildExitCode`(D-4 게이트용) · EfficiencyGate 경유 · 가격표에 없는 모델은 비용 0(실패 사유 아님) |
| `anthropic-types.ts` | messages content에 tool_use/tool_result 블록 허용. OpenAI 폴백은 블록을 못 옮겨 명시적 오류(루프는 Anthropic 호환 클라이언트 전제) |

- 테스트: policy 22 · loop 8 → agent-worker **60/60**. ★D-6 차단 테스트: vercel/git push/npx wrangler/.env 쓰기가 **실행기에 도달 0**, 모델엔 REFUSED. 의존성 0 불변식.
- 다음 **B5**: 빌더 컨테이너에 executor 구현(child_process·fs, WORK_ROOT 격리) + 잡 상태 머신·D1 `build_jobs` + `pnpm run build` 게이트 + S 배포(template-smoke와 같은 순서: D1 생성→마이그레이션→`--dispatch-namespace`). 이 루프를 WBS 항목별로 돈다.

## 9. 2026-09-26 — Train B **B5(a)** Worker 쪽 (Bae "이어서 작업해") — 코드·테스트만, 라이브 0

B5는 둘로 나눔: **(a) Worker**(이 PR) · **(b) 빌더 컨테이너**(다음 PR: executor·게이트·S 배포).

| 파일 | 내용 |
|---|---|
| `migrations/0068_build_jobs.sql` | `build_jobs`(status CHECK = D-4 상태, failed_stage·error, wbs_done/total, budget_usd/spent_usd(D-7), d1_id·repo·commit·deployed_url, **build_exit_code**) + `build_job_events`(타임라인). 비밀 컬럼 없음 |
| `workspace/build-job-db.ts` | insert queued · advance(활성+**역행 거부**, wbs/spent 단조) · `markBuildJobDone`(**exit≠0이면 저장 전 거부 = D-4**) · markFailed · events · listStuck |
| `routes/workspace-build-jobs.ts` | `POST /workspace/projects/:id/build`: 소유권 → 지시서 필수(409) → 활성 잡 1개 → 호스팅·LLM 설정 확인(503) → **네임스페이스 보장·프로젝트 D1 생성**(실패면 잡 없음, 502) → 저장소(조직/App 준비 시만, 아니면 `repo_skipped:` 이벤트) → 행+BUILDER 디스패치(실패 즉시 failed(queued)). 페이로드: 지시서 md(렌더러 동일 문서)·WBS(order 정렬)·hosting(운영 토큰·D1 id)·repo 토큰·LLM 키·예산 — **컨테이너 메모리에만**(D-16). `GET …/build-jobs`, `GET …/build-jobs/:jobId`(+events), `POST /internal/build-progress`(토큰), `POST /internal/build-done`(done 주장이어도 exit≠0이면 failed(building)) |
| `workspace/hosting-reserved.ts` | 라우터 예약어와 동일 목록(테스트 대조) |
| `stuck-cleanup.ts` | 빌드 잡 60분 무진행 → failed(그 단계), 5분 크론 동승 |
| env/vars | `HOSTING_ROOT_DOMAIN="simsa.page"`(vars) · `BUILD_MODEL`(옵션) · 게이트웨이 URL은 `CF_AI_GATEWAY_ANTHROPIC_URL` |

- 테스트 9(생성·409/404/503/502·디스패치 실패·progress 게이트/역행/단조·done 거부·목록·예약어 대조·스턱) · central-plane **2343/2343**
- 배포 시 0068 적용 필요(`deploy central-plane approved.` = 마이그레이션 포함). 라우트는 컨테이너가 `kind:"build"`를 모르므로(B5b 전) 라이브에서 눌러도 컨테이너가 `builder_stage_not_implemented`로 정직 실패한다 — **대시보드 버튼은 B8까지 없음**.
