# HANDOFF — 2026-07-18

이전: `docs/HANDOFF-2026-07-17.md` (추가 1~7 — D14/#296 Phase3·4/fix-first/RC 합의 레이어 전체).
main 기준 오늘 흐름: 격차 백로그 승인 → G1 착수·완결.

## 오늘 커밋

- **#377** `docs: Simsa 격차 백로그 G1~G14` — Bae "1~8 모두 작업" 승인 + 2차 검토 6건
  추가(G9 법적 기반 · G10 첫 5분 체험 · G11 읽기전용 공유 링크 · G12 클라이언트 오류
  수집 · G13 모바일+접근성 · G14 EN i18n). 착수 순서: G1→G2→G3→G12→G10→G11→G4-①→G9→
  G6→G7→G5→G8→G13→G14. 정본: `docs/simsa-gap-backlog-2026-07-18.md`.
- **#378** `feat(reengage): G1 복귀 이메일 루프` — 팩 export 3~14일 미복귀+email opt-in
  대상에게 (user,project)당 **평생 1통**(스팸 하드가드=PK). 매일 09:00 UTC(18:00 KST)
  크론 + `POST /admin/run-reengage`(ICT). 실패는 sent로 안 세고 마킹도 안 함(자연
  재시도). 대시보드 export 화면에 이메일 선택 수집(기존 notifications API 재사용).
  migration 0061. 테스트 5건. **배포+라이브 실행 확인**(`scanned:0` — 창 내 대상 없음
  정상).

## 상태

- central-plane 배포 라이브(0061 적용), dashboard CLI 배포(이메일 수집 UI).
- 검수 합의 레이어(#370~#376) 전부 라이브 — panel 전원 / council 유료(수동 그랜트).

## 다음 픽업

1. **G2 막힘 도우미** — 에러 붙여넣기→쉬운 말 다음 행동(빌드 중 복귀 접점). 그다음
   G3 회귀 비교 → G12 → … (백로그 순서).
2. D14 월요 크론(07:00 UTC) 첫 자동 사이클 → `GET /admin/deploy-trends` 리뷰.
3. 계정 3건(Bae): T4 GitHub 연결 2분 스모크 · 자동수리 라운드트립 · 웹빌더 실완주.
4. G1 관측: 다음 주부터 크론 로그 `cron:"reengage-nudges"`에서 sent/skipped_no_email
   추이 — skipped가 크면 수집 UI 노출 위치 보강.

## 주의

- reengage 발송 창(3~14일)·시각은 파라미터 — 원칙(평생 1통)은 잠금.
- 이메일 수집은 선택·고지("프로젝트당 최대 한 번") — 문구 바꿀 때 이 약속 유지.

## [추가 1] G2 막힘 도우미 완결 (#380, 라이브 5/5)

- `POST /workspace/unstick` — 붙여넣은 에러/상황 → 쉬운 설명+다음 행동 1~3+
  (AI가 고칠 문제면) 그대로 붙여넣을 지시문. recommend.ts 계약(게이트웨이·정직
  503·날조 금지). 프로젝트 개요에 StuckHelper 카드(복귀 이메일이 이 화면으로 안내).
- **라이브 실증**: env-var 에러 → Supabase 클릭경로+에이전트 지시문 / 모호 상황
  ("게시 버튼 무반응") → 콘솔 에러 찾는 법부터 정확히 안내. central+dashboard 배포.
- 다음: **G3 회귀 비교**(지난 검수 대비 깨진 항목 감지) → G12 → 백로그 순서.

## [추가 2] G3 검수 이력 비교 완결 (#382, 라이브 4/4)

- `check-compare.mjs`(순수): 통과→비통과=회귀(앰버, "최근 변경이 깨뜨렸을 가능성 —
  이것부터"), 비통과→통과=회복(그린, "고치기가 제대로 됐습니다"). 추가/삭제 항목은
  비교 제외, 횡이동 중립. 로컬 직전 결과 기반 — 익명 프로젝트까지 서버 의존 0.
- **라이브 실증** `check-compare-probe.mjs`: 직전 "통과"로 시드된 결제-제외 충돌
  항목이 실검수에서 failed → 회귀 배너·항목·힌트 4/4. ★프로브 함정: vercel 배포가
  Building인 동안 이전 배포가 서빙됨 — READY 확인 후 재프로브로 해결.
- 다음: **G12 클라이언트 오류 수집** → G10 → 백로그 순서.

## [추가 3] G12 클라이언트 오류 수집 완결 (#384, 라이브)

- `POST /workspace/client-errors`(절단 강제·경로 쿼리/해시 제거·전역 폭주 상한,
  migration 0062) + `GET /admin/client-errors`(ICT). dashboard 전역 리포터 —
  순수 게이트(세션 5건 캡·중복 제거·Script error류 노이즈 필터), 폼 값 미수집 계약.
- **라이브**: 수집 200 → admin 목록 확인, `?secret=X`가 서버에서 제거됨(프라이버시).
- 운영: 가끔 `GET /admin/client-errors`로 훑기 — 반복 메시지가 보이면 그게 조용한
  이탈 지점. 다음: **G10 첫 5분 체험** → G11 → 백로그 순서.

## [추가 4] G10 첫 5분 체험 완결 (#386, 라이브 7/7)

- `sample-project.mjs`: 전체 루프가 채워진 예시(동네 빵집 예약 앱)를 원클릭 로컬
  생성 — 스펙+항목+검수 결과(dual_confirmed 배지)+실패 항목 고쳐보기(fixes_ready).
  입구=목록 빈 상태 버튼+예시 섹션 헤더. 개요에 isSample 배너+[내 아이디어로 시작]
  출구. 정합성 테스트 5건(검수결과↔항목·summary·fixes_ready·증거·비교 기준선).
- **라이브 7/7**(`sample-project-probe.mjs`). ★배포 대기 함정 재확인: 직전 배포의
  Ready와 새 배포의 Ready를 구분할 것(created 시각 확인).
- 다음: **G11 읽기전용 공유 링크** → G4-① → 백로그 순서.

## [추가 5] G11 읽기전용 공유 링크 완결 (#389, 라이브 API 7/7 + 페이지 7/7)

- `/workspace/shares` 3종(생성·공개 열람·소유자 회수) — **스냅샷 모델**(공유 시점
  내용만, 이후 변경 미반영 — 페이지에 명시), 추측 불가 id, revoked/없음 동일 404,
  크기 60KB·시간당 20개 캡. migration 0063. checks 화면 [공유 링크 만들기](클립보드),
  `/s/[id]` 공개 페이지(로그인 불요, Simsa 푸터). G12 후속: client-errors CORS 프리플라이트.
- **라이브**: API 라이프사이클 7/7 + 수신자 시점 페이지 렌더·회수 반영 7/7
  (`share-page-probe.mjs`).
- 다음: **G4-① 지속성 검수**(저장→새로고침) → G9 법적 → 백로그 순서.

## [추가 6] G4-① 지속성 검수 완결 (#391, 픽스처 실증)

- inspector: 내용-추가 플로우(마커 노출+변화+라우트 유지)에서만 reload→마커 잔존
  확인(`persistedAfterReload`, 미측정 null=판정 무영향) + reload 스크린샷 증거.
  판정: visibleChange && persisted=false → Needs Fix — **네트워크·콘솔 신호가 전부
  침묵하는 '낙관적 유령'**(화면만 추가, 저장 없음)을 잡는 마지막 Potemkin 시험.
- 픽스처 F6 /optimistic-ghost 신설(판별쌍 F1 localStorage vs F6 무저장) + eval 타겟.
- **실증**(컨테이너 롤아웃 9분 대기 후): F6 → Needs Fix·works=false(사유에 새로고침
  단계 명시) / F1 → 7/17 기준선과 동일(무회귀). `E5` 절·eval-results-2026-07-18.
- 다음: **G9 법적 기반**(약관·개인정보·환불 — G6 결제의 전제) → G6 → 백로그 순서.
