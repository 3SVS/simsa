# HANDOFF — 2026-06-20

## Stage 75 — Outcome Quality Scorecard

실험이 "실제로 더 나은 제품 결과로 아이디어를 진전시켰는가?"를 **검수 결과 + 저장된 근거만으로**
결정론적으로 답하는 점수표. LLM 판단·"AI가 좋다고 함" 카피 일절 없음. **온디맨드 계산**(D1 테이블 추가 없음 —
점수 공식이 계속 진화할 것이므로 조기 영속화 = 마이그레이션 부채).

### 오늘 커밋 (main)
| 커밋 | 내용 |
|---|---|
| `bcc28ba` | Stage 75 — outcome quality scorecard (central-plane helper + GET 엔드포인트, dashboard Outcome quality 섹션, outcome.* i18n, 9+3+6 테스트) |

### 구현
- **central-plane** `src/workspace/experiment-outcome-scorecard.ts` — `computeOutcomeScorecard` 순수 헬퍼.
  basis 후보 = `selectedCandidateId` → benchmark winner → blocker basis → 첫 후보. metrics는 benchmark 계산 재사용(중복 없음).
  - score = `passed*3 − failed*3 − needsDecision*2 − inconclusive*1 + evidenceCovered*0.5`
  - grade = inconclusive(벤치마크/선택/항목 없음 또는 passRate null) → strong(≥0.85 & critical 0 & notVerified ≤1) → promising(≥0.65 & critical ≤1) → needs_work
  - nextEvolution(첫 매치): create_benchmark → clarify/rerun(미선택) → clarify(misaligned) → accept(strong) → fix_selected(잔여 blocker) → clarify(notVerified ≥2) → rerun(critical ≥2) → accept
  - suggestedFocusItemIds = non-passed 우선순위(failed>needs_decision>inconclusive) top5, reasons = 설명 코드
- **central-plane** `src/routes/workspace-experiment.ts` — `GET …/agent-experiments/:experimentId/outcome-scorecard?userKey=`
  (소유권 검증, candidate.benchmarkId로 벤치마크 로드, `{ok:true, scorecard}`)
- **dashboard** `src/lib/outcome-labels.mjs`(+`.d.mts`) 순수 매퍼 / `workspace-experiment-api.ts` `getOutcomeScorecard`+타입 /
  experiment 페이지 **Outcome quality** 섹션(grade 배지·메트릭 그리드·추천 다음 단계+이유·focus 칩·근거/수정/계획 버튼, openExp 변경 시 재로드) /
  i18n `outcome.*` (EN+KO+`.d.mts`)

### 카피 가드레일
"Best agent"/"AI says this is good" 금지. "Outcome quality" + 검수 기반 등급 + 상시 면책:
*"This scorecard is based on acceptance results and recorded evidence, not a subjective model judgment."*

### 검증
- central-plane **1005/1005**, dashboard **141/141**, repo typecheck **54/54**, lint clean, build green
- 신규 테스트: scorecard 헬퍼 9 + 엔드포인트 3 + outcome-labels 6
- **라이브** (central-plane deploy run `27855828511` success, dashboard `conclave-dashboard.vercel.app` READY):
  - userKey 누락 → 400, 미존재 실험 → 404, 타 사용자 → 403
  - 실제 실험 생성(`wexp_lo7e14ijug`, proj_stage75_demo) → 벤치마크 없음 → `grade:inconclusive`, `recommendedAction:create_benchmark`, reasons `[missing_benchmark, missing_selected_candidate]` ✅
  - benchmark+selected → graded 경로는 엔드포인트 테스트(실 handoff+decision 라운드트립)로 커버. 풀 라이브 시연은 실 PR review run 2개 필요(데모 프로젝트엔 없음 — known caveat, fake 안 함)

### 다음 세션 픽업 포인트 (Stage 75 시점)
1. **노출된 Vercel 토큰 revoke** (이번 세션 배포 사용).
2. (선택) 라이브에서 실 벤치마크 보유 실험으로 graded scorecard 육안 확인.

---

## Stage 76 — Evolution Action Pack

Stage 75 scorecard의 `recommendedAction` + `suggestedFocusItemIds`를 에이전트/팀원에게 바로 전달할 수 있는
**결정론적 copy-ready instruction pack**으로 변환. LLM·에이전트 자동실행·branch/commit/patch·persistence 테이블 없음.
대시보드에서 scorecard(+benchmark snapshot)로 온디맨드 생성.

### 오늘 커밋 (main, Stage 76)
| 커밋 | 내용 |
|---|---|
| (이 커밋) | Stage 76 — evolution action pack (dashboard helper + experiment 페이지 Evolution action pack 섹션 + evolution.* i18n + 9 tests) |

### Evolution action pack model
`apps/dashboard/src/lib/evolution-action-pack.mjs`(+`.d.mts`) 순수·결정론. 모든 노출 텍스트는 localized `s`(=`t.evolution`)로 주입(buildCandidatePrompt 컨벤션). `buildEvolutionActionPack(input, s)` / `buildEvolutionActionPackText(pack, s, meta)` / `resolveFocusItems` / `statusLabelFor`. 입력 타입(SavedExperiment/AgentBenchmarkResult/acceptanceItems)은 Stage 77 central-plane persistence 이전까지 안정.

### recommendedAction별 behavior
- accept: Decision·Evidence summary·Pre-merge checklist·Next review (코드 수정 지시 없음)
- fix_selected: Goal·Focus acceptance items·Constraints·Expected output·After completion (selected candidate 대상, "제품 의도 다시 쓰지 마라" 톤)
- rerun_experiment: Why rerun·Suggested setup·Candidate roles·How to compare (실험 자동생성 안 함)
- clarify_acceptance_items: Why clarify·Items needing clarification·Questions·After clarification (acceptance item 자동수정 안 함)
- create_benchmark: Why benchmark first·Required inputs·Steps·What to expect

### Focus item resolution
`suggestedFocusItemIds` → title는 basis candidate `benchmark.itemOutcomesByCandidate` → `remainingBlockers` → project acceptance items → **itemId fallback**(없는 title 발명 안 함). status 라벨: Issue found/Needs decision/Not verified.

### Dashboard UI
Outcome quality 섹션 내 **Evolution action pack** 서브카드 — Generate(benchmark snapshot fetch, best effort)·preview(action chip+target candidate+sections)·Copy(결정론 markdown). scorecard 로드 실패 시 needScorecard 안내. scorecard 있으면(create_benchmark/inconclusive 포함) 항상 생성 가능.

### Fix instructions와 구분 (rename 안 함)
Fix instructions=PR review/benchmark blocker로 한 구현 수정. Evolution action pack=outcome scorecard로 다음 루프(accept/fix/rerun/clarify/benchmark) 결정.

### i18n / tests
`evolution.*`(EN+KO+`.d.mts`, parity 10/10). `test/evolution-action-pack.test.mjs` 9개(액션별 pack, focus status, itemId fallback, acceptance-item title fallback, copy markdown+**userKey/token 미포함**). dashboard **150/150**·typecheck **54/54**·lint clean·build green.

### 라이브 검증
dashboard 재배포(`conclave-dashboard.vercel.app`). `/projects/:id/experiment` 렌더, scorecard 있으면 Generate+preview+Copy 동작. EN/KO 토글로 pack 현지화.

### Known limitations
- 생성은 dashboard-side·비영속(Stage 77 → central-plane).
- 풀 fix_selected pack(focus title 해석)은 benchmark+selected candidate 필요. create_benchmark/inconclusive pack은 benchmark 없이 동작.
- acceptance-item title fallback은 헬퍼에 배선됨; 대시보드는 현재 benchmark snapshot만 전달(title 이미 거기 존재). project-spec items 배선은 보류.

### 다음 세션 픽업 포인트 (Stage 76 시점)
1. **노출된 Vercel 토큰 revoke**.
2. (선택) 실 벤치마크+selected 실험으로 fix_selected action pack populated 육안 확인.
3. 스펙상 다음 후보: Stage 77 (action pack central-plane persistence).

---

## Stage 77 — Persisted Evolution Action Packs

Stage 76의 dashboard-side 온디맨드 액션 팩을 D1에 저장 가능한 workflow asset으로 승격.
서버가 canonical builder가 되어 클라이언트 pack 내용은 절대 신뢰하지 않음. LLM·에이전트 자동실행·branch/commit/patch·billing 변경 없음.

### 오늘 커밋 (Stage 77)
| 커밋 | 내용 |
|---|---|
| (이 커밋) | Stage 77 — persisted evolution action packs (migration + canonical helper + 3 endpoints + dashboard Save/List/Open/Copy + i18n + 27 tests) |

### Data model / migration
`apps/central-plane/migrations/0044_workspace_evolution_action_packs.sql` — `workspace_evolution_action_packs` 테이블(`pack_json`에 전체 snapshot, `benchmark_id`/`selected_candidate_id` nullable). 인덱스 2개(`project_id+user_key`, `experiment_id`). scorecard는 여전히 온디맨드.

### Central canonical helper
`apps/central-plane/src/workspace/evolution-action-pack.ts` — dashboard `.mjs`의 결정론 TypeScript 포트. `buildEvolutionActionPack` / `buildEvolutionActionPackText` / `resolveFocusItems` / `statusLabelFor`. `DEFAULT_EVOLUTION_STRINGS`는 canonical EN bundle로 라우트 핸들러가 주입 → 저장 pack은 결정론 영문. UI 크롬은 i18n 유지. 양쪽이 같은 golden fixture(`apps/central-plane/test/fixtures/evolution-action-pack-golden.json`, 5 케이스)로 lockstep.

### API endpoints
- `POST …/agent-experiments/:experimentId/evolution-action-packs` — body `{userKey}`. Stage 75 로직으로 scorecard 계산 + 벤치마크 로드 → canonical helper로 pack 빌드 → pack_json 저장 → `{id, recommendedAction, title, createdAt, pack, text}` 반환. 클라이언트 pack 내용 절대 신뢰 안 함.
- `GET …/evolution-action-packs` — lightweight list (`{id, experimentId, recommendedAction, title, createdAt}`). pack body 미포함.
- `GET …/evolution-action-packs/:actionPackId` — full detail. pack_json 재파싱 + experiment.title로 text 결정론 재생성(저장 안 함).

### Ownership 검증
userKey 없음 → 400, experiment.projectId 불일치/미존재 → 404, experiment.userKey 불일치 → 403, action_pack의 experiment+project 불일치 → 404, action_pack.userKey 불일치 → 403. 링크된 benchmark는 projectId+userKey 매치 시에만 로드.

### Dashboard Save / List / Open / Copy
`apps/dashboard/src/app/projects/[id]/experiment/page.tsx` Evolution action pack 서브카드:
- **Save action pack** — POST 호출 → 서버 pack이 클라이언트 preview 대체(spec contract: "Prefer server version after save")
- **Saved action packs** — 마운트 + 저장 시 GET list, lightweight items 렌더
- **Open saved pack** — GET detail → 섹션 카드 + Copy saved pack
- **Copy saved pack** — `actionPack.text` 클립보드
- scorecard 변경 시 열린 saved pack 초기화

API wrappers는 `apps/dashboard/src/lib/workspace-experiment-api.ts`에 `saveEvolutionActionPack` / `listEvolutionActionPacks` / `getEvolutionActionPack` + `SavedEvolutionActionPack(...)` 타입.

### i18n
`evolution.*`에 EN+KO 11개 키 추가(save, saved, saving, noSaved, open, copySaved, savedOk, saveFailed, createdAt, serverGenerated, serverGeneratedDesc). `.d.mts` 동기화. i18n parity 10/10.

### Tests / build
- central-plane helper `evolution-action-pack.test.mjs` — **8** (golden 5 + standalone 3, EVOLUTION_ACTIONS, resolveFocusItems fallback, deterministic text + no userKey/token, create_benchmark 4 sections).
- central-plane endpoints `workspace-evolution-action-pack.test.mjs` — **15** (POST: no benchmark→create_benchmark, with benchmark+selected→accept, missing userKey, unknown experiment, other user 403, no pack_json/text leak / GET list: empty, multiple, ownership / GET detail: full pack+text, pack from another experiment→404, other user 403, missing userKey, unknown id).
- dashboard parity `evolution-action-pack-parity.test.mjs` — **5** (golden 5 케이스로 `.mjs`가 canonical `.ts`와 lockstep).
- **central-plane 1027/1027** (Stage 76 1005 → +22) · **dashboard 155/155** (Stage 76 150 → +5) · **typecheck 54/54** · **lint clean** (pre-existing export warning 1개) · i18n parity 10/10.

### 라이브 검증
배포 후(필수: `pnpm migrate:apply`로 0044 적용):
- POST → 201 + actionPack + pack + text(결정론 markdown, userKey/token 미포함)
- GET list → 200 + lightweight items(pack 미포함)
- GET detail → 200 + full pack + 재생성 text
- userKey 누락 400, 미존재 404, 타 사용자 403, 다른 실험의 pack id 404
- 대시보드: Generate → Save → Saved list 갱신 → reload 시에도 유지 → Open → Copy. EN/KO 토글로 크롬 현지화.
- 벤치마크/decision 없는 실험에서는 `create_benchmark`/inconclusive pack persistence 검증(spec: "Do not fake populated success").

### Known limitations
- 저장 pack 본문은 canonical EN 스냅샷. UI 크롬만 i18n. 로케일 영속화는 Stage 78+ 결정.
- Pack은 immutable(Stage 77엔 PATCH/DELETE 없음). scorecard 변하면 새 pack 저장; 옛 pack은 save-time 근거 반영.
- 전용 라우트 없음(`/projects/:id/experiment/$EXP/action-pack/$ID` 미도입). 공유 링크 필요해지면 Stage 78에 추가.
- `acceptanceItems` 폴백은 helper에 배선; 서버는 현재 benchmark snapshot만 전달.
- `pack_json`은 구조 데이터만 저장; `text`는 매 GET마다 재생성. 향후 helper text 렌더 변경 시 옛 pack도 새 렌더 상속.

### Stage 78 결정 필요한 점
1. **저장 pack 로케일 처리** — save-time locale 기억 vs 매 read마다 클라이언트가 재렌더(structural pack을 직접 보여주기). 진짜 양언어 사용자 등장 시 결정.
2. **공유 가능한 saved-pack route** — `/projects/:id/experiment/.../action-pack/:id`. 필요 시 도입.
3. **Pack outcome 기록** — 사용자가 saved pack을 따랐는지 결과로 마킹해 다음 outcome scorecard가 "이 루프는 액션 팩 X를 따랐다"를 알게 할지.
4. **노출된 Vercel 토큰 revoke** (Stage 75/76 시점에서 계속 이월).

---

## Stage 78 — Action Pack Follow-up Tracking

Stage 77의 saved Evolution Action Pack을 실제 다음 evolution loop(follow-up PR / review run / benchmark)와 연결.
LLM 판단·에이전트 자동실행·benchmark 자동생성·branch/commit/patch·billing 변경 없음. Stage 75 scorecard 공식 불변.

### 오늘 커밋 (Stage 78)
| 커밋 | 내용 |
|---|---|
| (이 커밋) | Stage 78 — action pack follow-up tracking (migration 0045 + PATCH endpoint + list/detail follow-up 노출 + dashboard 추적 폼 + Mark-as-copied + i18n + 26 tests) |

### Follow-up data model / migration
`apps/central-plane/migrations/0045_workspace_evolution_action_pack_followups.sql` — `workspace_evolution_action_packs`에 nullable 6컬럼 ALTER 추가(`followup_status`, `followup_pull_request_number`, `followup_review_run_id`, `followup_benchmark_id`, `followup_note`, `followed_at`). DB-level default 없이 API-layer에서 null → `"not_started"` normalize. Option A(단일 테이블 ALTER) 채택 — MVP 단순성 우선.

### Status allowlist
`not_started · copied · in_progress · reviewed · benchmarked · completed · abandoned` (central `FOLLOWUP_STATUSES`, dashboard helper 양쪽 export, parity 테스트로 검증).

### Follow-up API endpoint
`PATCH /workspace/projects/:id/agent-experiments/:experimentId/evolution-action-packs/:actionPackId/followup` — body `{userKey, status, pullRequestNumber?, reviewRunId?, benchmarkId?, note?}`.
- status allowlist enforcement
- pullRequestNumber 양의 정수 검증
- reviewRunId/benchmarkId는 projectId+userKey 매치 시에만 link(다른 owner 데이터 누출 방지)
- note ≤ 1000자
- `followed_at`은 첫 not_started 탈출 시 stamp, 이후 transition에선 유지(안정적 "first followed at")
- review/benchmark 자동생성 안 함

### Ownership / 검증 behavior
Stage 77 ownership 그대로 + reviewRunId/benchmarkId 재검증(getReviewRunById/getAgentBenchmarkById의 projectId+userKey 매치). 에러: `invalid_status`, `invalid_pr_number`, `review_run_not_found`/`review_run_mismatch`, `benchmark_not_found`/`benchmark_mismatch`, `invalid_note`/`note_too_long`, `not_found`/`forbidden`.

### Saved action pack list/detail 변경
- list item에 `followupStatus`(normalize 되어 항상 enum) + `followupPullRequestNumber`/`followupReviewRunId`/`followupBenchmarkId`/`followedAt` 추가
- detail에 `followup: {status, pullRequestNumber?, reviewRunId?, benchmarkId?, note?, followedAt?}` 객체 추가
- POST 응답에도 `followup: {status: "not_started"}` 포함

### Dashboard follow-up UI
`/projects/:id/experiment` 열린 saved pack 카드에 **Follow-up** 서브카드:
- 현재 status badge(현지화) + status select(7개)
- PR number `<input type="number" min={1}>`
- Review run ID free-text input(`wprr_…`)
- Benchmark ID free-text input(`wab_…`)
- Note textarea(maxLength=1000)
- **Save follow-up** 버튼(saving/saved/error 상태)
- **Mark as copied** 버튼(Copy saved pack과 분리)
- `Followed at: …` 타임스탬프(stamp 된 후 표시)

Saved-list 행에 색상 코드된 status pill + PR 배지 추가(completed/benchmarked 그린, abandoned 그레이, in-flight 인디고).

### Copy / Mark-as-copied behavior
Spec 권장대로 **분리**. Copy saved pack(Stage 77)은 클립보드 쓰기만 → followup PATCH 호출 안 함. Mark as copied(Stage 78)는 명시적 `{status: "copied"}` PATCH → followed_at stamp. "복사≠사용"이라서 audit trail 분리.

### i18n
`evolution.*`에 EN+KO 22개 키 추가 + `.d.mts` 동기화(followup, followupStatus, followupDesc, markCopied, saveFollowup, savingFollowup, followupSaved, followupFailed, status7개, followupPullRequestNumber, followupReviewRun, followupBenchmark, followupNote, followedAt). i18n parity 10/10. status label 키는 central allowlist와 완전 1:1.

### Tests / build
- central-plane endpoints **18** 추가(POST 응답 followup 포함, list normalize, PATCH status-only/stamping/no-stamp, PR number persistence, reviewRunId 매치/미스매치/미존재, status invalid, PR invalid, missing userKey/other user/unknown pack/cross-experiment 404, note too long/within limit, followedAt 안정성, detail 재로드 persistence)
- dashboard helper **8** 추가(`action-pack-followup.test.mjs`: status allowlist 정렬, 모든 status가 dictionary 키 매치, 폴백, payload builder optional 드롭, invalid PR number 거부, list-item 매핑)
- **central-plane 1045/1045** (Stage 77 1027 → +18)
- **dashboard 163/163** (Stage 77 155 → +8)
- **typecheck 54/54** · **lint clean**(pre-existing export warning 1개) · i18n parity 10/10

### 라이브 검증
배포 후(필수: 0045 migration apply):
- PATCH status only → 200 + followedAt stamp · status not_started 유지는 stamp 안 함
- PATCH pullRequestNumber/reviewRunId/benchmarkId/note persistence
- userKey 누락 400, status invalid 400, PR < 1 400, reviewRunId 다른 owner 400, 미존재 400, 타 사용자 403, 미존재 pack 404, 다른 experiment의 pack id 404
- 대시보드: Open saved pack → Follow-up form 자동 채움 → status 변경 + Save → list pill 갱신 + Followed at 표시 / Mark as copied → Copied pill / EN/KO 토글
- 실 follow-up review run 없으면 status+PR번호+note 경로만 검증("Do not fake populated success")

### Known limitations
- status transition state-machine 미적용(allowlist에만 의존). Stage 79에 필요 시 layered.
- pack당 follow-up 슬롯 1개(같은 pack을 두 PR에 쓰면 두 번째가 덮어씀). 다중 시도 추적은 별도 stage.
- saved-pack outcome 비교(원본 vs follow-up benchmark)는 미구현 — Stage 79 영역.
- note는 free-text(구조 필드 없음).
- review_run/benchmark 입력은 free-text(드롭다운 미적용). 실험 페이지에 review history 별도 섹션 있음.
- 전용 saved-pack route 미도입(Stage 77 limitation 그대로 이월).

### Stage 79 결정 필요한 점
1. **Pack outcome 비교** — 기록된 follow-up review run/benchmark id로 "액션 팩 적용 전 vs 후"(unresolved blocker 감소, pass rate 변화 등) 계산해 scorecard에 "이 루프는 액션 팩 X를 따랐다" annotation으로 노출할지.
2. **status state-machine** — comparison 데이터가 정당화하면 illegal transition 차단(예: benchmarked는 followup_benchmark_id 필수).
3. **Federated sync for follow-up signals** — 익명화된 `{action, status, outcome_delta, day_bucket, sha256}`로 어떤 recommendedAction이 실제로 효과 있는지 federation이 학습하게 할지.
4. **공유 saved-pack route**(Stage 77/78 이월) — 팀원/에이전트에게 loop record 공유 필요해지면 도입.
5. **노출된 Vercel 토큰 revoke** (계속 이월).

---

## Operating rule change (2026-06-21~)

Stage 79부터 적용되는 새 운영 룰:

- Stage 단위 main push / production deploy / remote D1 migration apply **금지**.
- 작업 브랜치: `claude/stage-79-82-evolution-loop` (또는 명시적으로 지정된 브랜치).
- Local commit은 허용, push는 Bae가 명시적으로 승인할 때만.
- live verification 안 함 — local test/build/typecheck 결과만 보고.
- 보고서에 다음 4줄 필수: Push skipped · Production deploy skipped · Remote migration apply skipped · Live verification skipped (operating rule).
- Release checkpoint trigger 문구("Release checkpoint 진행해", "이제 push/deploy 해", "Stage 79~82 묶어서 배포해")가 있을 때만 push/deploy.
- Vercel/GitHub/Cloudflare 토큰은 문서/로그/커밋/보고 어디에도 쓰지 않음.

---

## Stage 79 — Before/After Evolution Impact Comparison

Stage 78의 follow-up 정보(reviewRunId / benchmarkId)를 활용해 action pack 적용 전후 acceptance outcome 변화를 결정론적으로 비교.
LLM 판단·에이전트 자동실행·benchmark 자동생성·persistence 추가 없음. Stage 75 scorecard 공식 불변.

### 오늘 local commit (Stage 79)
| 커밋 | 내용 |
|---|---|
| (local, no push) | Stage 79 — before/after evolution impact (canonical helper + GET impact endpoint + dashboard auto-load card + dashboard helpers + i18n + 31 tests). **Push: skipped by operating rule.** |

### Impact comparison model
`apps/central-plane/src/workspace/evolution-impact.ts` — 순수 결정론 helper.
- `snapshotFromBenchmark` — 후보 metrics에서 snapshot 추출(selected→packTarget→winner→basis→first 우선순위)
- `snapshotFromReviewRun` — review run `resultJson.results[]`에서 status 카운트
- `buildImpactComparison` — verdict / reasons / delta 계산
Snapshot에 itemIds(정렬 dedupe) 포함 → acceptance set alignment 가드.

### Before / after source selection
- **Before**: `actionPack.benchmarkId` 컬럼(Stage 77) → fallback 실험 후보들의 첫 benchmarkId → 없으면 `before = null + missing_before`.
- **After**: `followup_benchmark_id`(Stage 78) → `followup_review_run_id` fallback → 없으면 `after = null + missing_followup + missing_after`.
- 양쪽 모두 projectId+userKey 매치 시에만 로드(타 owner는 `limitations`에만 기록, 데이터 누출 없음).

### Delta / verdict rules
```
passRateDelta = after - before (양쪽 null이면 null)
critical/notVerified/blocker/passedDelta = after - before
```
개선 신호: passRate↑ / critical↓ / blocker↓ / notVerified↓
악화 신호: passRate↓ / critical↑ / blocker↑ (notVerified↑는 spec상 악화 신호 아님)
Verdict 순서: missing → different_acceptance_set → mixed_signals → improved → regressed → unchanged.

### API endpoint
`GET /workspace/projects/:id/agent-experiments/:experimentId/evolution-action-packs/:actionPackId/impact?userKey=...` — userKey/owner 검증 후 deterministic impact 반환. **on-demand 계산, D1 영속화 없음**(formula 진화 중).

### Dashboard impact UI
열린 saved pack 카드의 Follow-up 다음에 **Evolution impact** 서브카드 자동 로드:
- verdict pill 색상(improved 에메랄드 / regressed 레드 / unchanged 그레이 / inconclusive 앰버)
- Before · After · Delta 3-그리드(passRate / critical / not-verified / blockers)
- Reasons chip 리스트 + Limitations chip 리스트(raw code 보존)
- empty/inconclusive 안내 로컬라이즈
- `apps/dashboard/src/lib/evolution-impact.mjs`(+`.d.mts`) display 헬퍼: `impactVerdictLabelKey`/`impactReasonLabelKey`/`formatDeltaInt`/`formatDeltaPercent`/`formatRate`/`isImpactEmpty`

### i18n
`evolution.*`에 EN+KO **38 키** 추가 + `.d.mts` 동기화(impact, impactDesc, calculateImpact, impactVerdict, verdict4개, impactBefore/After/Delta, delta4개, impactMissingFollowup, impactInconclusiveExplanation, reason12개, impactReasons/Limitations, impactPassRate/Blockers/Critical/NotVerified). i18n parity 10/10.

### Tests / build (local only)
- 신규 central helper **14** + central endpoints **9** + dashboard helpers **8** = **31** tests
- **central-plane 1068/1068** (Stage 78 1045 → +23)
- **dashboard 171/171** (Stage 78 163 → +8)
- **typecheck 54/54** · **lint clean** (pre-existing export warning 1개) · i18n parity 10/10
- Next build은 sandbox에서 Google Fonts(`SELF_SIGNED_CERT_IN_CHAIN`)으로 실패 — Stage 79 코드 무관, 환경 이슈 명시(Stage 77/78 동일).

### 운영 상태
- **Push: skipped by operating rule.**
- **Production deploy: skipped by operating rule.**
- **Remote migration apply: skipped by operating rule** (Stage 79는 마이그레이션 추가 없음).
- **Live verification: skipped by operating rule.**

### Known limitations
- impact persistence 없음(매 GET마다 재계산).
- 아이템 정렬 비교는 정확 매칭만(`different_acceptance_set` 가드, 퍼지 매칭 없음).
- 타 owner follow-up 데이터는 `limitations`로 흡수(403 아님) — 방어적 redundancy.
- basis candidate는 scorecard와 동일 정책(selected→target→winner→basis→first). 결정 변경 시 before 스냅샷이 *현재* 선택 따라감(Stage 80에서 save-time lock 가능).
- verdict threshold 0 기반(±1%도 신호로 카운트). 실 데이터에서 노이즈 보면 epsilon 도입 검토.
- CSV/export 없음.

### Stage 80 결정 필요한 점
1. **Aggregate impact 뷰** — 실험당 saved pack 여러 개의 verdict 분포("5개 중 3 improved").
2. **Federated impact signal** — 익명화된 `{recommendedAction, verdict, hasFollowup, day_bucket, sha256}`.
3. **Impact 기반 status state-machine** — strong improved일 때 followup_status를 completed로 user-confirmed 권유.
4. **Save-time basis lock** — pack_json에 basis/selected 박제해 결정 변경에 영향 안 받게.
5. **Configurable threshold** — meaningful delta epsilon.
6. (계속 이월) Vercel 토큰 revoke.

---

## Stage 80 — Experiment Evolution Impact Summary

Stage 79의 per-pack impact를 experiment 단위로 집계해 "이 실험의 evolution loops가 전체적으로 개선 신호를 만들었는가?"를 결정론적으로 요약. Stage 79 `buildImpactComparison`을 **재사용**(verdict rule 재구현 없음). LLM·에이전트 자동실행·persistence 추가 없음. Stage 75 scorecard 공식 불변.

### 오늘 local commit (Stage 80)
| 커밋 | 내용 |
|---|---|
| (local, no push) | Stage 80 — experiment evolution impact summary (aggregator + GET summary endpoint + shared loadImpactForActionPack helper + dashboard auto-load card + helpers + i18n + 25 tests + test mock outcome handler bonus fix). **Push: skipped by operating rule.** |

### Aggregate model
`apps/central-plane/src/workspace/evolution-impact-summary.ts` — 순수 deterministic. `buildEvolutionImpactSummary({projectId, experimentId, entries})` → `EvolutionImpactSummary{actionPackCount, followedPackCount, verdictCounts, recommendedActionCounts, recommendedActionVerdicts(알파벳 정렬), averageDelta(unweighted mean), overallVerdict, reasons, limitations(unique+sorted)}`.

### Reuse of Stage 79
새로운 shared `loadImpactForActionPack(env, row, opts)` helper로 Stage 79 GET impact 엔드포인트와 Stage 80 GET summary 엔드포인트가 같은 코드 경로 사용 → 두 surface 간 verdict drift 가능성 0.

### Followed pack 판단
4-조건 OR(`followup_status !== "not_started"` OR `followup_review_run_id` OR `followup_benchmark_id` OR `followup_pull_request_number`). Mark as copied 단독도 followed로 카운트(but impact는 inconclusive 유지).

### Average delta / overall verdict
- **Average delta**: metric별로 non-null 값만 unweighted mean. 전부 null이면 metric=null + reason에 `not_enough_comparable_data`. 스펙상 item-count weighting 금지(Stage 81+).
- **Overall verdict**(first match): actionPackCount==0 → `no_followups+no_saved_action_packs` / followedPackCount==0 → `no_followups+no_followups` / inconclusive≥70% → `mostly_inconclusive` / regressed>improved → `regressed+regressions_detected` / improved>regressed && improved>0 → `mostly_improved+more_improved_than_regressed` / else → `mixed+mixed_results`.
- 문장은 evidential만(주관 금지).

### API endpoint
`GET /workspace/projects/:id/agent-experiments/:experimentId/evolution-impact-summary?userKey=...` — owner 검증 → list packs → per-pack getFull + loadImpactForActionPack → aggregator → `{ok, summary}`. 후보-fallback benchmarkId 1회 hoist. **D1 영속화 없음**.

### Dashboard summary UI
Saved action packs 리스트 **바로 위**에 Evolution impact summary 카드. savedPacks 변화 시 자동 재요청. 색상 verdict pill, 2-empty state, 6-셀 metric 그리드, average change 4-칸, recommendedAction 분석 리스트(`total · ↑ · ↓ · = · ?`), reasons/limitations.

### i18n
`evolution.*`에 EN+KO **30 키** + `.d.mts` 동기화. i18n parity 10/10.

### Tests / build (local only)
- 신규 central aggregator **11** + central endpoints **8** + dashboard helpers **6** = **25** tests
- **Bonus**: Stage 78 test mock의 누락된 `SET outcome` candidate UPDATE handler 추가(updateCandidateOutcome이 link handler에 빠져 `benchmark_id`를 "selected"로 덮어쓰는 latent 버그 fix — Stage 80에서 decision+POST를 같은 env에서 실행하면서 노출)
- **central-plane 1087/1087** (Stage 79 1068 → +19)
- **dashboard 177/177** (Stage 79 171 → +6)
- **typecheck 54/54** · **lint clean** · i18n parity 10/10
- Next build은 sandbox에서 Google Fonts(`SELF_SIGNED_CERT_IN_CHAIN`)으로 실패 — Stage 80 코드 무관

### 운영 상태
- **Push: skipped by operating rule.**
- **Production deploy: skipped by operating rule.**
- **Remote migration apply: skipped by operating rule** (Stage 80은 마이그레이션 추가 없음).
- **Live verification: skipped by operating rule.**

### Known limitations
- 영속화 없음(매 GET 재계산).
- Mark as copied만 한 pack도 followed로 카운트 → impact는 inconclusive 유지(스펙대로).
- average delta는 unweighted — 4-item과 40-item pack이 동등 기여. 실 데이터로 weighting 결정(Stage 81+).
- N+1 쿼리(list + per-pack getFull + 벤치마크/리뷰 조회). 1~10 packs 규모 OK.
- experiment 단위만(project 단위 rollup은 Stage 81+).

### Stage 81 결정 필요한 점
1. **Project-level rollup** — "프로젝트 P의 모든 실험에서 X improved / Y regressed".
2. **Federated impact signal** — 익명화 `{recommendedAction, overallVerdict, packCount, day_bucket, sha256}`.
3. **Item-count weighted averages** feature flag — 실 데이터로 unweighted vs weighted 비교.
4. **Summary persistence(at decision)** — 실험 완료 시점 스냅샷.
5. **Status state-machine 권유** — mostly_improved + 모든 followed pack에 benchmark면 실험 자체 completed 제안.
6. (계속 이월) Vercel 토큰 revoke.

---

## Stage 81 — Project Evolution Learning Signals

Stage 79의 per-pack과 Stage 80의 per-experiment impact를 **프로젝트 전체**로 집계해 어떤 `recommendedAction`이 실제로 outcome 개선을 만들었는지 학습 신호로 노출. Stage 79 `loadImpactForActionPack`을 그대로 재사용(verdict rule 재구현 없음). LLM·에이전트 자동실행·persistence·federated signal 모두 추가 없음. Stage 75/79/80 공식 불변.

### 오늘 local commit (Stage 81)
| 커밋 | 내용 |
|---|---|
| (local, no push) | Stage 81 — project evolution learning signals (canonical aggregator + GET project learning endpoint + dashboard project-page learning card + helpers + i18n + 26 tests + mock listExperiments handler). **Push: skipped by operating rule.** |

### Project-level learning model
`apps/central-plane/src/workspace/project-evolution-learning.ts` — 순수 deterministic. `buildProjectEvolutionLearning({projectId, experimentCount, entries})` → `ProjectEvolutionLearningSignals{experimentCount, actionPackCount, followedPackCount, comparablePackCount, verdictCounts, recommendedActionEffectiveness(알파벳 정렬, improvementRate/regressionRate per action), averageDelta(unweighted), topSignals(cap 5), limitations(dedup+sorted)}`.

`ProjectLearningSignal` 유니온: `action_often_improves` / `action_often_regresses` / `not_enough_data`.

### Reuse of Stage 79
실험 owner 필터 → 각 owned 실험의 후보-fallback benchmarkId 1회 hoist → 각 액션 팩에 대해 **Stage 79 `loadImpactForActionPack`** 그대로 호출 → entries 빌드. 3 surface(per-pack / per-experiment / project-level) 모두 같은 헬퍼로 verdict 계산 → drift 0.

### Comparable / effectiveness rules
- **Comparable**: `delta !== null && verdict !== "inconclusive"`. mixed_signals/missing_*/different_acceptance_set 모두 제외. `unchanged`는 zero delta로 comparable에 포함.
- **improvementRate** = improved/comparable, **regressionRate** = regressed/comparable. comparable=0이면 null.
- **Top signals**:
  - comparablePackCount < 3 → `[not_enough_data]`
  - action별 comparable ≥ 2일 때 `improvementRate ≥ 0.67` → `action_often_improves`, `regressionRate ≥ 0.5` → `action_often_regresses`
  - 결과 비면 `not_enough_data` 추가, cap 5
- 문장 evidential 강제(`Early signal: fix_selected often improved (3/3)`), 과장 금지.

### API endpoint
`GET /workspace/projects/:id/evolution-learning?userKey=...` — listExperiments → owner 필터 → per-experiment listPacks → per-pack getFull → loadImpactForActionPack → aggregator → `{ok, learning}`. **D1 영속화 없음**.

### Dashboard learning UI
**프로젝트 상세 페이지**(`/projects/:id`) requirements 다음 섹션. `EvolutionLearningCard`가 userKey 자동 로드 → mount + userKey resolve 시 GET 호출.
- 4-셀 stat 행(Experiments / Action packs / Followed / Comparable)
- 빈 상태 안내(스펙 문구)
- 데이터 있으면 verdict counts(색상) + Average change 4-칸 + recommendedAction effectiveness 표(`comparable/total ↑↓?` + 개선/악화율) + Top signals 리스트 + Limitations chip
- 마지막 줄 disclaimer: `This is based on project-local evidence, not a global model judgment.`

`apps/dashboard/src/lib/project-evolution-learning.mjs`(+`.d.mts`) display 헬퍼: `topSignalLabelKey` / `formatRatePercent`(부호 없음) / `formatAverageDeltaPercent`(부호 있음) / `formatAverageDeltaCount`(소수점 1, 부호 있음) / `learningHasNoData`.

### i18n
`evolution.*`에 EN+KO **22 키** + `.d.mts` 동기화(learningTitle/Desc/Disclaimer, 4 stat label, 5 analysis label, 2 empty state, 6 signal copy). i18n parity 10/10.

### Tests / build (local only)
- 신규 central aggregator **13** + central endpoints **7**(missing userKey, 빈 프로젝트, 실험만 있고 팩 없음, inconclusive만, action_often_improves end-to-end, cross-tenant isolation, 토큰 누출 없음) + dashboard helpers **6** = **26** tests
- 추가 mock: `listExperiments(projectId)` 결과 매핑(이전엔 Stage 77-80 엔드포인트가 enumerate 안 했음)
- **central-plane 1107/1107** (Stage 80 1087 → +20)
- **dashboard 183/183** (Stage 80 177 → +6)
- **typecheck 54/54** · **lint clean** · i18n parity 10/10
- Next build은 sandbox에서 Google Fonts(`SELF_SIGNED_CERT_IN_CHAIN`)으로 실패 — Stage 81 코드 무관

### 운영 상태
- **Push: skipped by operating rule.**
- **Production deploy: skipped by operating rule.**
- **Remote migration apply: skipped by operating rule** (Stage 81은 마이그레이션 추가 없음).
- **Live verification: skipped by operating rule.**

### Known limitations
- 영속화 없음(매 GET 재계산).
- 프로젝트 N+M+P 쿼리(1 list + M getExp + M cands + M listPacks + P getFull + P 벤치마크/리뷰). < 50 experiment까지 OK(Stage 72 limit).
- `listExperiments` 50개 cap → 51번째부터 silently 제외.
- cross-project 뷰 없음(per-project만).
- `Mark as copied`만 한 pack은 followed지만 comparable 아님(impact는 inconclusive 유지).
- top-signal 임계값(0.67/0.5/3/2) 하드코딩 — 실 데이터로 튜닝.
- trend/history 없음(현재 스냅샷만).
- federated signal 없음(Stage 81 prohibition).

### Stage 82 결정 필요한 점
1. **Federated impact signal export** — 익명화 opt-in `{kind, recommendedAction, verdict, comparable, day_bucket, sha256}` 전송. 결정 #21 wire format 따름.
2. **Trend over time** — (week, project, improvementRate) sliding window → "프로젝트의 개선율 추세".
3. **Configurable thresholds** — 프로젝트별 improvement/regression bar 조정.
4. **Project-level summary persistence(at experiment-completion)** — 결정 시점 스냅샷 박제.
5. **Cross-project view(org-level)** — 멀티-프로젝트 사용자용 leaderboard.
6. (계속 이월) Vercel 토큰 revoke.

---

## Stage 82 — Project Evolution Timeline

프로젝트의 evolution(experiment → benchmark → decision → action pack → follow-up → impact)을 시간순 단일 스토리로 노출. Conclave가 단순 대시보드가 아니라 "AI-built software evolution record"임을 증명. Stage 79 `loadImpactForActionPack` 그대로 재사용(4번째 surface). LLM·subjective summary·persistence·federated signal 모두 추가 없음. Stage 75/79/80/81 공식 불변.

### 오늘 local commit (Stage 82)
| 커밋 | 내용 |
|---|---|
| (local, no push) | Stage 82 — project evolution timeline (canonical builder + GET timeline endpoint + dashboard project-page timeline card + helpers + i18n + 29 tests). **Push: skipped by operating rule.** |

### Timeline model
`apps/central-plane/src/workspace/project-evolution-timeline.ts` — 순수 deterministic. `buildProjectEvolutionTimeline({projectId, experiments, benchmarks, actionPacks})` → `ProjectEvolutionTimeline{projectId, eventCount, events, limitations}`.

**9 event types**: `experiment_created`, `benchmark_created`, `decision_recorded`, `action_pack_saved`, `followup_recorded`, `impact_improved`, `impact_regressed`, `impact_unchanged`, `impact_inconclusive`. Title은 캐논 영어, 대시보드가 i18n `timelineEventLabelKey(type)`으로 재현지화.

### Source data / event 구성
| Event | Source | occurredAt |
|---|---|---|
| experiment_created | experiments[*] | exp.createdAt |
| decision_recorded | exp.decidedAt | exp.decidedAt |
| benchmark_created | candidates에서 unique benchmarkId 수집 후 owner 매치 | benchmark.createdAt |
| action_pack_saved | packs[*] | pack.createdAt |
| followup_recorded | pack.followup.followedAt | followedAt |
| impact_* | Stage 79 `loadImpactForActionPack` | followedAt |

스펙 권장대로 **impact 이벤트는 followedAt 있을 때만**(fresh-unused pack은 benchmark/review fetch 자체 건너뜀). 4 surface(per-pack/per-experiment/project-learning/project-timeline) 모두 같은 helper로 verdict 계산 → drift 0.

### Sorting / limit rules
`occurredAt DESC, id ASC` (golden 안정성). Cap 50, 초과 시 `limitations`에 `timeline_truncated`.

### API endpoint
`GET /workspace/projects/:id/evolution-timeline?userKey=...` — owner 필터 → list events → builder → `{ok, timeline}`. **D1 영속화 없음**.

### Dashboard timeline UI
**프로젝트 상세 페이지**(`/projects/:id`) Evolution learning signals **다음**에 새 `Evolution timeline` 섹션. `EvolutionTimelineCard`가 userKey 자동 로드 → mount + userKey resolve 시 GET 호출.
- 각 event row는 색상 type pill(impact_improved 에메랄드 / impact_regressed 레드 / impact_unchanged 그레이 / impact_inconclusive 앰버 / decision_recorded 인디고 / benchmark_created 블루 / experiment_created 슬레이트 / action_pack_saved 퍼플 / followup_recorded 틸)
- 현지화 type label + 선택적 status / recommendedAction mono chip + summary + 로컬화 timestamp
- 서버 emit relative `href`로 Next `<Link>` Open 버튼: experiment/action-pack/followup/impact → `/projects/:id/experiment?experiment=...`, benchmark → `/projects/:id/benchmark/<id>`
- Limitations row 하단(timeline_truncated는 앰버 pill로 i18n)

`apps/dashboard/src/lib/project-evolution-timeline.mjs`(+`.d.mts`) display 헬퍼: `TIMELINE_EVENT_TYPES`, `timelineEventLabelKey`, `timelineLimitationLabelKey`, `timelineHasNoEvents`.

### i18n
`evolution.*`에 EN+KO **15 키** + `.d.mts` 동기화(timelineTitle/Desc/Empty/Open, 9 event label, timelineTruncated). i18n parity 10/10.

### Tests / build (local only)
- 신규 central builder **15**(empty, 9 event 분기, decision/followup 조건, impact 4 verdict 매핑, sort desc + tie-break, cap 50 + truncated, href encoding, 토큰 누출 없음) + central endpoints **9**(missing userKey, empty project, experiment/action_pack/followup/impact 이벤트, sort desc, cross-tenant isolation, 토큰 누출 없음) + dashboard helpers **5**(9 event type 라벨 parity vs dictionary, fallback, limitation 매핑 + passthrough, empty predicate, inline chrome) = **29** tests
- **central-plane 1131/1131** (Stage 81 1107 → +24)
- **dashboard 188/188** (Stage 81 183 → +5)
- **typecheck 54/54** · **lint clean** · i18n parity 10/10
- Next build은 sandbox에서 Google Fonts(`SELF_SIGNED_CERT_IN_CHAIN`)으로 실패 — Stage 82 코드 무관

### 운영 상태
- **Push: skipped by operating rule.**
- **Production deploy: skipped by operating rule.**
- **Remote migration apply: skipped by operating rule** (Stage 82는 마이그레이션 추가 없음).
- **Live verification: skipped by operating rule.**

### Known limitations
- 영속화 없음(매 GET 재계산).
- Cap 50, 초과 시 pagination 없음(Stage 83+).
- N+M+P+B 쿼리(experiments + candidates + packs + impacts + benchmarks).
- Event 필터/타입 토글 없음(Stage 83+).
- 서버 relative href emit → 대시보드 URL 구조와 느슨한 결합.
- 주/일 grouping 없음(flat list).
- federated/cross-project 없음.

### Stage 83 전 결정 필요한 점
Stage 79~82 evolution-record 아크 완료. 다음 후보:
1. **Release checkpoint** — Stage 77~82 마이그레이션 remote D1 적용 + central-plane/dashboard 배포 + live verification. 운영 룰상 명시적 trigger("Release checkpoint 진행해" / "이제 push/deploy 해" / "Stage 79~82 묶어서 배포해") 필요.
2. **Federated impact signal export** — 결정 #21 wire format. 실 cross-project 데이터가 release 이후에 생김.
3. **Timeline event 필터링**(타입 chip) — 실 프로젝트 이벤트 풍부해지면.
4. **Configurable threshold / state machine** — `mostly_improved` + 모든 followed pack에 benchmark면 experiment auto-complete 제안.
5. **Trend over time** — Stage 82 타임스탬프 인프라 깔렸으니 sliding window 저렴.
6. (계속 이월) Vercel 토큰 revoke.

---

## Stage 83 — Release Checkpoint Preparation

Stage 77~82 evolution-record 아크의 release 준비. **새 product feature 추가 없음**: inventory, migration review, API endpoint 정리, 대시보드 smoke 체크리스트, production risk review, rollback plan, go/no-go 체크리스트 작성만. 실제 push/deploy/remote migration apply/live verification은 운영 룰상 Bae 명시적 승인 전까지 불가.

### 오늘 local commit (Stage 83)
| 커밋 | 내용 |
|---|---|
| (local, no push) | Stage 83 — release checkpoint preparation (release inventory + migration review + API/dashboard inventory + risk review + rollback plan + go/no-go checklist). **Push: skipped by operating rule.** |

### Release inventory(요약)
- **6 stages**: Stage 77(c416eed) → 82(9b916c8). 44 files, +9,757 / −4.
- **2 migrations**: `0044_workspace_evolution_action_packs.sql` (Stage 77 — CREATE TABLE + 2 indexes), `0045_workspace_evolution_action_pack_followups.sql` (Stage 78 — ALTER TABLE ADD COLUMN × 6 nullable).
- **8 신규 endpoints** (action pack POST/GET 리스트/GET 디테일/PATCH followup + 4 GET impact/summary/learning/timeline).
- **2 변경된 dashboard 페이지**: `/projects/:id` (Stage 81/82 카드), `/projects/:id/experiment` (Stage 77/78/79/80 카드).
- **6 신규 helper(central)** + **6 신규 helper(dashboard)** + **8 API wrapper** + **156개 i18n 키** EN/KO/`.d.mts` 동기화.
- **+126 central tests** (1005→1131), **+38 dashboard tests** (150→188).

### Migration review
- `0044`: `CREATE TABLE IF NOT EXISTS` + 인덱스 2개. 멱등, 기존 테이블 영향 없음.
- `0045`: `ALTER TABLE ADD COLUMN` × 6 (전부 nullable, default 없음). 추가형, backward-compatible.
- **순서**: 0044 → 0045 → central-plane deploy → dashboard deploy. 한 번에 적용 권장.
- **migration 전 endpoint 동작**: D1 에러 → 500 `save_failed`/`update_failed`. 대시보드는 localized 에러 상태로 fail closed.
- **롤백**: SQLite는 DROP COLUMN 미지원이므로 마이그레이션 롤백 권장 안 함. 코드 롤백만(R7 마이그레이션은 모두 nullable이라 안전).

### API endpoint inventory
8개 신규 endpoint 전부 `userKey` 필수 + per-route ownership 검증(projectId + experimentId + actionPackId 매치). 외부 link(reviewRunId, benchmarkId)는 link 시 (projectId, userKey) 재검증 → cross-tenant leakage 없음. 각 endpoint에 happy-path + guardrail smoke 정리(분리된 stage-83 doc 참조).

### Dashboard smoke checklist
릴리스 시 manual 점검(Stage 83에서 실행 안 함):
- **`/projects/:id`**: page load · learning card empty/data · timeline card empty/data · color pill · href 네비게이션 · `timeline_truncated` pill · EN/KO 토글
- **`/projects/:id/experiment`**: 액션 팩 생성/저장/복사/Mark as copied · follow-up form · impact 카드 · summary 카드 · EN/KO 토글

### Local verification 결과
- **central-plane 1131/1131** · **dashboard 188/188** · **typecheck 54/54** · **lint clean** (pre-existing `/export/page.tsx` 경고만) · i18n parity 10/10
- Next build은 sandbox env에서 Google Fonts(`SELF_SIGNED_CERT_IN_CHAIN`)으로 실패 — 코드 무관.
- `mcp-workspace` test는 build 먼저 돌리면 14/14 pass — pre-existing turbo 의존성 순서 이슈, Stage 77~82와 무관.

### Production risk review(요약)
| # | Risk | 등급 | 완화 |
|---|------|------|------|
| R1 | 마이그레이션 미적용 상태로 central-plane deploy | **High** | 순서 강제(D1 → worker → dashboard) |
| R2 | dashboard가 worker보다 먼저 deploy됨 | Medium | fail closed → 일시적 빈 상태, 복구 가능 |
| R4 | 마이그레이션 부분 적용(0044만 성공) | Medium | wrangler fail-fast + re-run으로 0045 적용 |
| R11 | 노출된 Vercel 토큰 (Stage 75부터 이월) | **Medium** | release 전 revoke + rotate **필수** |
| R10 | Vercel build env에서 첫 폰트 fetch flake 가능성 | Low | 빌드 로그 모니터링, 필요 시 §7 롤백 |
| R6 | cross-tenant leakage | Low | 모든 endpoint가 (projectId, userKey) 재검증 + isolation 테스트 통과 |
| 기타 | R3/R5/R7/R8/R9/R12/R13 | Low | (자세한 표는 stage-83 spec doc) |

### Rollback plan
- **dashboard 문제** → Vercel 이전 deployment promote. central-plane 변경 불필요.
- **worker 문제** → Cloudflare Workers 이전 version으로 rollback. 새 카드는 localized 에러로 fail closed.
- **마이그레이션 문제** → 코드만 rollback. SQLite DROP COLUMN 미지원 → 마이그레이션 자체 rollback 권장 안 함. Additive nullable이라 양쪽 코드 모두 안전.
- **endpoint 단일 문제** → 컴포넌트별 fail closed 패턴 → 일부 카드만 빈 상태, 나머지 정상.

### Go/no-go checklist (operator가 release 시 확인)
- [ ] Vercel 토큰 revoke + rotate (R11)
- [ ] central-plane 1131/1131, dashboard 188/188, typecheck 54/54, lint clean
- [ ] `0044`, `0045` 리뷰 완료, **순서대로** remote D1 적용 완료
- [ ] 배포 순서 확정 (D1 → worker → dashboard)
- [ ] smoke-test userKey + projectId 준비
- [ ] rollback plan 리뷰
- [ ] **Bae가 명시적으로** "Release checkpoint 진행해" / "이제 push/deploy 해" / "Stage 77~82 묶어서 배포해" 중 하나를 말함

### 운영 상태
- **Push: skipped by operating rule.**
- **Production deploy: skipped by operating rule.**
- **Remote migration apply: skipped by operating rule.**
- **Live verification: skipped by operating rule.**

### 권고
**READY for release checkpoint, pending operator confirmation.** 차단 항목은 (1) Bae 승인 + (2) Vercel 토큰 회전(R11)뿐. 나머지는 초 단위로 확인 가능. 승인되면 forward pass 한 번으로 끝: `pnpm migrate:apply` → `pnpm ship` (central-plane) → `git push origin main` (Vercel auto-deploy) → smoke test.

### Stage 84 결정 필요한 점
1. **Release checkpoint 실행**(approval-gated): 위 go/no-go 모두 OK + Bae approval.
2. (release 후) **Federated impact signal export** — 결정 #21 wire format. cross-project 데이터 누적 시작.
3. **Timeline pagination** — 50 cap 초과 프로젝트 등장 시.
4. **Item-count weighted averages** feature flag — 실 데이터 비교 후 default 결정.
5. **mcp-workspace turbo build-before-test 순서 fix** — 무관하지만 follow-up 필요.
6. (계속 이월) Vercel 토큰 revoke (release 전 필수).

---

## Stage 84 — Brand Rename Readiness Audit

**제품 이름은 안 바꿈.** 향후 rebrand가 쉬워지도록 public-facing "Conclave" 문자열을 전수 조사·분류하고, 저위험 surface 2개만 `BRAND` 상수 뒤로 분리. 나머지는 audit only.

### 오늘 local commit (Stage 84)
| 커밋 | 내용 |
|---|---|
| (local, no push) | Stage 84 — brand rename readiness audit (BRAND 상수 도입(dashboard + central) + 저위험 surface 2개 swap + drift 가드 6 tests + 분류 audit doc). **Push: skipped by operating rule.** |

### Brand surface audit
- 전체 3,417 occurrences / 536 files
- **A**(public-facing, rebrand-target): dashboard `<title>`/metadata, AppSidebar wordmark(이미 i18n), i18n body 문장, central 생성 텍스트(PR 코멘트/Telegram/builder pack/PR fix brief/action pack heading)
- **B**(internal, **freeze**): npm 패키지 `@conclave-ai/*`, env `CONCLAVE_*`, localStorage `conclave_*`/`conclave:*`, DB 테이블/마이그레이션, 라우트 경로, CSS class `brand-*`, MCP package id, GitHub App 이름, `.conclave/` 학습 저장소, public domain
- **C**(historical, freeze): stage docs, HANDOFF 히스토리, release notes, marketing 카피

### 적용한 변경 (저위험 surface 2개만)
1. `apps/dashboard/src/app/layout.tsx` metadata title/description → `BRAND.metadataTitle`/`metadataDescription`
2. `apps/central-plane/src/workspace/evolution-action-pack.ts` `DEFAULT_EVOLUTION_STRINGS.packHeading` → `BRAND.actionPackHeading`

### 신규 파일
- `apps/dashboard/src/lib/brand.mjs`(+`.d.mts`) — `BRAND = {productName, productShortName, tagline, metadataTitle, metadataDescription}`
- `apps/central-plane/src/workspace/brand.ts` — `BRAND = {productName, productShortName, actionPackHeading}` as const
- 각 앱 `test/brand.test.mjs` — 값 pin + i18n drift catcher + Stage 77 contract preservation
- `stage-84-brand-rename-readiness-audit.md`

### Stage 84에서 의도적으로 안 바꾼 것 (Category A-deferred)
- 본문 문장형 i18n 키 ("Conclave is reading…" 등) — `${BRAND.productName}` 템플릿화 시 i18n parity 위험
- KO 생성 텍스트(PR 코멘트, Telegram, builder pack, PR fix brief) — 단일-locale 문장, rename PR 시 일괄 처리 권장
- AppSidebar wordmark — 이미 `t.brand.wordmark`로 config-driven
- 마케팅 사이트 — 별도 repo / 별도 rename pass
- 내부 namespace 전체 — Stage 84 계약상 freeze

### Saved artifact 호환성
- Stage 77 저장 packs는 **rewrite 안 함**. `pack_json`의 `sections[]`는 save-time 박제됨.
- `DEFAULT_EVOLUTION_STRINGS.packHeading` 값은 동일("Conclave Evolution Action Pack") → 기존 packs와 신규 packs 모두 같은 heading.
- 추후 BRAND 값이 바뀌면 신규 pack `text` 재생성만 새 brand 반영. 옛 `pack_json` 박제 그대로(immutable artifact policy, Stage 77 정책).

### Tests / build (local only)
- 신규 dashboard **3** + 신규 central **3** = **6** drift-guard tests
- **central-plane 1134/1134** (Stage 83 1131 → +3)
- **dashboard 191/191** (Stage 83 188 → +3)
- **typecheck 54/54** · **lint clean** · i18n parity 10/10
- Next build은 sandbox에서 Google Fonts(`SELF_SIGNED_CERT_IN_CHAIN`) 실패 — Stage 77~83과 동일 환경 이슈, Stage 84 코드 무관

### 운영 상태
- **Push: skipped by operating rule.**
- **Production deploy: skipped by operating rule.**
- **Remote migration apply: skipped by operating rule.** (Stage 84는 마이그레이션 추가 없음)
- **Live verification: skipped by operating rule.**

### Remaining rename risks(요약)
- **B1**: BRAND 바뀔 때 i18n sibling 까먹기 → **drift 가드 테스트로 catch**
- **B2**: 저장된 Stage 77 packs는 옛 brand 표기 — **의도된 immutable policy**, doc 명시
- **B3**: ~12개 KO/EN 본문 문장 hardcoded "Conclave" — **rename PR에서 일괄 처리**
- **B5**: 마케팅 사이트 — out of scope
- **B6/B9/B10**: 내부 namespace 전체 freeze 권장 — 외부 의존성 깨짐 방지
- **B7/B8**: Public URL DNS / GitHub App 표시명 — rename 시점에 DNS + redirect 전략 + App rebind 전략 결정

### 권고: Conclave 이름으로 release 먼저, 이후 rename
**Stage 77~82 release를 Conclave 이름으로 먼저 진행.** 이유:
1. 제품 surface가 성숙·검증됨 — brand 결정 대기로 보류하면 가치 손실
2. Stage 77 packs가 "Conclave 시대" artifact로 dated → 추후 rename이 자연스러운 시간 boundary
3. Stage 84 audit + BRAND 상수로 추후 rename이 site-wide search/replace에서 → 2 상수 변경 + drift tests + 본문 ~12개 PR 한 번으로 축소
4. Category B freeze는 release 타이밍 무관 — 이번 release에 굳히는 게 오히려 안전
5. Stage 83 release prep doc + 브랜치 그대로 ship 가능

Stage 83 go/no-go에 한 줄 추가: "Brand rename audit landed (Stage 84); release ships under the existing Conclave brand."

### Stage 85 결정 필요한 점
1. **Release checkpoint 실행**(여전히 Bae approval-gated) — Stage 83 체크리스트 그대로 + Stage 84 audit landed 한 줄 추가.
2. **(release 후) 새 brand 결정 + rename PR** — `BRAND.*` 2 파일 + 본문 문장 ~12개 + 마케팅 사이트.
3. **GitHub App / 도메인 / DNS 전략** — rename 시점에 결정.
4. **저장 pack rewrite 여부** — 권고: no(immutable artifact). rename PR에서 명시.
5. (계속 이월) Vercel 토큰 revoke (release 전 필수).
