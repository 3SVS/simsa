# HANDOFF — 2026-07-12

> 다음 세션 인수 문서. 직전 핸드오프 `HANDOFF-2026-07-09.md`, 진실의 소스 `docs/simsa-prd.md`.
> 세션 상세는 Claude 메모리 `session-2026-07-09-prd-nondev-copy-ux.md`.

## 1. 현재 상태 — 전부 프로덕션 LIVE (main = e15a8db)

| PR | 내용 | 상태 |
|---|---|---|
| #322+#323 | 프로젝트 삭제 (ack 모달·D1→R2 순서·토스트 하드닝) | LIVE |
| #325 | **repo 링크 도돌이표 근본수정** — 링크가 애초 미저장(소유권 404). 예시트랩(proj_mjx1) 3계층 차단 + `lib/project-mirror.ts` self-heal + 정직 카피 | LIVE |
| #326 | 깃헙 팝업 연결(auth→즉시 repo 선택, `/github/connected` postMessage) + 코드갈래 저장소-먼저 워크 + bridge 억제 | LIVE |
| #327 | 아이디어 갈래 워크 idea→spec→items→**export에서 종료**(코드 없는 유저 repo 행진 제거) + 사이드바 새 프로젝트 즉시 표시 | LIVE |
| #328 | 사이드바 "코드 변경" 탭 숨김(코드 갈래 or repo 실연결 후만) — 아이디어 유저 repo 노출 3면 전부 닫힘 | LIVE |

## 2. 7/10 인시던트 원인 확정 (D1 읽기, 배님 승인 하)

- **proj_mjx1 유령 행 없음** → 전역 선점 미발생, DELETE 불필요. 새 가드(#325)가 재선점 원천 차단.
- **실원인 = 멀티 깃헙 계정 미스매치**: 배님 userKey 3개(브라우저별: uk_mqi7…/uk_mr6l…/uk_mqru…현재) + 깃헙 계정 2개(**seunghunbae-3svs** ↔ **seunghunbae-collab**). 스모크 중 연결=3svs인데 대상 repo가 그 계정으로 링크 불가 → 에러 저salience로 묻힘 → 도돌이표. 링크 행은 역대 3건뿐(마지막 7/4).
- wrangler는 재로그인으로 D1 스코프 확보됨(이 머신). 광역 유저 조회는 3자 PII로 분류기 차단 — 본인 키 한정 조회만.

## 3. 다음 배치 후보 (아이디어 갈래 워크스루 잔여 4건)

1. **"완성도 확인 결과 요약" 갑툭튀** — 아이디어 확인 직후 요청 안 한 요약 표시. 화면 코드 확인 후 판단(추정 수정 금지). 배님 스크린샷 오면 pinpoint.
2. **export(빌더팩) 페이지 출구 승격** — "만들고 돌아왔어요 → 연결(/p/:id/connect)" CTA를 1차 시민으로. 지금은 조건부 링크라 막다른 길로 경험됨.
3. **settings 페이지 repo 섹션 강등** — 사이드바 유도는 껐지만 직접 진입 시 여전히 최상단이 repo 연결. entryPath별 재배치(알림·서비스 위로).
4. **#295 처분** — 사이드바 수정이 #327과 중복. rebase 후 net-new만 추리거나 close(사유 코멘트).

## 4. 설계 결정 대기 (배님)

- **멀티 깃헙 계정**: OAuth 계정 선택기 부재(전환=disconnect+github.com 로그아웃) + 같은 계정 두 번째 브라우저 연결 시 첫 연결 뺏김(`upsertGitHubConnection` user_key 재할당 — 정당 재클레임과 구분 불가). **감사의 "GitHub App 세분권한 + 권한 카드" 트랙과 묶어서** 볼 것. 소프트 개선: repo 선택 패널에 "현재 ○○ 계정 저장소 표시 중" 안내.
- ③ 비활성 버튼 힌트 폴리시(선택).

## 5. 이월 (2026-07-09 핸드오프에서 계속)

- **R2 orphan 청소 잡** — 삭제 D1→R2 순서의 트레이드오프. cron 스윕 or lifecycle rule. (§HANDOFF-2026-07-09 5-B)
- **Langfuse #310 리전 줄** 기입(EU) · **latency 첫 쿼리**(#303 전후 output_tokens — 5k tok spec=UX 상한 관점)
- **#298** 날조 mock 제거 rebase-merge · **로그인 벽 P1** 재등재

## 6. 라이브 QA 상태

7/9 배치 5종 스모크에서 T4 계열 FAIL → 근본수정 후 재배포됨. **배님 재검증 대기**: ⓐ아이디어 갈래(사이드바 즉시 표시·다음이 빌더팩에서 멈춤·GitHub 탭 없음) ⓑ내 프로젝트 repo 연결 유지 ⓒ팝업 연결 플로우 ⓓ삭제 모달/실삭제. FAIL 시 화면+페이지만 기록.
