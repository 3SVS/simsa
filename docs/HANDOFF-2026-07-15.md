# HANDOFF — 2026-07-15

> 루프 복구 세션. 9일 공백(마지막 커밋 7/10, 마지막 로컬 세션 7/6) 후 "다 고쳐서 이어보자" 지시로
> 운영 루프를 재가동. 진실의 소스: `docs/simsa-prd.md`.

## 한 줄 요약

카나리 5일 연속 빨간불(deploy stale)의 근본 원인 2개를 제거 — ① 미배포분 배포(central-plane + dashboard 프로덕션), ② 알림 채널 부재 수리(PR #330: 실패 시 GitHub 이슈 자동 오픈/복구 시 자동 클로즈, secret-free). **카나리 green 복구 + 검증 런 success.** 로컬 표류 작업(9일 묵은 미커밋 1,166줄)은 백업 브랜치로 보존 — 원격 리디자인(#244~#254)으로 대체 확인.

## 이 세션에서 한 일

| 항목 | 상태 | 증거 |
|---|---|---|
| 로컬 WIP 보존 | ✅ | `wip/simsa-landing-restyle-2026-07-15` 브랜치 push. 내용은 원격 #244~#254 리디자인으로 **대체됨** — 병합 불요, 참고용만 |
| central-plane 배포 | ✅ | run 29422327936 success → `/healthz` deployedSha=`e15a8db`(=main) |
| dashboard Vercel 배포 | ✅ | dpl_C8ZckHg9SmFkWtEq9EbGJT7CpEH3 ● Ready → app.trysimsa.com 200. #322/#323(삭제), #325/#326(GitHub 연결), #327/#328(idea 흐름) 라이브 |
| 카나리 green 복구 | ✅ | run 29422647296 success (5일 만의 첫 green) |
| 카나리 이슈 알림 (PR #330) | ✅ 머지+검증 | 실패 시 `canary` 라벨 이슈 1개 오픈(스팸 방지), green 시 자동 클로즈. GITHUB_TOKEN만 사용. 검증 런 29425390945 success |
| api.trysimsa.com 유령 DNS | ✅ 종결(조치 불요) | trysimsa.com은 Vercel NS + `*` 와일드카드 ALIAS 기본 레코드 → 미정의 서브도메인의 표준 동작. 아펙스가 이 팀 소유·검증이라 탈취 위험 없음 |
| 라이브 스모크 (익명 플로우) | ✅ **8/8 PASS** | `tools/simsa-completion-loop-spike/anonymous-smoke.mjs` (repo에 보존). 실 LLM 왕복 · #327 · #328 · 삭제 QA ⓐⓑ · 예시카드 삭제버튼 없음 · EN 누수 0(육안) |
| 스모크가 찾은 신규 버그 수리 (PR #331) | ✅ 머지+배포+재검증 | 같은 라우트에서 삭제 시 사이드바에 유령 항목 잔존(#327과 같은 클래스). store 단일 지점에서 `simsa:projects-changed` 이벤트 발화로 근본 수정 — 재실행 8/8 |
| deploy-stale 헛알람 구조 수정 (PR #333) | ✅ 머지+검증 런 success | 5일 빨간불의 구조 원인 — stale 체크가 경로 무관이라 dashboard/docs 머지도 Worker 알람을 울림. Worker 경로(apps/central-plane·packages·pnpm-lock) 변경 시에만 발화하도록 수정. **따라서 "main이 Worker보다 N커밋 앞섬 + 카나리 green"은 이제 정상 상태일 수 있음** (비-Worker 커밋) |

## ★ Bae 액션 (남은 것)

1. **카나리 Telegram DM 업그레이드** (선택, 1분):
   `gh secret set TELEGRAM_BOT_TOKEN --repo 3SVS/simsa` + `gh secret set CANARY_TELEGRAM_CHAT_ID --repo 3SVS/simsa`
   (이슈 알림은 이미 라이브 — Telegram은 더 시끄러운 채널 추가일 뿐)
2. **자동수리 LLM 라운드트립 실검증** — 7/2부터 이월. OAuth 계정 필요.
3. **T4 스모크** (repo 연결 프로젝트의 code-check 링크 유지) — GitHub 연결 계정 필요, 익명으로 불가.
4. **T1 리포트 EN 라이브 렌더** — 인증 인스펙션 실행 필요 (익명 idea 프로젝트는 배포 URL이 없어 visual check 불가). UI 레벨 EN 전환·누수 0은 확인 완료.

## 이월 항목 (7/9 핸드오프에서 계속)

- R2 orphan 청소 잡 (프로젝트 삭제의 D1→R2 순서상 비용 누수) — 미착수
- #295 rebase 후 net-new 판단, #298 rebase-merge 권고 — 열림
- Langfuse 리전 줄 기입 (`docs/decision-status.md` #310)
- latency=output-length 쿼리 (Langfuse로 #303 전후 output_tokens 비교)

## 교훈 (이 세션의 발견)

- **카나리가 잡으라던 실패를 카나리 자신이 겪음**: 알림 채널이 이메일뿐이면 5일 침묵이 가능하다. 모니터링은 "감지"가 아니라 "응답까지"가 루프다.
- **워크스페이스 이원화 주의**: 로컬(이 머신)과 원격(클라우드 에이전트) 작업이 분기하면 로컬 미커밋 작업은 조용히 죽는다. 세션 시작 시 `git fetch` + origin/main 대조를 습관화.
- 세션은 반드시 `C:\Users\seung\.conclave\conclave-ai`에서 시작 (`C:\Users\seung\conclave-ai`는 빈 스캐폴드).
