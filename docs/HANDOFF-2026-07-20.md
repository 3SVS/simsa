# HANDOFF — 2026-07-20

이전: `docs/HANDOFF-2026-07-18.md` (추가 1~14 — G1~G14 백로그·G8·P0 핫픽스·자동수리 실증·E-corpus-1 파킹).
오늘 흐름: **E-corpus-1 근본 해결(6차 만에 종결)** + auto_fix 성숙 코드분 완결.

## 오늘 커밋 (#421~#424, 전부 당일 머지·배포)

- **#421** `feat(inspection): 러너 위상 로그 + RUNNER_REV 마커` — 관측 장비.
- **#422** `feat(repair): auto_fix 성숙` — ①**private repo 실지원**: repair 라우트가
  `resolveRepoAccessToken` 사용(링크 repo가 private면 GitHub App 설치 토큰으로
  clone/push, public은 기존 fast path 그대로) ②clone 접근거부를 `repo_access_denied:`
  안정 프리픽스로 분류(`classifyCloneError`) → 대시보드 비개발자 안내 카드(공개 전환/
  App 설치, KO/EN) ③모호판정 진단 사다리(buildRepairReview — 발견성/무변화 코드가설
  2~3개 확인 전 포기 금지, 추측수정 금지 레일 유지). central 53/53·dashboard 21/21.
- **#423** `feat(inspection): 위상 트레이스 in-band 회수` — ★**tail에는 컨테이너
  stdout이 안 나온다(실측)** → 위상 로그를 실패 콜백 error에 `||trace:`로 탑재,
  report_json으로 판독. RUNNER_REV로 롤아웃 반영도 in-band 검증.
- **#424** `fix(inspection): E-corpus-1 근본수정 ec1-fix1` — 트레이스가 hang 지점 확정:
  **검수는 +50s 정상 완료, 진범은 finally의 `context.close()`**(recordVideo 파이널라이즈,
  0.25 vCPU)이고 이전 코드는 close 전에 killTimer부터 해제해 구조 수단까지 없앴다
  (#412~#418 4차가 전부 빗나간 이유 = "검수 중 hang" 가정, 실제는 "종료 중 hang").
  수정 = close/video 대기 전부 `raceOrNull` 타임아웃 레이스(15s/10s/5s), killTimer
  해제는 close 뒤로. 로컬 대조실험(close 0.1s resolve)으로 컨테이너 특이성도 증명.

## 실증 결과 (프로덕션)

- **F7 /heavy-site: 6연속 247s 빈손 실패 → done + UAR 정상 리포트.** 빈손 4분
  타임아웃 클래스 소멸.
- **F1: 7/18 기준선과 완전 동일**(works=null·UAR·done) — 무회귀. 오판 0 유지.
- 전말: `docs/simsa-corpus-eval-2026-07-19.md` (근본 원인·종결 절 추가).

## 상태

- central-plane v68 라이브(ec1-fix1 + #422 sandbox 컨테이너 포함), dashboard CLI 배포
  라이브(접근거부 안내 카드). deploy-central-plane은 `-f confirm=deploy`로 디스패치.
- ★dashboard Vercel CLI 배포에 **`--archive=tgz` 필수**가 됨(파일수 49,514>15,000 제한,
  exit 0인데 실패하는 함정 — `vercel ls`로 확인). 메모리 기록됨.

## 다음 픽업

1. **auto_fix 성숙분 라이브 실증**: simsa-autofix-test를 private로 되돌리고 GitHub App
   설치 → [고치기] → auto_fix 라운드트립(#422의 App 토큰 경로) + 접근거부 카드 실증
   (App 미설치 상태에서 한 번). 모호판정 사다리는 apply-walmart 재시도로.
2. **E-corpus-2**: 위치권한 팝업 처리(권한 프롬프트 자동 거부 후 플로우 지속).
3. G6-2 토스 연동 트레인(배 님 가입 완료 트리거 대기) · G14-b(서버 EN화) ·
   D14 월요 크론 리뷰(`GET /admin/deploy-trends`) · 실유저 트래픽.

## 주의

- 컨테이너 진단은 앞으로도 in-band(||trace:)로 — tail 계획 금지. 롤아웃 확인은
  RUNNER_REV 또는 `wrangler containers info/instances`(인스턴스 VERSION 열).
- ec1-fix1의 raceOrNull 타임아웃(15s/10s/5s)은 파라미터 — 원칙(리포트를 정리
  작업의 볼모로 잡지 않음)은 인바리언트 테스트로 잠금.
