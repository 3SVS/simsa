# HANDOFF — 2026-09-27 (비전 재정렬 + BM·이코노미 분석, 코드 PR 1)

이전: `docs/HANDOFF-2026-09-25.md`(E2E·B3·B4·B5(a)). 이 문서는 2026-09-26~27 전략 분석 세션과 열린 PR 상태를 담는다.

## 1. 오늘 열린 PR (전부 미머지 — 승인 대기)

| PR | 내용 | 종류 | 상태 |
|---|---|---|---|
| #548 | B5(a) 빌드 잡 D1·라우트·디스패치(0068) | 코드 | CI green, 머지 대기. 재정렬안은 B5를 W1 뒤로 두되 **보류하지 않음** |
| #549 | `docs/simsa-vision-realignment-2026-09-27.md` — 세 문 동급·공통 엔진·데이터 봉투 W1·결정 재론 문안 | 문서 | 머지 대기 + §5 Bae 결정 3 |
| #550 | `docs/simsa-bm-economics-2026-09-27.md` — 원가표·수익 엔진 9·P&L·반박 정정·데이터 사다리 | 문서 | 머지 대기 + §9 Bae 결정 3 |
| #551 | agent-worker OpenAI 폴백 다중 턴 블록 변환(빌드 루프가 OpenAI만으로 완주, 66/66) | 코드 | CI 대기. **미커밋으로 남아 있던 B5 작업**을 발견해 별도 PR로 올림. #548과 파일 겹침 없음 |

주의: `tools/simsa-completion-loop-spike/journey-audit-result.json`은 E2E 실행 산출물로 수정 상태로 남아 있음(커밋 안 함). 로컬 브랜치 3개(`docs/vision-realignment-2026-09-27`, `docs/bm-economics-2026-09-27`, `feat/train-b5-openai-tool-blocks`) 모두 main 기준.

## 2. 비전 재정렬 요지 (#549)

- 세 문(아이디어 / 안 됨 / 생각과 다름)은 동급, 엔진 하나 공유 = 의도 확보 + 의도 대조 판정 + 고치기/만들기 루프 + 출처 구분 영수증 + 데이터 봉투.
- 확인된 끊김 13건(전부 파일 근거, 반박 검증 통과): 의도 확정 카드가 D1·판정에 안 닿음 · 재검수가 intent 유실 · verify-sweep에 AC 미전달 · 수리는 GitHub+OAuth 필수(주소만 앱은 불가) · web_builder 고침 지시 미배선 · 검수 행에 봉투 컬럼 0(region·built_with·finding_code·user_verdict) · 검수/수리 상한·킬스위치 0 · "생각과 다름" 참양성 0.
- 결정 재론 문안: D-19/17/8/21/20/13/2/1/7 amend, D-4/15 keep, Train C reopen(C2a 코드 연결 / C2b 주소만), B10 파일럿 = Bae 아이디어 20개 중 6건.
- 로드맵: W1 공통 엔진·정직성·데이터(~9일, 0069 additive) → W2~3 문별 인도(B5b ∥ C2a ∥ A7+프롬프트 팩) → W3~4 파일럿.
- `docs/EVIDENCE-RULE.md`가 repo에 없음(메모리·D-4가 인용) — 위치 확인 필요.

## 3. BM·이코노미 요지 (#550)

- 원가 비대칭: 검수 $0.004(LLM 0회)·지시서 $0.22·심사 $0.05·호스팅 $0.02/앱·월 → 활성 1인당 월 $0.19. 리스크는 빌드(분포 $2.7~41, 실측 0)·수리($0.5, 상한 없음).
- 선택 BM: works-or-free 골격(빌드 $99 성공 시만·수리 쿼터 초과 $29) + 무료 면 확대 + Care $19/앱·월 + 어필리에이트 + 임베드 API(데이터 채널) + 비순위 평가 리포트 + 라이선스. 기관 도장 제외.
- **반박 검증 5/5 정정**: organic 유입·복귀(M1 60→20, 성장 12→0%, 복귀 15→5%), 카드율 4→1%, 빌드 원가 점추정 불가, MoR은 Paddle만(auth hold 없음), 동의-쿼터 교환은 GDPR 위험. 정정 후 12개월 D3 매출 ≈ $0·D2 ≈ $2K, 영업결과 98%는 창업자 인건비 → **유입 채널이 지배 변수**.
- 레포 결함 3건: pricing.ts 단가(haiku 4배 과소·opus 3배 과대)·build-loop 요청 모델 과금(D-7 무력)·Langfuse 모델 라벨. W1에 2b로 추가 제안.
- 손익분기 월 활성 1,500~3,000(인건비 포함)[추정].

## 4. Bae 결정 대기 (6)

재정렬: ①D-21 두 층(비식별 운영 메타 방침 고지만으로 기록) ②`design lock approved`(재정렬) + `train C start approved` + B10 정의 교체 ③파일럿 재료 6건 준비(Lovable·v0·Bolt로 (b)(c)용 3건).
BM: ④과금 예외 범위(빌드 $99·수리 $29만, 나머지 영구 무료) ⑤청구 주체·Paddle ⑥동의 정책(대칭 opt-in + 비EU 보너스).

## 5. Bae 액션 (이월)

- GitHub 조직 `simsa-hosted` 생성 + App 설치(Administration·Contents write) — B3 저장소 자동 생성의 선행.
- Google 로그인 브라우저 완주 1회(라이브 시작 200·redirect 일치까지만 확인됨).
- Paddle 판매자 가입 가능 여부 확인(법인 서류) — BM 결정 ⑤ 전제.

## 6. 다음 세션 픽업

1. 승인 문구 수신 → #548/#549/#550/#551 머지(원격 CI green + 체크리스트 확인 후).
2. 재정렬 승인 시: 설계 문서 레지스트리에 D-번호 amend 반영 → W1 항목 1(재검수 intent 유지)부터. 0069는 #548 머지 뒤.
3. BM 결함 3건은 작은 코드 PR로 즉시 가능(pricing 공식가·실응답 모델 과금·cached 토큰 전달) — D-7 [PILOT] 수치 영향이라 착수 전 한 줄 승인.
4. 워크플로 산출물 위치: `…/tasks/w0ojwnzc3.output.json`(BM), `…/subagents/workflows/wf_287f7c16-227/journal.jsonl`(재정렬). 결과 레코드에 label이 없어 `started`의 agentId로 join.
