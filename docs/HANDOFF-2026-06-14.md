# HANDOFF — 2026-06-14

## 오늘 커밋

| 커밋 | 내용 |
|------|------|
| `4d83710` | Stage 39 — GitHub Actions Node 24 마이그레이션 (7 workflows) + deploy/CI hardening 검증 |
| `f7d1b9f` | Stage 40 — 다시 확인할 항목 선택 UX (RerunPanel picker + selectedItemIds 정규화) |
| `58c99fe` | Stage 40 fix — rerun-selection을 .mjs+.d.mts로 (Node 20 CI 호환) |
| `90026f5` | Stage 41 — history list 빠른 "남은 문제 다시 확인" (rerunAction + QuickRerun 자동이동) |
| `bcd30c1` | Stage 42 — history list "남은 문제 Fix Pack" → detail ?action=fix-pack 자동 생성 |
| `a9ad424` | Stage 43 — run detail 공유 selectedItemIds (re-run/Fix Pack/comment 공통 선택) |
| `15f78ad` | Stage 44 — selectedItemIds localStorage 영속화 (같은 run 복원, stored []≠null) |
| `9960472` | Stage 45 — ?fromRunId 비교 자동 표시 (AutoComparisonPanel, query>lineage) |
| `9aa6544` | Stage 46 — 비교 결과 PR comment shortcut (AutoComparisonPanel→CommentPanel, Policy A) |
| `0e217b3` | Stage 47 — progress-emit "parallel" 테스트 flaky 안정화 (timer→controlled deferred) |
| `22445dc` | Stage 48 — AutoComparisonPanel 상태 전환(이전→현재) 표시 |
| `45beaa0` | Stage 49 — PR comment "다시 확인 결과 비교" 섹션에 상태 전환 표시 (backend body) |
| `6527392` | Stage 50 — Release Checkpoint (Stage 33~49 안정화, 문서/QA/release-note/CHANGELOG) |
| `036518f` | Stage 51 — Live E2E QA (backend 계약 검증 + Bae용 수동 스크립트) |
| `080f73d` | Stage 52 — Beta Readiness 패키지 (Bae 수동 QA + 시나리오 + 피드백 + triage, 문서 5개) |
| `795c946` | Stage 53 — Dashboard Vercel 배포 셋업 가이드 (CORS/OAuth는 dashboard.conclave-ai.dev에 하드와이어) |
| `e521297` | Stage 53 실배포 — dashboard Vercel 배포(conclave-dashboard.vercel.app) + exact origin CORS/OAuth allowlist |
| `ae76c67` | Stage 53 라이브 QA fix — "저장하고 프로젝트 시작하기" 404 해결 (projects/[id] 개요 + idea를 server→client, getLocalProject ?? getProject). Vercel 재배포 + 라이브 200 검증 |
| `51aa54c` | Stage 53 라이브 QA fix — GitHub 연결 OAuth가 dashboard.conclave-ai.dev(NXDOMAIN)로 복귀하던 버그. settings 페이지 returnTo를 상대(pathname)→절대(href)로. Vercel 재배포 + 라이브 번들 location.href 검증 |
| `534cc05` | docs — Stage 53 fix 2건 문서화 (HANDOFF + stage-53) |
| (Stage 54) | Live E2E QA — 자동 검증 surface 전수 GREEN(라우트/CORS/OAuth/safety/로직), 신규 P0/P1 0건, 코드 무변경. 실데이터 D~L는 연결 userKey 입력 대기. doc: stage-54-live-e2e-qa.md |
| `08b0edc` | Stage 55 — 실데이터 E2E(proj_7w5zhyaw, 3SVS/My-first-product PR#1): review→history→FixPack→re-run→비교→**PR comment 실게시**(issuecomment-4733114388) 전부 검증. **P1 발견·수정**: 부분 productSpec → review 크래시 → normalizeProductSpec/Items 경계 정규화(6 tests, 942/942), deploy-central-plane 자동배포+라이브 재검증. doc: stage-55-live-data-e2e-qa.md |
| `a2f4902` | Stage 56-1 — repos listing 개선(affiliation=owner,collaborator,organization_member + visibility=all + Link pagination + permissions). ★라이브 결과 여전히 org repo 누락(3SVS org가 OAuth 앱 제한) → 직접입력 필요 |
| `b34869f` | Stage 56-2 — 직접 owner/repo 입력 fallback: GET /workspace/github/repos/lookup(fetchGitHubRepoByFullName) + dashboard picker 직접입력 input. backend 라이브 검증 OK(3SVS/My-first-product ok=True, CORS, not_found/invalid). ★dashboard Vercel 재배포 대기. 8 tests, 950/950. doc: stage-56-org-repo-picker.md |
| (Stage 57) | dashboard 재배포(ff975f9, settings chunk page-1ac8ebc) + 라이브 검증 — 새 번들에 직접입력 UI 반영(목록에 없는/repos/lookup/placeholder 확인) + lookup 흐름 라이브 OK(ok=True, CORS=vercel, invalid/not_found 매핑). 코드 무변경. ★육안 클릭만 Bae 1회. doc: stage-57-dashboard-direct-repo-input-live-verification.md |
| (Stage 58) | 베타 온보딩 패키지(문서 5개): onboarding/test-guide(11과제)/feedback-form/known-limitations/readiness-checklist(1~12 Ready, 13~14 limitation, 15~17 Bae). 복사용 안내문 포함. 코드 무변경. healthz 200·safety OFF 재확인 |
| `af41bae` | Stage 61 — MCP 서버 신설 packages/mcp-workspace(stdio, 9 tools, central-plane 래핑). 토큰 미노출·userKey env 주입·ownership authz·post OFF+confirm·stderr audit·dry-run. central-plane GET /workspace/projects 추가. 7 tests + 라이브 스모크 OK |
| `b747b3f` | (중간) central-plane idea-to-spec **영어 기본 생성**(buildPrompt locale 분기 + generic fallback EN). 새 프로젝트 생성물 영문. 950 pass, 자동배포 |
| (Stage 62) | MCP 패키징/문서/과금: README 운영판(positioning·env표·config 3종·billing·safety·troubleshooting), TOOL_META export+credit/confirm 문구, env 별칭(API_BASE_URL/ENABLE_PR_COMMENT_POST/AUDIT_LOG), scripts/smoke.mjs, 14 tests, npm pack dry-run(17 files). npm publish 안 함. doc stage-62 |
| `c939386` | Stage 63 pass 1 — 깊은 화면 i18n: spec(Product brief)·items(Acceptance items)·fixes(Fix instructions) 본문 EN/KO + 브랜드(.card/.btn). dict spec/items/priority/fixesScreen. 77/77, parity 10/10. doc stage-63 |
| `ddf0e38` | Stage 63 pass 2a — checks(Draft review+PR review 2섹션)·settings Telegram(정책 Problems only/Always/Disabled+이력) 전면 i18n. dict checks/runStatus/telegram. 77/77, lint green |
| `3f1d337` | Stage 63 pass 2b — review history 목록 전면 i18n(헤더/상태/run 카드/quick actions/PR별 요약, locale 날짜, statusLabel). dict history 확장. 77/77, lint green |
| `ec99ec5` | Stage 63 pass 2c — github 페이지 **메인 흐름** i18n(연결/PR 목록/선택/연결/review 상태). dict github 확장. ★남은 pass 2d: github 깊은 패널(Result/Comparison/Comment/Fix/Credit 배너)+run detail(149). 77/77, lint green |
| `f1b8ecf` | Stage 59 — dashboard i18n(dictionary-first) + UI polish. EN 기본/KO 토글(I18nProvider+useI18n+LanguageToggle), 글로벌 AppHeader, ProjectShell nav i18n, 상태 라벨 제품친화 영어(Passed/Issue found/Not verified/Needs decision, StatusBadge+StatusText), projects 홈/metadata 영어화. 9 i18n tests, 76/76. ★남은 화면별 한글 copy는 점진(stage-59 doc §7). eslintrc에 .d.mts ignore |
| `e64ff21` | Stage 60 — ★Linear-grade neutral 재디자인(Bae 취향 확정: 미니멀/딥그린/neutral/보라금지) + GitHub flow i18n 심화. 토큰 전역 리매핑(gray→zinc, indigo→딥그린, brand alias), violet→slate 전면, globals Linear 프리미티브(.card/.btn/.input/.callout/.page-title), AppHeader/ProjectShell 컴팩트(mock 배지 제거), settings(GitHub 연결) 전면 i18n+restyle, github 헤더 영어화. dict github/review/history 추가. 10 i18n tests, 77/77, typecheck 53, lint green. ★Vercel 재배포 대기. 남은 copy(github 패널/Telegram/idea·spec·items)=Stage 61 |

(이전: HANDOFF-2026-06-13.md 참조 — Stage 30~38.1)

## Stage 40 요약 (다시 확인할 항목 선택)

- 기록 상세(`/projects/:id/github/history/:runId`)의 `RerunPanel`을 **항목 체크박스 picker**로 확장.
- preset 4개: 추천 선택 / 전체 선택 / 통과 제외 / 모두 해제. 기본 = 통과하지 않은 항목.
- 0개면 버튼 disabled + 안내. 버튼 "선택한 항목 다시 확인하기 (N개)".
- re-run은 Stage 37 endpoint 재사용, body selectedItemIds로 전송 (우선순위 body > source > linkedPR).
- ComparisonPanel에 "선택한 N개 항목을 다시 확인했습니다." + 기존 4개 비교 그룹.
- 서버: `normalizeSelectedItemIds` (dedup/trim/빈문자열 제거/cap 500), endpoint 구조 불변.
- history list 직접 re-run 버튼은 추가 안 함 (상세에서만). billing/autofix/patch/commit 없음.

## ★ 주의 — dashboard 테스트는 .ts import 금지

ci.yml은 **Node 20 & 22 매트릭스**로 `pnpm test`를 돈다. Node 20은 `.ts` type-stripping 미지원 →
dashboard 순수 로직 테스트는 **plain ESM `.mjs` + `.d.mts` 선언 쌍**으로 작성해야 모든 Node에서 통과한다.
(dashboard `allowJs:false`라 `.js` 단독 import 불가 → `.d.mts` 필수.) 첫 시도(.ts)가 Node20 job을 깨서 58c99fe로 수정.

## Stage 41 요약 (history list 빠른 다시 확인)

- 기록 목록 각 run에 **"남은 문제 다시 확인 (N)"** 버튼 (Option A: `review-history` 응답에 `rerunAction` lightweight 필드, full results 미포함).
- 클릭 → failed/inconclusive/needs_decision만 자동 선택 → Stage 37 endpoint → **새 run detail로 자동 이동** `?fromRunId=`.
- 0개면 disabled, 결과 미저장이면 results_unavailable. 항목 편집은 "상세에서 항목 선택 →"로 유도 (list inline picker 없음).
- 서버 `recommendedRerunItemIds`는 dashboard helper와 미러링.

## Stage 42 요약 (남은 문제 Fix Pack)

- 기록 목록 각 run에 **"남은 문제 Fix Pack (N)"** 버튼 → detail `?action=fix-pack` 이동 (Option B). count 0이면 disabled.
- detail page가 `?action=fix-pack` 읽어 **FixPackPanel 자동 열림 + scrollIntoView + 자동 생성** (남은 문제만).
- FixPackPanel: 출처 안내("특정 확인 기록 기준 … 최신 PR 상태와 다를 수 있습니다") + "남은 문제 N개로 Fix Pack을 만들었어요".
- Stage 36 fix-brief endpoint 재사용 (reviewRunId + selectedItemIds → sourceReviewRun). 서버 변경 없음.
- Fix Pack = 수정 지시서 preview/copy만. patch/commit/branch 없음. shared selection은 Stage 43 후보.

## Stage 43 요약 (공유 selectedItemIds)

- run detail에 **page-level `selectedItemIds` 공유 상태** + **`ReviewItemSelectionPanel`**("이번에 다룰 항목", picker 분리).
- RerunPanel / FixPackPanel / CommentPanel 모두 공유 selectedItemIds 사용. 빈 선택 시 re-run·Fix Pack disabled (comment는 서버 fallback).
- `toggleItemSelection` 헬퍼. 서버/history list 변경 없음.

## Stage 44 요약 (선택 항목 영속화)

- run detail 선택을 **localStorage**(`conclave:review-selection:v1:<project>:<run>`)에 client-side 저장/복원. 새로고침·재진입 시 직전 선택 복원 → re-run/Fix Pack/comment에 사용.
- **stored [] ≠ null**: []는 "모두 해제"한 의도(복원), null은 없음(추천 fallback). `?action=fix-pack`에서 []이면 자동 생성 차단.
- helper `review-selection-storage.mjs`(+`.d.mts`), hydration write-skip으로 복원값 즉시 덮어쓰기 방지. 서버 저장 없음.

## Stage 45 요약 (fromRunId 비교 자동 표시)

- quick re-run 후 새 run detail이 **`?fromRunId`**(없으면 `rerunOfReviewRunId` lineage)로 source vs current 비교를 **AutoComparisonPanel**에 자동 표시 (summary 아래).
- helper `review-run-comparison.mjs` — central-plane와 동일 분류(STATUS_SCORE), `pickComparisonSourceRunId`(query>lineage). 비교는 client-side(기존 run-detail endpoint 2회 fetch), 서버 변경 없음.
- non-blocking: 실패는 작은 안내만, re-run/Fix Pack/comment 정상.

## Stage 46 요약 (비교 결과 PR comment shortcut)

- AutoComparisonPanel(Stage 45)에 **"이 비교 결과를 PR comment로 남기기"** 버튼 → CommentPanel로 스크롤 + 비교 포함 체크 + preview 자동 생성. 기존 backend `includeRerunComparison`(Stage 38) 재사용.
- **Policy A**: lineage(`rerunOfReviewRunId`) 있는 run만 활성 (backend가 rerun_of_review_run_id로 비교 계산). fromRunId-only는 화면 표시만.
- helper `canPostComparisonToComment` / `buildComparisonCommentInput`(includeComparison 미포함, lineage 없으면 강제 false). 신규 endpoint 없음.

## Stage 47 요약 (progress-emit flaky 안정화)

- `progress-emit.test.mjs` "emits in parallel" 테스트의 **타이머 의존 flaky 제거** (원인 B). 제품 코드 무변경.
- fixed setTimeout delay → **controlled deferred + waitForEventCount**. parallelism은 "모두 완료 전 시작"으로 deterministic 증명, 완료 순서는 명시 release로 제어. 기대값 약화 없음.
- 단일 20× + full-parallel ×3 무실패. 43ms→8ms.

## Stage 48 요약 (비교 상태 전환 표시)

- AutoComparisonPanel 각 항목에 **이전 상태 → 현재 상태** TransitionPill + 현재 근거/다음 조치 + 그룹 설명. 무엇이 좋아지고 나빠졌는지 한눈에.
- helper `compareReviewRunResults`가 transition-aware `ReviewRunComparisonItem` 반환(+`getReviewStatusLabel`/`buildStatusTransitionLabel`). 분류/summary는 Stage 45 동일.
- current-only="새 항목 → …", source-only=drop. backend/comment body/Stage 46 shortcut 미변경.

## Stage 49 요약 (PR comment 상태 전환 표시)

- rerun comparison PR comment body의 "다시 확인 결과 비교" 섹션이 항목별 **이전 상태 → 현재 상태** + 조치 필요 항목엔 "다음 조치" 표시.
- `pr-review-compare.ts` 비교 item에 additive `from?`/`nextAction?` 추가(API shape 비파괴), `pr-comment.ts buildRerunComparisonPart`가 transition 라벨로 렌더. 신규 endpoint/DB/request shape/dashboard 변경 없음.

## Stage 50 요약 (Release Checkpoint)

- Stage 33~49를 하나의 release checkpoint로 정리(문서 전용, 새 기능 없음).
- 문서: `stage-50-release-checkpoint.md`(scope/QA A~J/safety/known issues/rollback), `release-note-v0.14-pr-review-workflow.md`(제품), `CHANGELOG.md`(apps-only 항목, 버전 bump 없음).
- 확인: central-plane live smoke 전부 배선·healthz 200, safety flags OFF, dashboard next build 8/8, node --test 3456 green.

## 현재 상태

- main `6527392` (Stage 50 docs), 전체 green
- central-plane 45beaa0 배포본 라이브(healthz 200), 전체 node --test **3456/3456**, ci Node 20·22 success
- production actual debit/blocking OFF 확인됨
- ✅ release checkpoint 정리 완료 — 베타/실사용 테스트 진입 가능
- ci Node 20 & 22 둘 다 success, deploy-central-plane success, healthz 200
- 전체 node --test **3381/3381**, typecheck 53/53, build 29/29, next build OK
- dashboard에 첫 `test` 스크립트 생김 (turbo `pnpm test` 52 tasks)
- actual debit 여전히 OFF (wrangler.toml)

## 다음 세션 픽업 (Stage 54 전 결정)

- ✅ dashboard 라이브 = **https://conclave-dashboard.vercel.app** (Vercel project `conclave-dashboard`, root apps/dashboard). CORS+OAuth allowlist에 exact origin 반영·라이브 검증 완료.
1. **Bae: `https://conclave-dashboard.vercel.app`에서 수동 QA**(`stage-52-bae-manual-ui-qa-pack.md` A~L) 실행. 특히 B(GitHub 연결/OAuth) — OAuth App 콜백은 central-plane worker라 그대로일 것이나, 막히면 알려주기. P0 없으면 베타 진입.
2. **Bae: Vercel 토큰 revoke** (채팅 노출됨). 그리고 (선택) Vercel UI에서 conclave-dashboard 프로젝트 **Git 연결**(현재 미연결 → main push auto-deploy 안 됨; 안 하면 dashboard 변경마다 `vercel deploy --prod` 수동).
3. 기능 후보(backend scope, 택1): fromRunId-only 비교 comment Policy B / selectedItemIds 서버 저장.
4. 보류 유지: actual debit 활성화, payment provider, private repo, autofix, release.yml node 승격.

## 참고 문서
- `conclave-builder-pack/out/stage-40-selected-items-rerun-ux.md` — Stage 40 상세
- `conclave-builder-pack/out/stage-39-deployment-ci-hardening.md` — deploy checklist + troubleshooting
