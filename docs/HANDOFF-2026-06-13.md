# HANDOFF — 2026-06-13

## 오늘 커밋 목록 (최종)

| 커밋 | 내용 |
|------|------|
| `ec8ffc4` | Stage 30 — pending ledger visibility + manual mark-failed |
| `07d2135` | Stage 31 — limited actual debit rollout guard |
| `bd2d9de` | Stage 32 — internal actual debit test run guide |
| `6fe44c2` | Stage 33+34 — credit top-up requests + PR review history timeline |
| `b3ee1e3` | Stage 35 — PR review run detail endpoint + history detail page |
| `6045e45` | Stage 36 — run-specific Fix Pack & PR comment inline panels |
| `b8c50bb` | Stage 37 — run-specific re-run + comparison (compareSpecificReviewRuns) |
| `2e4b59b` | Stage 38 — rerun lineage DB + comparison in PR comment body |
| `732d921` | Stage 38.1 — 2개 실패 테스트 수정 (GrokAgent XAI_API_KEY 버그 + B.1 stale regex) |
| `bdf049e` | Stage 33 파일 commit bookkeeping (6fe44c2는 Stage 34만 포함했었음) |

## 현재 상태

- **3364/3364** 테스트 전부 통과, typecheck + build green
- **배포 완료**: `24ce137..bdf049e` push → deploy-central-plane.yml run 27433868321 성공 (smoke test 포함), `/healthz` 200
- **D1 migrations 0036~0039** remote 적용 완료 (0036/0037도 미적용 상태였음)
- 주의: 로컬 `pnpm ship`은 Containers binding 때문에 Docker 필요 → 항상 main push로 Actions 배포

## Stage 37-38 요약

**Stage 37** — 특정 run을 기준으로 다시 확인하기
- `rerunOfReviewRunId?` in POST review; source run validation (3-way: project+repo+prNumber)
- `compareSpecificReviewRuns` helper → `SpecificRunComparison` type
- dashboard detail 페이지: `RerunPanel` + `ComparisonPanel`

**Stage 38** — rerun 결과 비교를 PR comment에 포함
- D1 migration 0039: `rerun_of_review_run_id TEXT` 컬럼 + 인덱스
- `pr-review-db.ts` 전면 재작성 (shared COLS/mapRow)
- `buildRerunComparisonPart()`: "다시 확인 결과 비교" 섹션 (4 그룹)
- comment/preview + comment/post: `includeRerunComparison` 옵션, 5개 warning 코드
- dashboard: lineage badge + source run 링크 + CommentPanel checkbox (lineage 있을 때 default 체크)

## 다음 세션 픽업

1. actual debit 활성화: `ACTUAL_DEBIT_ALLOWED_USER_KEYS`에 user key 추가 (Containers 함정 — secret은 Actions workflow_dispatch로)
2. deploy-central-plane.yml의 Node 20 actions deprecated 경고 — **2026-06-16부터 Node 24 강제**, actions 버전 업데이트 필요
3. Stage 33 credit top-up UX (낮은 우선순위)
4. Stage 39 착수 전 결정: 다음 우선순위 (dashboard 미배포분 Vercel 확인 등)
