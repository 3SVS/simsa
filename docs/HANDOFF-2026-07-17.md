# HANDOFF — 2026-07-17

> "이제 작동하냐 + 정확도/퀄리티를 팩트로 가져와봐(경쟁 비교 포함), 잘 보이려 하지 말고" →
> 실측 평가 → 지적된 P0 2건 당일 수정·배포. 진실의 소스: `docs/simsa-prd.md`.

## 한 줄 요약

프로덕션 Simsa 실측 평가 후 **지적된 P0 2건을 당일 수정·배포·라이브 실증**. **P0-A 실현가능성**
(#346): 모바일 게임 등 웹 불가 아이디어를 경고 없이 스펙화하던 것 → 결정론 감지 + 정직 경고 +
네이티브 excluded 분리. **P0-B 검수 오탐**(#347): 작동하는 vercel.com을 "작동 안 해요"로 오판
하던 것 → 노이즈(애널리틱스·봇차단) 격리, Potemkin(백엔드 실패)은 유지. 평가 리포트 아티팩트에
업데이트 배너 추가(측정 기록 보존).

## 실측 평가 (아티팩트: `simsa-assessment.html`, claude.ai/code/artifact/d0d7e434…)

프로덕션에 실제 입력을 익명으로 돌려 측정. **팩트 요약:**
- **아이디어→스펙→빌더팩 앞단: 견고.** 6/6 실제 LLM, 항목 10개·완성기준 100%, 빌더팩 6파일.
  진짜 강점 = **범위 정직성**(v1에서 뺄 것 명시, 의료 케이스 "진단·처방" 제외 = 자격 경계 준수).
- **약점 3건(당시 실측):** ①실현가능성 경고 부재 ②시각 검수 false-negative(vercel 오판)
  ③비개발자 언어 누수(openQuestions의 Firebase/AWS/API).
- **경쟁:** 코드리뷰(CodeRabbit/Greptile 등) 최상위도 F1 50~62%·노이즈 전쟁. 아이디어→스펙
  생성기(ChatPRD/Kiro/Spec Kit)는 문서에서 멈춤. 앱검증(Replit/Autonoma)은 개발자용·빌더 종속.
  **"비개발자 acceptance 레이어" 단일 제품은 없음(미점유 사분면)**, 단 Replit·Autonoma 인접.

## 수정·배포 (main HEAD=`6927f30`, central-plane 배포 완료)

| PR | 내용 | 라이브 실증 |
|---|---|---|
| **#346 P0-A** | `detectNonWebBuildable()` 결정론 감지(mobile/desktop/game/hardware/extension, 웹 마커 veto, 게임 단독≠네이티브) + 프롬프트 정직성 규칙 + `warnings` 주입 + 대시보드 warnings 렌더 | ✅ "아이폰 3D 게임" → 정직 경고 + excluded에 "Unity/Unreal 네이티브 앱 전문팀 필요" |
| **#347 P0-B** | 노이즈 allowlist(`isNoiseResource`: 애널리틱스/광고/폰트/텔레메트리만)·나머지(자체도메인+Supabase/Firebase 백엔드)는 결함 유지. `decideFromEvidence`를 `nondev-report.ts`로 이동(테스트 가능). 콘솔 에러 판정 제외(low 격하). 못 누른 CTA→"확인 필요". **컨테이너 재빌드** | ✅ vercel.com "작동 안 해요(works=false)" → "확인 필요(works=null)". Potemkin 유지 |

**검증:** generate-feasibility 9/9 + inspection-accuracy 12/12 + 전체 1727/1727. 배포 후
`/healthz` deployedSha=main, 컨테이너 이미지 재빌드 확인(`EDIT conclave-ai-simsainspector`),
롤아웃 후 라이브 재검수 확인.

## 설계 노트

`docs/simsa-accuracy-p0-2026-07-17.md` — D1~D4 LOCKED. Bae "착수해"로 착수 승인. P0-A 원칙
= 결정론 감지+정직 경고, P0-B 원칙 = "관대함이 아니라 결함 신호와 도구 잡음의 분리".

## ★ 남은 것 (다음 세션)

- **비개발자 언어 (P1)** — openQuestions의 개발자 용어(Firebase/AWS/Chart.js/API) 제거. 실측
  3/6 케이스에서 누수. 프롬프트에 "openQuestions도 비개발자 언어로" 규칙 + 후처리 필요.
- **실제-타겟 검수 정확도 실증 (P1)** — 신생 vibe 앱(작동/깨진 것 각각 알려진 상태) 데이터셋으로
  검수 verdict 정확도 수치화. 오탐 원인은 제거됐으나 실제 타겟 수치는 아직 없음 = 진짜 증명 대상.
- **완전한 온보딩 플랫폼 매트릭스 UI** — #296 설계(web_buildable/mobile_handoff/other_handoff).
  P0-A는 경고만; 핸드오프 산출물 생성은 P1.
- 여전히 Bae 계정 필요 이월 2건(자동수리 라운드트립·T4 스모크) — HANDOFF-2026-07-16 참조.

## 교훈

- **잘 보이려 하지 말라 = 실패 사례를 실측으로.** vercel 오판을 그대로 실측·문서화한 게 P0-B
  수정의 출발점. 측정 기록은 지우지 않고 [업데이트]로 조치를 덧붙임(아티팩트).
- **정확도 = 관대함이 아니라 신호/잡음 분리.** 오탐을 줄이려 무조건 통과시키면 Potemkin(진짜
  결함)을 놓친다. 노이즈만 allowlist하고 백엔드 실패는 유지 = 정확도 양방향 개선.
- 세션은 반드시 `C:\Users\seung\.conclave\conclave-ai`에서 시작.

---

## [추가] 같은 날 후반 세션 — P1 2건 완결 + 타겟 적합성 실측 + P1~P3 (main=`47fcc6b`)

### 머지·배포 (central-plane 3회 배포: 02f00ca→95faf42→2b44a21→47fcc6b, 전부 라이브 실증)

| PR | 내용 | 라이브 실증 |
|---|---|---|
| **#351** | 비개발자 언어 P1 — 프롬프트 규칙 + `sanitizeOpenQuestions` (도구명→일반인 결정 재작성, 본인 언급 면제, 단일 초크포인트) | 프로브 4/4 누수 0 (`tools/simsa-completion-loop-spike/nondev-language-probe.mjs`) |
| **#352** | 검수 플로우 드라이버 D5~D9 — CTA 어휘 3단(의도 명명>어휘>소수 폴백)·type→클릭 합성·변화 기준 판정·number 입력값·죽은버튼 conjunction. visual-run.mjs의 **#347 이전 낡은 판정 사다리 사본 제거** | 픽스처 재실측 |
| **#353** | uncaught 예외는 `pageerror`로 옴 — 리스너 1줄 + 인베리언트 (F4 갭) | F4 → works=false |
| **#354** | **P1~P3 (배 승인)** — D10 `web_builder` export 타겟(자기완결·Secrets UI·Publish)·D11 배포 경로 유동화(**GitHub 강요 금지**, Netlify Drop/계정생성부터 택1)·D12 `service-examples.ts` 필요 기반 예시·D13 질문 필터 | 프로브 7/7 (web_builder 팩 + 질문 청정) |

### 실측 2건 (신규 문서·인프라)

- **검수 정확도** `docs/simsa-inspection-accuracy-eval-2026-07-17.md` — ground truth 픽스처
  6종(`tools/simsa-inspection-fixtures/` + 워커 `simsa-inspection-fixtures.seunghunbae.workers.dev`).
  **베이스라인: 변별력 0**(작동앱=Potemkin 동일 리포트, 드라이버가 액션 미발화) → D5~D9 후
  **6/6 전 케이스 정답** (깨끗한 UAR / Potemkin·크래시 Needs Fix / 빈페이지 정직 안내).
  자동 검수의 상한은 UAR(works=true는 사람 수용 전용) — 채점 정정 포함.
- **타겟 적합성** `docs/simsa-target-fit-eval-2026-07-17.md` — 10분야 실측(배 지시 "객관 평가").
  분야 적합성 강함(반복 0.000·가드 4종 유지) / 스펙 스택토큰 0 / **빌더팩 10/10 CLI 전제 +
  Vercel·Supabase 단일 예시 = 이식성 미달** → P1~P3(#354)로 조치. 러너
  `tools/simsa-target-fit-eval/run.mjs`, 원자료 dump-*.json.

### 아티팩트

`simsa-assessment.html` 업데이트 ②(동일 URL): P1 2건 해결 배너, 검수 타일 6/6, §04 타겟
적합성 신설, §05 조치 현황 갱신. 측정 기록 보존.

### ★ 남은 것 (다음 세션)

- **D14 배포·서비스 동향 워처** (배 지시 "cloudflare drop/netlify 등 최신 동향 체크 기능") —
  설계 `docs/simsa-builder-pack-portability-2026-07-17.md` D14 [OPEN]: changelog-monitor 레일에
  배포 플랫폼 소스 추가 → 주간 요약 → **갱신 대상은 service-examples.ts 단일 파일**, 자동 반영
  없이 리뷰 큐 경유. 다음 트레인.
- 웹빌더 실완주 실측(Lovable/Replit 계정 필요) · solo 스펙 본문 로그인 과잉(7/16 발견 잔존) ·
  실유저 vibe 앱 검수 재확인 · 온보딩 플랫폼 매트릭스 UI(#296) · Bae 계정 이월 2건.
- **dashboard Vercel 배포 주의**: main 자동 배포가 안 걸려 있음(7/15 커밋에 멈춰 있었음) —
  대시보드 변경 머지 후 **레포 루트**에서 `npx vercel deploy --prod --yes --archive=tgz`
  (apps/dashboard에서 실행하면 루트 설정 중복으로 경로 에러, 파일 수 초과라 tgz 필수).

---

## [추가 2] 같은 날 최종 배치 — 배 지시 4건 (main=`2a59d4e`)

- **#356 (D14~D16)**: ① **D16 에이전트 선택 단계** — export 페이지가 Claude Code로 조용히
  자동 생성하던 것 제거, "빌더팩을 어떤 개발 AI용으로 받으시겠어요?" 명시 선택(4옵션) 후 생성.
  built_with 프리셀렉트는 후속(로컬 Project 셰이프에 builtWith 없음). ② **D15 solo 스펙
  auth-free** — 프롬프트 확장 + `applySoloSpecGuard`(included/userFlow/items에서 인증
  아티팩트 결정론 제거, veto는 명시적 요청만·부정 언급 제외). **라이브 실증: solo 아이디어
  → 로그인 아티팩트 0 (실 LLM)**. ③ **D14 배포 동향 워처 v1** — `deploy-trend-watcher.ts`,
  월 07:00 크론 합류, Haiku 관련성 필터 → `deploy_trend_suggestions` 리뷰 큐(migration 0059),
  `GET /admin/deploy-trends`. 자동 반영 없음(갱신 seam = service-examples.ts).
- **#358 플로우 감사 수정 전건 (배 "전부 착수 승인")** — A "전부 추천대로 하고 넘어가기"
  1클릭 · **D17 적응형 질문 수**(배 추가 지시 "6개 국한 금지": 프롬프트 4~8개+max_tokens
  10000) · B1 개요 할 일 갈래 분기 · B2 심사 지도 KO/EN 전면 비개발자 언어화 · B3 팝업
  위저드 억제 · B4 스테이지 "만들기·검수" · B5 이중 번호 · B6 아이디어 갈래 GitHub 잔재
  게이트 · B7 settings "준비와 연결". central+dashboard 배포(0b5dd2e).
  **라이브 재프로브 10/10** (`flow-fix-probe.mjs`): 복잡 아이디어→질문 8개, 일괄 수락 8/8,
  개요·지도·라벨 전부 확인.
- **전체 플로우 E2E 감사** (배 지시 "탭별 이동·프로세스 효율성·적합성 재검토") —
  `docs/simsa-flow-audit-2026-07-17.md`. 위저드 실완주(proj_hepck0xm) + 전 탭 순회.
  **효율**: 아이디어→프로젝트 ~11클릭(질문 6개 개별 수락이 최대 마찰 → "모두 추천대로" 제안),
  프로젝트→빌더팩 3클릭, 에러·데드엔드 0. **적합성 P1 2건**: ①개요 '지금 할 일'이 아이디어
  갈래에 "GitHub 저장소 연결" 코드 갈래 할 일 제시 ②심사 지도 페이지가 내부 용어(인테이크/
  릴리스 체크포인트/머지) 통째 누수. P2: 참여 팝업이 위저드 가림 · '검수' 스테이지에 '빌더 팩'.
  P3: 이중 번호("1. 1."), checks의 GitHub 섹션 잔존, 페이지 제목 부재. **수정은 미착수 —
  배 승인 대기.**
