# HANDOFF — 2026-07-17

> "이제 작동하냐 + 정확도/퀄리티를 팩트로 가져와봐(경쟁 비교 포함), 잘 보이려 하지 말고" →
> 실측 평가 → 지적된 P0 2건 당일 수정·배포. 진실의 소스: `docs/simsa-prd.md`.

## 한 줄 요약

프로덕션 Simsa 실측 평가 후 **지적된 P0 2건을 당일 수정·배포·라이브 실증**. **P0-A 실현가능성**
(#346): 모바일 게임 등 웹 불가 아이디어를 경고 없이 스펙화하던 것 → 결정론 감지 + 정직 경고 +
네이티브 excluded 분리. **P0-B 검수 오탐**(#347): 작동하는 vercel.com을 "작동 안 해요"로 오판
하던 것 → 노이즈(애널리틱스·봇차단) 격리, Potemkin(백엔드 실패)은 유지. 평가 리포트 아티팩트에
업데이트 배너 추가(측정 기록 보존).

## 실측 평가 (아티팩트: `simsa-assessment.html`, claude.ai/code/artifact/d0d7e434…)

프로덕션에 실제 입력을 익명으로 돌려 측정. **팩트 요약:**
- **아이디어→스펙→빌더팩 앞단: 견고.** 6/6 실제 LLM, 항목 10개·완성기준 100%, 빌더팩 6파일.
  진짜 강점 = **범위 정직성**(v1에서 뺄 것 명시, 의료 케이스 "진단·처방" 제외 = 자격 경계 준수).
- **약점 3건(당시 실측):** ①실현가능성 경고 부재 ②시각 검수 false-negative(vercel 오판)
  ③비개발자 언어 누수(openQuestions의 Firebase/AWS/API).
- **경쟁:** 코드리뷰(CodeRabbit/Greptile 등) 최상위도 F1 50~62%·노이즈 전쟁. 아이디어→스펙
  생성기(ChatPRD/Kiro/Spec Kit)는 문서에서 멈춤. 앱검증(Replit/Autonoma)은 개발자용·빌더 종속.
  **"비개발자 acceptance 레이어" 단일 제품은 없음(미점유 사분면)**, 단 Replit·Autonoma 인접.

## 수정·배포 (main HEAD=`6927f30`, central-plane 배포 완료)

| PR | 내용 | 라이브 실증 |
|---|---|---|
| **#346 P0-A** | `detectNonWebBuildable()` 결정론 감지(mobile/desktop/game/hardware/extension, 웹 마커 veto, 게임 단독≠네이티브) + 프롬프트 정직성 규칙 + `warnings` 주입 + 대시보드 warnings 렌더 | ✅ "아이폰 3D 게임" → 정직 경고 + excluded에 "Unity/Unreal 네이티브 앱 전문팀 필요" |
| **#347 P0-B** | 노이즈 allowlist(`isNoiseResource`: 애널리틱스/광고/폰트/텔레메트리만)·나머지(자체도메인+Supabase/Firebase 백엔드)는 결함 유지. `decideFromEvidence`를 `nondev-report.ts`로 이동(테스트 가능). 콘솔 에러 판정 제외(low 격하). 못 누른 CTA→"확인 필요". **컨테이너 재빌드** | ✅ vercel.com "작동 안 해요(works=false)" → "확인 필요(works=null)". Potemkin 유지 |

**검증:** generate-feasibility 9/9 + inspection-accuracy 12/12 + 전체 1727/1727. 배포 후
`/healthz` deployedSha=main, 컨테이너 이미지 재빌드 확인(`EDIT conclave-ai-simsainspector`),
롤아웃 후 라이브 재검수 확인.

## 설계 노트

`docs/simsa-accuracy-p0-2026-07-17.md` — D1~D4 LOCKED. Bae "착수해"로 착수 승인. P0-A 원칙
= 결정론 감지+정직 경고, P0-B 원칙 = "관대함이 아니라 결함 신호와 도구 잡음의 분리".

## ★ 남은 것 (다음 세션)

- **비개발자 언어 (P1)** — openQuestions의 개발자 용어(Firebase/AWS/Chart.js/API) 제거. 실측
  3/6 케이스에서 누수. 프롬프트에 "openQuestions도 비개발자 언어로" 규칙 + 후처리 필요.
- **실제-타겟 검수 정확도 실증 (P1)** — 신생 vibe 앱(작동/깨진 것 각각 알려진 상태) 데이터셋으로
  검수 verdict 정확도 수치화. 오탐 원인은 제거됐으나 실제 타겟 수치는 아직 없음 = 진짜 증명 대상.
- **완전한 온보딩 플랫폼 매트릭스 UI** — #296 설계(web_buildable/mobile_handoff/other_handoff).
  P0-A는 경고만; 핸드오프 산출물 생성은 P1.
- 여전히 Bae 계정 필요 이월 2건(자동수리 라운드트립·T4 스모크) — HANDOFF-2026-07-16 참조.

## 교훈

- **잘 보이려 하지 말라 = 실패 사례를 실측으로.** vercel 오판을 그대로 실측·문서화한 게 P0-B
  수정의 출발점. 측정 기록은 지우지 않고 [업데이트]로 조치를 덧붙임(아티팩트).
- **정확도 = 관대함이 아니라 신호/잡음 분리.** 오탐을 줄이려 무조건 통과시키면 Potemkin(진짜
  결함)을 놓친다. 노이즈만 allowlist하고 백엔드 실패는 유지 = 정확도 양방향 개선.
- 세션은 반드시 `C:\Users\seung\.conclave\conclave-ai`에서 시작.
