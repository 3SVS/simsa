# HANDOFF — 2026-06-24

Four trains today. Two merged to `main` (MCP work), two are decision-ready PRs awaiting Bae merge approval. MCP stays **unpublished**; no production deploy performed.

## Merged to main
1. **Stage 141~147 — MCP Server Runtime Wiring** → `481fd72` (merge-only). 9 Basic tools registered + Basic-only mode; `copy-basic-tools.mjs` ships the Basic `.mjs` in `dist`.
2. **Stage 148~153 — MCP Manual Host QA / Private Dogfood** → `de6f7e6` (merge-only). Dev tooling: `smoke:basic`, `smoke:basic:stdio` (real stdio MCP protocol), `qa:basic-tools`, `print:claude-desktop-basic-config`; troubleshooting docs.
3. **Stage 154~159 — MCP Private Dogfood Evidence + Handoff i18n** → `dfb15eb` (merge-only). Claude Desktop app-side evidence **Status A (Bae-confirmed)**; `Simsa-Basic` display name; intake/handoff **P0 first-screen i18n** (`intake.*` en+ko) — **merged, not deployed**.

## Merged to main (cont.)
4. **Stage 160~166 — Simsa Wax Seal Thinking / Motion System** → **`9c4e593`** (merge-only). Burgundy wax-seal pulse + evidence dots + calm i18n status. `SimsaSealThinking` (compact/panel), `lib/seal-thinking.mjs` pure helper, `globals.css` `@keyframes` + reduced-motion, `loading.*` en/ko, truthful intake integration (panel on real `detailLoading`; compact on Save/Refresh; instant deterministic previews keep no spinner). Dashboard-only (8 src/test + 6 docs). **Merged, not deployed.** Post-merge on `main`: dashboard 232/232, typecheck + build ok; monorepo typecheck 57/57; MCP regression 74/74; worktree clean.

## 5) Workspace Collaboration / Profile / Integrations Foundation (Stage 168~174) — IN PROGRESS, NOT merged
**PR #155** `docs/stage-168-workspace-collaboration-integrations`, HEAD `087b7bf`. Mostly planning/docs + one small now-safe code slice. **Do not merge until the Stage 175 checkpoint + Bae approval.**
- **168** gap inventory · **169** `/account` IA · **170** **code:** local `/account` stub (`app/account/page.tsx` + `lib/account-preferences.mjs` + `account.*` i18n EN/KO + sidebar badge→`/account`; **no real auth**) · **171** workspace/team model planning · **172** share/invite/permission UX · **173** export/import surface · **174** GitHub/Vercel integration UX + safety.
- **Verified facts:** tenancy = client `user_key` (no User/members/roles tables; migrations ≤0046). GitHub backend (OAuth + encrypted tokens + PR) **exists**; project-level GitHub UI exists; Vercel = CLI `platform-vercel` adapter + URL-as-intake only (no dashboard integration). Export exists (zip + central-plane `/export`); import missing.
- **★ Gating dependency:** the whole collaboration layer (workspace/team/invite/roles/share/private+public links/account-level integrations) is **blocked by a real auth/identity decision** — `userKey` is tenant-scoping, not auth. An Auth/Identity train likely must come first.
- Post-Stage-170 verification on the branch: dashboard **242/242**, typecheck + build ok (`/account` 1.87 kB), monorepo typecheck 57/57.
- **Next: Stage 175 — Collaboration Foundation Checkpoint / PR #155 merge-readiness review** (decision + Bae approval; NOT an automatic merge).

## Pickup points for next session
1. **Stage 175 — PR #155 merge-readiness checkpoint** (the live in-progress work). Then Bae merge decision.
2. ~~Bae merge approval for PR #154~~ — **DONE: merged → main `9c4e593`** (merge-only, no deploy).
3. **MCP Manual Host evidence is Status A** (Bae ran Claude Desktop) — only follow-up is whether to act on the recorded UX gaps.
4. **Biggest strategic decision pending: auth/identity** — gates the entire collaboration layer (Stage 171~174). Until then only the now-safe `/account` stub + export/handoff/copy sharing exist.
5. **Open follow-ups (all await Bae):**
   - **Dashboard deploy** — would ship the motion system (now on `main` `9c4e593`) + the Stage 159 intake i18n (already on `main`) live to `app.trysimsa.com`. `vercel deploy --prod` from repo root, project `conclave-dashboard`. **Not deployed yet.**
   - **Intake i18n P1** — onboarding/beta/deep-section copy + `lib/intake-*.mjs` labels (Stage 158 deferred).
   - **Locale propagation** — `I18nProvider` `?locale=` + handoff builder `locale` param.
   - **App-wide spinner replacement** — 8 generic `animate-spin` page sites (deliberate later pass).
   - **MCP custom icon** — only if a supported Claude Desktop config field is confirmed.
   - **MCP publish readiness / release** — separate Bae-approved release train (publish held).

## Cautions
- **No payment provider assumption** — provider **TBD**, Korea-compatible first; Stripe not assumed.
- MCP stays **unpublished**; publish only via the lockstep `release.yml` on explicit Bae approval, never from a laptop.
- Motion: burgundy reuses existing `brand` (oxblood) tokens — no new color system; reduced-motion + `role="status"` are required in any future motion work.
