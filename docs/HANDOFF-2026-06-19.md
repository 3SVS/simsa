> **SUPERSEDED** — 이 문서는 Conclave 시대 가정 하에 작성되었습니다. 제품은 Simsa로 전환되었고, 최신 방향·우선순위는 `docs/simsa-research-audit-2026-07.md` (감사 v2)를 보세요.

# HANDOFF — 2026-06-19

## Stage 63 마무리 — 대시보드 인터페이스 전체 i18n + 라이브 배포

전체 대시보드 인터페이스를 EN/KO 토글(영어 기본)로 전환 완료하고 프로덕션에 배포·검증했습니다.

### 오늘 커밋 (main)
| 커밋 | 내용 |
|---|---|
| `6964ffa` | github 깊은 패널(ReviewResult/PRComment/FixBrief) i18n + 중복 `comparison` 네임스페이스 정리 |
| `da5e814` | run detail 화면(history/[runId]) i18n |
| `211decb` | settings 누락 에러 문구 2건 |
| `4954b15` | credits + export 화면 (blue→브랜드 indigo, 이모지 제거) |
| `faa5e39` | admin/usage + admin/credits (adminCredits 174 keys; 서브에이전트 변환 후 직접 검증) |
| `9f6c964` | QuestionCard + run-detail/history `.mjs` 헬퍼 렌더 문구 dict 이동 |
| `0a15b93` | rate-limit 토스트 → `common.rateLimited` |
| `3f53995` | stage-63 문서 (pass 2e~2h) |

### 결과 — 렌더되는 인터페이스 한글 = 0
남은 한글은 모두 비렌더: KO 사전(의도) · 코드 주석 · 파일명 정규식 `[가-힣]`(기능) · 데드코드(`labels.ts` 한글맵, 미사용 `MockUserBadge`, `review-run-comparison.mjs` STATUS_KO/buildStatusTransitionLabel) · `mock-generators.ts` 데모/오프라인-폴백 제품 콘텐츠(생성 콘텐츠 known-issue) · backend `summaryText`(대시보드 범위 밖).

### i18n 패턴 (누적)
Dictionary-first: `apps/dashboard/src/i18n/dictionary.mjs`(+`.d.mts` 타입, parity 테스트 `test/i18n.test.mjs`). `useI18n()`/`statusLabel(t,...)`. run-status는 `statusText(t, status)`(statusLabel + runStatus fallback). 한글 라벨맵 → `(t, value)` 헬퍼, 컬러맵은 모듈 상수 유지. brand: `blue`→`indigo`(tailwind가 indigo→옥스블러드 remap). 보간은 `"{n}"` + `.replace`.

### 검증 (각 패스 동일)
dashboard **77/77** · i18n parity **10/10** · typecheck clean · build green(16 routes) · lint clean(기존 `export/page.tsx` useEffect 경고만).

### 라이브 배포
- `https://conclave-dashboard.vercel.app` — Vercel `seunghunbae-3svs-projects/conclave-dashboard`.
- ★ **Root Directory = apps/dashboard** 설정 → 배포는 **repo root**에서 `vercel --prod` 실행해야 함(apps/dashboard에서 실행 시 경로 2중 append로 실패). Git 미연결 → 수동 배포.
- 검증: admin/usage가 새 EN 문구를 SSR("Admin — Usage", "Admin Key", "Load", "Range"). `/projects` 200, `/admin/usage` 200, `/` 307.
- 브라우저 EN/KO 토글 E2E는 Bae 수동 육안(에이전트 브라우저 조작 불가); SSR 영어 기본 콘텐츠가 에이전트측 증빙.

### 다음 세션 픽업 포인트
1. **Bae 수동 육안 QA** — 라이브에서 우측 상단 EN/KO 토글로 9개 화면(credits/export/admin 2개/run detail/github 패널 등) 전환 확인.
2. **노출된 Vercel 토큰 revoke** (이번 세션에서 배포에 사용).
3. (선택) 데드코드 정리: `labels.ts` 한글 라벨맵·미사용 `MockUserBadge` 제거, `mock-generators.ts` 데모 콘텐츠 영문화.
4. (선택) Vercel Git 연결로 수동 배포 제거.
5. 스펙상 다음 후보: Stage 64 "Multi-Agent Build Benchmark".

### 주의사항
- 안전 플래그(actual debit/billing) 여전히 OFF — 변경 없음.
- backend/central-plane 무변경. 이번 작업은 dashboard 프론트 전용.
- 기존 프로젝트의 저장 데이터 언어는 마이그레이션 안 함(known issue 유지).

---

## Stage 64 — Multi-Agent Build Benchmark (`917d1e0`)

Conclave 핵심 가설(다중 에이전트 워크플로가 단일 에이전트보다 더 잘 합격하는 구현을 만든다)을 acceptance 기준으로 검증하는 최소 결정적 구조. 실제 에이전트 실행 없이 기존 review run을 candidate로 비교.

- **모델**: `apps/dashboard/src/lib/agent-benchmark.mjs`(+`.d.mts`) — 순수·결정적. metrics(passRate/critical=failed+needs_decision/not-verified=inconclusive/score=passed*3-failed*3-needs_decision*2-inconclusive*1), rank(score→passRate→critical→not-verified→id), winner(+structured rationale/blockers), no-clear-winner(score차≤1 & 같은 passRate & 같은 critical), recommendation은 candidate≥2.
- **★결정**: dashboard-side 계산 — 입력이 이미 review-history로 클라이언트 접근 가능 + 순수/테스트 가능 + UI 작업. central-plane/엔드포인트/MCP/마이그레이션 무변경. 순수 함수라 후속에 central-plane 이전 가능.
- **UI**: `/projects/:id/benchmark`(client) — selector(label/mode/source)·metrics cards·comparison table·recommendation·blockers·next action·empty/need-more. nav Review 그룹에 Benchmark 추가.
- **i18n**: `benchmark.*` + `nav.benchmark`(EN/KO/.d.mts, parity 10/10). rationale/blocker는 structured→UI에서 t 렌더(영어 박지 않음).
- **테스트**: `test/agent-benchmark.test.mjs` 12개 → dashboard **89/89**.
- **배포/검증**: 라이브 배포 READY, `/projects/:id/benchmark` 200, mock 프로젝트 SSR 영어 본문 확인("Compare build candidates" 등). 실데이터 비교/토글은 Bae 수동.
- **문서**: `conclave-builder-pack/out/stage-64-multi-agent-build-benchmark.md`.
- **검증**: typecheck clean · build green(17 routes) · lint clean(기존 export 경고만).

### Stage 65 후보
persisted benchmark(central-plane 이전 + endpoint + ownership/run-project 검증) · 동일 acceptance set 정합성 가드 · (장기) 실 에이전트 실행 연결 · score 가중 캘리브레이션.

---

## Stage 65 — Persisted Multi-Agent Build Benchmark (`850c344`, 배포/마이그레이션 완료)

Stage 64 dashboard-only 벤치마크를 central-plane persisted·ownership-검증·cross-device artifact로 발전.

- **D1**: migration `0040_workspace_agent_benchmarks`(result_json snapshot + 승격 컬럼 candidate_count/winner_candidate_id/no_clear_winner). **워크플로로 원격 적용 완료.**
- **Canonical 로직**: `workspace/agent-benchmark.ts`(Stage 64 .mjs 포팅 + `computeAcceptanceSetAlignment`). 저장본 = source of truth, dashboard .mjs = 미저장 preview. ★두 구현은 공유 golden fixture(`test/fixtures/agent-benchmark-golden.json`)를 양쪽 테스트가 assert해 lock-step.
- **API**: `POST/GET list/GET detail /workspace/projects/:id/agent-benchmarks`. 서버 검증: userKey·candidate 2~5·id 유니크·각 reviewRun 존재 AND project_id 일치 AND user_key 일치. 저장본은 소유자에게만.
- **Alignment guard**: candidate run들의 selectedItemIds 비교 → 다르면 aligned:false+warning, snapshot에 저장.
- **Dashboard**: `workspace-benchmark-api.ts` + `/benchmark` 확장(title·Save(2~5 gate)·saved list·acceptance warning). i18n benchmark.* 확장.
- **테스트**: central-plane **967/967**(5 golden+12 route 신규), dashboard **95/95**(parity+save gate), repo typecheck 54/54, build green.
- **라이브 검증**: deploy-central-plane.yml(migration 0040 적용 ✓ · Worker deploy ✓ · smoke ✓). GET list→`{"ok":true,"benchmarks":[]}`, POST 검증(userKey/count/run-not-found) 라이브 동작. dashboard 재배포 200. 실 run 2개 POST 라운드트립은 Bae 수동.
- **문서**: `conclave-builder-pack/out/stage-65-persisted-agent-benchmark.md`.

### Stage 66 후보
saved-detail 전용 뷰+deep-link · acceptance set strict 정책 · benchmark rename/delete · (장기) 실 에이전트 실행 연결 · score 가중 캘리브레이션.

---

## Stage 66 — Benchmark Evidence Detail (`3238b5f`, dashboard 배포 완료)

저장된 벤치마크를 재방문·공유 가능한 evidence artifact로 완성. **dashboard 전용**(Stage 65 엔드포인트 재사용, 백엔드 무변경).

- **신규 route** `/projects/:id/benchmark/:benchmarkId`: recommendation(winner/no-clear-winner + "추측하지 않는다" 프레이밍) · candidate comparison table(mode/source/passed/critical/not-verified/score/run) · per-candidate metrics(원본 count 병기) · Why(rationale) · winner blockers(status 그룹) · acceptance-set alignment(misaligned 강조/aligned 작게) · source review runs · Open review run 링크. saved list **Open → detail**(기존엔 winner run).
- **Copy summary**: `buildBenchmarkSummaryText` 순수 결정적 assembler(LLM 금지), UI가 지역화 라인 주입 → 복사 텍스트가 EN/KO 따름. 3 tests.
- **i18n**: benchmark.* 확장(detail*/recommendedCandidate/why/copySummary/sourceRuns/scoreNote/summary*…), EN/KO, parity 10/10.
- **테스트**: dashboard **98/98**, typecheck clean, build green(18 routes), lint clean. ★렌더 레벨 테스트는 node --test 전용 환경상 비현실적→로직(copy-summary)+라이브로 커버.
- **라이브**: 재배포 200, detail SSR "Loading the benchmark…"(EN), backend GET detail 미존재→404. populated detail 육안은 Bae 수동(실 run 2개로 Save→Open).
- **문서**: `conclave-builder-pack/out/stage-66-benchmark-evidence-detail.md`.

### Stage 67 후보
benchmark rename/delete(+UI) · 공유 링크/PR comment 붙이기 · acceptance set strict 정책 · item-level blocker 노출 · (장기) 실 에이전트 실행 연결.

---

## Stage 67 — Benchmark PR Comment Evidence (`ff818c8`, dashboard 배포 완료)

저장된 benchmark를 GitHub PR comment로 공유(preview-first + explicit confirm). **dashboard 전용** — 기존 comment POST 엔드포인트가 customBody를 그대로 게시 → 재사용, 백엔드/OAuth/MCP 무변경.

- **순수 markdown**: `lib/agent-benchmark-comment.mjs` — `buildBenchmarkPrCommentMarkdown`(테이블+recommendation+why+blockers+note+misalign blockquote, LLM/network/token 없음) + `resolveBenchmarkPrTarget`(모든 candidate가 같은 PR→post 허용, 다르면 copy-only).
- **detail Share 섹션**: Preview PR comment→미리보기+경고→Post comment to PR(postPRComment에 deterministic body+같은PR candidate reviewRunId 전달)→posted+링크. mixed-PR→copy-only note. Copy summary 유지.
- **i18n**: benchmark.* 확장(previewPrComment/postToPr/postWarning/mixedPrNote/prIntro/…), EN/KO, parity 10/10.
- **테스트**: dashboard **107/107**(comment 9 신규: winner/no-clear/misalign/no-blockers/no-leak/deterministic + same/mixed/missing PR). typecheck clean, build green(18 routes), lint clean.
- **라이브**: 재배포 200, detail SSR OK. populated preview/post 육안은 Bae 수동(실 saved benchmark+같은PR candidate+GitHub 연결 필요; 기존 comment 엔드포인트는 이전 라이브 게시 검증됨).
- **문서**: `conclave-builder-pack/out/stage-67-benchmark-pr-comment-evidence.md`.

### Stage 68 후보
benchmark preview/post audit 이벤트 · item-level blocker(항목 제목) · benchmark rename/delete(+UI) · read-only share URL · (장기) 실 에이전트 실행 연결.

---

## Stage 68 — Item-level Benchmark Blockers (`957f4b6`, 배포 완료)

benchmark evidence를 count → acceptance item 단위(어떤 항목이 막혔는지 제목·상태·근거·후보)로 강화.

- **canonical**: `agent-benchmark.ts`에 extractCandidateItemOutcomes + remainingBlockers(winner/top-ranked non-passed 항목, severity issue/decision/not_verified, 저장 evidence). buildBenchmarkResult가 `itemResultsByCandidate` 수신→itemOutcomesByCandidate/remainingBlockers/blockerBasisCandidateId. route가 run의 results 파싱해 전달. LLM 없음.
- **dashboard mirror**(.mjs) — ★공유 golden fixture에 item-level 케이스 추가로 lock-step.
- **detail**: Remaining blocker items 섹션(배지+제목+근거+후보). 구버전 saved는 필드 부재→count fallback + "newer benchmarks" note.
- **copy/markdown**: item-level 있으면 항목 라인(markdown은 bold status + 들여쓴 evidence), 없으면 count fallback. blockerLines가 string|{text,evidence?} 수용.
- **i18n**: blockerItems*/evidence/oldBenchmarkBlockers.
- **backward compat**: item results 없으면 필드 부재(골든 명시 검증).
- **테스트**: central 968/968(골든+route item-level), dashboard 108/108(parity+markdown item-level), repo typecheck 54/54, build green.
- **라이브**: deploy-central-plane(Worker✓ smoke✓), dashboard 재배포. GET list/POST 검증 라이브, detail 200. populated 육안은 Bae 수동(non-passed 항목 가진 실 run 2개로 새 benchmark).
- **문서**: `conclave-builder-pack/out/stage-68-item-level-benchmark-blockers.md`.

### Stage 69 후보
후보별 acceptance item matrix UI(itemOutcomes 이미 저장) · benchmark rename/delete · share URL/audit 이벤트 · evidence 품질(출처/다중) · (장기) 실 에이전트 실행 연결.

---

## Stage 69 — Benchmark Item Matrix (`442bc3c`, dashboard 배포 완료)

Stage 68 `itemOutcomesByCandidate`로 detail에 후보 × acceptance item matrix. **dashboard 전용**(백엔드 무변경).

- **순수 helper** `lib/agent-benchmark-matrix.mjs`: `buildBenchmarkMatrix` → rows(항목×후보 status, missing 포함, hasDisagreement, best/worst severity) + itemsCompared/disagreementCount. candidate/item order 안정, 결과 없으면 missing.
- **detail**: Acceptance item matrix 섹션(insights N compared/M different + status 배지 표 + 후보 컬럼 label·mode·source + disagreement 행 amber tint·"Different results" chip). 구버전 saved(필드 부재)→fallback("newer benchmarks") note.
- **i18n**: matrixTitle/Desc/differentResults/matrixItemsCompared/Disagreements/Unavailable*/missingResult.
- **copy/PR comment**: 무변경(Stage 68 유지, spec 옵션 — PR 비대화 방지).
- **테스트**: dashboard **114/114**(matrix 6: order/missing/disagreement/best-worst/fallback/empty). typecheck 54/54류, build green, lint clean.
- **라이브**: 재배포 200, detail SSR OK. populated matrix 육안은 Bae 수동(Stage 68+ saved benchmark 필요).
- **문서**: `conclave-builder-pack/out/stage-69-benchmark-item-matrix.md`.

### Stage 70 후보
matrix evidence 노출 · matrix 정렬/필터(disagreement만) · copy/PR compact insight · benchmark rename/delete·share URL·audit · (장기) 실 에이전트 실행.

---

## Stage 70 — Benchmark Matrix Evidence Drilldown (`99ff763`, dashboard 배포 완료)

Stage 69 matrix를 실용 evidence view로. **dashboard 전용**.

- **disagreement 필터**: "Show different results only" 토글(`filterMatrixRows`), 없으면 empty state.
- **evidence drilldown**: row별 View/Hide evidence → 후보별 status+evidence(없으면 "No evidence text was stored" fallback). 저장 evidence만, LLM 없음.
- **matrix insight**: `getMatrixInsights`(itemsCompared/disagreementCount) → copy summary·PR comment에 compact 2줄(counts only, ★full 표 PR comment 금지). builders에 optional matrixHeading/matrixLines 추가, 없으면 생략(구버전 호환).
- **i18n**: showDifferentOnly/viewEvidence/hideEvidence/noEvidenceStored/noDifferentResults*/matrixInsight*.
- **테스트**: dashboard **122/122**(filter/insights/evidence + summary·comment matrix-insight, no-leak). typecheck clean, build green(18 routes), lint clean.
- **라이브**: 재배포 200, detail SSR OK. populated 토글/drilldown 육안은 Bae 수동(Stage 68+ saved 필요).
- **문서**: `conclave-builder-pack/out/stage-70-benchmark-matrix-evidence-drilldown.md`.

### Stage 71 후보
matrix 정렬(status/disagreement) · evidence 출처(파일/라인) · benchmark rename/delete·share URL·audit · 다중 evidence · (장기) 실 에이전트 실행.

---

## Stage 71 — Manual Multi-Agent Experiment (`0ecfd8b`, dashboard 배포 완료)

Conclave 안에서 single vs multi-agent 개발 실험 계획 + 역할별 deterministic prompt 복사 → 결과 PR을 benchmark로 비교하는 manual protocol. **dashboard 전용**(agent 실행/백엔드/benchmark 저장 무변경).

- **순수 로직** `lib/agent-experiment.mjs`: EXPERIMENT_TEMPLATES 3종(single baseline/multi split/builder+reviewer) + `buildCandidatePrompt`(역할지시+context+brief+acceptance items+constraints+output+report, LLM/token 없음) + `buildAllPromptsText`.
- **`/projects/:id/experiment`**: 템플릿 선택·후보 prompt 카드(role·agent·Copy)·Copy all·workflow 6단계·purpose note(실험 vs Fix)·Open Benchmark. nav Review 그룹. brief/items는 로컬 프로젝트 도출.
- **i18n**: experiment.* + nav.experiment. prompt도 UI 언어 따름.
- **테스트**: dashboard **128/128**(experiment 6: 템플릿/prompt 포함/copy-all/no-leak). typecheck clean, build green(19 routes), lint clean.
- **라이브**: 재배포 200, SSR 풀 렌더("Manual multi-agent experiment"/"Choose a template"/"Single agent baseline"/"How to run…") — 데이터 불필요. 토글/Copy 육안은 Bae.
- **문서**: `conclave-builder-pack/out/stage-71-manual-multi-agent-experiment.md`.

### Stage 72 후보
persisted experiment(central-plane) · experiment→benchmark 후보 자동연결 · prompt 커스터마이즈 · experiment outcome 기록 · (장기) 실 에이전트 실행.

---

## Stage 72 — Persisted Multi-Agent Experiments (`c0f059d`, 배포/마이그레이션 완료)

Stage 71 prompt generator를 저장·재방문·candidate PR/review run 추적 workflow로.

- **D1**: migration 0041 — `workspace_agent_experiments` + `workspace_agent_experiment_candidates`(별도 테이블, 링크 자주 PATCH). 원격 적용 완료.
- **API**: POST/GET list/GET detail/PATCH-candidate `/workspace/projects/:id/agent-experiments`. 서버검증: userKey·title·templateId allowlist·candidate enum·id 유니크; experiment 소유; reviewRunId/benchmarkId는 같은 project+userKey 소유. status는 링크에서 도출(planned→pr_linked→reviewed→benchmarked).
- **dashboard**: `/experiment`에 Save experiment·Saved list·candidate linking 패널(PR input + review run 드롭다운 + Update). `workspace-experiment-api.ts` + 순수 canSaveExperiment/experimentCandidateStatus.
- **i18n**: experiment.* 확장.
- **테스트**: central 979/979(experiment 11), dashboard 130/130, repo typecheck 54/54.
- **★라이브 populated 라운드트립**(가짜 아님): POST→201(draft) → GET list(candidateCount 2) → PATCH PR#9→pr_linked. migration 0041 적용 검증.
- **문서**: `conclave-builder-pack/out/stage-72-persisted-agent-experiments.md`.

### Stage 73 후보
experiment→benchmark 원클릭(reviewRun prefill) · experiment status 전이 · prompt_copied/selected/rejected 기록 · benchmark↔experiment 양방향 · (장기) 실 에이전트 실행.

---

## Stage 73 — Experiment → Benchmark Handoff (`9424f80`, 배포/마이그레이션 완료)

experiment의 linked review run으로 바로 benchmark 생성 + 결과 역링크. end-to-end evidence workflow.

- **★결정 Option B**: 신규 엔드포인트 + source_experiment_id 컬럼(migration 0042). Stage 65 계산 재사용(복제 아님)→원자적·서버검증·source 저장.
- **API**: `POST …/agent-experiments/:experimentId/benchmark` — experiment 소유 · linked(reviewRunId) candidate 2+ · 각 run project+userKey 소유 · benchmark 생성(sourceExperimentId) · candidate benchmarkId+status benchmarked · experiment status benchmarked.
- **benchmark-db**: source_experiment_id 저장/반환. benchmark detail이 노출.
- **dashboard**: experiment detail Benchmark handoff(Create from experiment, 2+ 게이트, Open benchmark result) + benchmark detail "Source experiment→Open experiment" + experiment ?experiment= 자동 open. 순수 canCreateBenchmarkFromExperiment/mapExperimentCandidatesToBenchmark.
- **i18n**: experiment.*/benchmark.* 확장.
- **테스트**: central 983/983(handoff 4), dashboard 132/132, repo typecheck 54/54.
- **라이브**: migration 0042 적용✓. handoff 계약 검증(not_enough_linked_runs/userKey_required/not_found), `/experiment` 200. populated는 Bae 수동.
- **문서**: `conclave-builder-pack/out/stage-73-experiment-benchmark-handoff.md`.

### Stage 74 후보
experiment status 전이 전체 · benchmark 후 selected/rejected outcome · handoff 재생성 정책 · acceptance set strict · (장기) 실 에이전트 실행.

---

## Stage 74 — Experiment Outcome Decision (`31327c6`, 배포/마이그레이션 완료)

Experiment→Benchmark 이후 candidate 채택/거절/수정 결정 기록.

- **D1**: migration 0043 — candidate outcome/outcome_note/decided_at + experiment decision_status/selected_candidate_id/decision_note/decided_at(nullable).
- **API**: `POST …/agent-experiments/:eid/decision`. 검증: experiment 소유·candidate 소속·outcome enum·selected≤1·selectedCandidateId 일치·decisionStatus allowlist·note≤1000. candidate outcome+status(selected/rejected/needs_fix), experiment status(selected→completed, needs_fix→decision_made), decision 요약 저장. GET detail에 decision 필드.
- **dashboard**: experiment Decision 섹션(후보별 winner/needs-fix/reject + note + decision note + Save, benchmark 경고) + benchmark detail "Ready to decide?" callout(Record decision→experiment 자동open). 순수 buildExperimentDecision.
- **i18n**: experiment.*/benchmark.* 확장.
- **테스트**: central 992/992(decision 9), dashboard 135/135, repo typecheck 54/54.
- **★라이브 populated 라운드트립**: create→decision(b selected/a rejected)→completed→GET 영속→multiple_selected 검증. migration 0043 적용.
- **문서**: `conclave-builder-pack/out/stage-74-experiment-outcome-decision.md`.

### Stage 75 후보
decision→next action(selected blocker로 Fix instructions) · decision 이력 · decision을 PR comment/copy 반영 · experiment status 전체 전이 · (장기) 실 에이전트 실행.
