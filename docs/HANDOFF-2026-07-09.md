# HANDOFF — 2026-07-09

> 다음 세션/다른 워크스페이스가 읽는 **인수 문서**. (세션별 상세는 Claude 메모리
> `session-2026-07-09-prd-nondev-copy-ux.md`.) 진실의 소스: `docs/simsa-prd.md`.

## 1. 오늘 배치 — T1~T6 (전부 main 반영)

단일 PRD 통합(`docs/simsa-prd.md`, #315) 위에서 비개발자 카피/정직화 + UX Basics 전수.

| 태스크 | 내용 | PR | 상태 |
|---|---|---|---|
| T1 | 비개발자 시각 리포트 EN/KO i18n (was KO-only) — `apps/central-plane/src/nondev-report.ts` (**central-plane 변경**) | #316 | main |
| T4 | code-check 링크상태 유실 **근본수정** (D1 read-after-write 일시 null→하드 false 붕괴) — `apps/dashboard/src/lib/repo-settle.mjs` 공유 리트라이, 3사이트 통일 (**dashboard-only**) | #317 | main |
| T2 | verdict 배너에 "사전 점검, 라이브 확인 아님" caveat | #318 | main |
| T3 | 베타 크레딧 배너 코드체크 시점 완전 숨김 (`CreditDryRunBanner` 베타 브랜치 `return null`) | #319 | main |
| T5 | 하드코딩 화면 i18n 전수 — **검증만, 누수 0건** (이전 세션이 이미 정비) | — | 변경없음 |
| T6 | UX Basics ④ 실패-로드 오도/데드엔드 **6곳** (checks·intake×2·sources×2·overview×2) | #320 | main |

검증: 각 PR CI green 후 머지 · dashboard typecheck ✅ · test 519/519 ✅ (EN/KO i18n parity 포함) · central-plane i18n 테스트 7건 ✅.

## 2. ★미검증 플래그 — 배포 스모크로 닫아야 함

**이 배치의 UI 변경(T2/T3/T4/T5/T6)은 브라우저 인터랙션이라 헤드리스 CI/에이전트 환경에서 실트리거 미검증.** T1의 라이브 locale=en 렌더도 전체 인증 인스펙션 실행이 필요해 curl 스모크 불가. 코드경로 추적 + typecheck + 519 테스트로만 검증됨. **아래 5종 라이브 스모크(브라우저)로 닫을 것 — 이게 정직 플래그를 닫는 유일한 방법:**

- [ ] repo 연결 프로젝트 → code-check "확인하기" → "코드변경"으로 안 되돌아감 + 링크 유지 (**T4**)
- [ ] verdict에 "라이브 아님" caveat 표시 (**T2**)
- [ ] 베타 크레딧 노이즈("N/5 무료") 안 보임 (**T3**)
- [ ] EN/KO 토글 → 리포트 양언어 + 하드코딩 누수 0 육안 (**T1/T5**)
- [ ] 데드엔드 6곳 중 2~3곳 실제 클릭 → 출구/재시도 있음 (**T6**)

## 3. 배포 상태

- **central-plane**: ✅ **LIVE** (run 28995243970, 2m55s). `/health` = `{ok:true, version:"0.13.15", environment:"production"}`, HTTP 200. URL `https://conclave-ai.seunghunbae.workers.dev`. 이 배치에 D1 마이그레이션 추가분 **없음**.
- **dashboard (Vercel)**: ✅ **LIVE** (`npx vercel --prod`, deploy `2olmgkxsv`, ● Ready, 현재 프로덕션 — 이전 프로덕션 1일 전 배포에서 교체됨). `app.trysimsa.com/` → 307 → `/projects` 정상. 배포 순서 = **central-plane 먼저** (T1이 워커 변경, Stage-83 기본값; T4는 dashboard-only라 stale-worker 창 없음 확인).
- **배포 후**: 위 §2 5종 스모크 실행 → 미검증 플래그 닫기.

## 4. 결정 상태 (배님 2026-07-09 회신)

1. **배포**: 승인됨. central-plane 먼저(diff로 T4 dashboard-only 확인 완료) → 5종 스모크.
2. **③ 비활성 버튼 힌트 + #295**: 배포 안정 **후** 별도 PR. **선행: #295 중복 스캔(완료 — 아래)**.

## 5. #295 재검토 메모 (후속 PR 전 필독)

`#295 fix(post-create-flow): new projects show in sidebar + forward exits (④⑥⑨)` — 2026-07-07, `mergeable=UNKNOWN`(rebase 필요).
- **파일 충돌 예상**: `github/page.tsx`·`AppSidebar.tsx`(T4와 겹침) + `dictionary.mjs`/`.d.mts`(T6와 겹침) + `ServiceMcpSetup.tsx`(신규).
- **테마 중복**: #295의 "forward exits from dead-ends"가 T6의 데드엔드 6곳과 부분 겹칠 수 있음.
- **처리**: main에 rebase → 충돌 해소 → T6 대비 **net-new 가치 남는지** 확인 → 남으면 머지, 아니면 non-overlap 부분만 cherry-pick. 배님 판단.

## 5-B. [2026-07-10 추가] 프로젝트 삭제 기능 (#322 + 하드닝 #323) — 머지됨, **배포 대기**

배님 요청으로 전체 프로젝트 삭제를 로컬+서버 end-to-end 신규 구현(기존엔 삭제 경로가 어디에도 없었음).

- **dashboard**: `workflow-store.deleteProject(id)`(리스트 엔트리 + `conclave_wf_ext_*`/`conclave_outcomes_*` 블롭 + sync-failed 정리) + 카드 휴지통 버튼(예제 카드 제외) + `deleteProjectFromDb` 미러.
- **central-plane**: `DELETE /workspace/projects/:id`(getOwnedProject 게이트, missing/not-owned 둘 다 404) + `db.deleteProject` — **18개 프로젝트 스코프 테이블 + experiment candidates(서브쿼리) + 프로젝트 행**을 단일 배치 트랜잭션 캐스케이드, R2 증거(업로드 문서·visual-check 스크린샷)도 삭제. **user 스코프(github_connections·oauth_states·credit_balances·credit_ledger·notification_settings)는 절대 미접촉** — 마이그레이션 테이블별 대조 검증. CORS `Allow-Methods`에 DELETE 추가(기존 sources 삭제 잠재 갭도 해소).
- **하드닝 #323** (배포 전 3종 가드 스크루티니 → 2개 보강): ① `window.confirm` 단일 → **acknowledge 체크박스 모달**("되돌릴 수 없음" red 강조, 체크 전 "영구 삭제" disabled) ② 캐스케이드 순서 **D1 트랜잭션 먼저 → R2 나중**(부분실패 시 dangling 참조 방지, 순서 테스트로 고정) ③ silent `.catch(()=>{})` 제거 → 미러 실패 토스트, 404=idempotent 성공. → 메모리 `feedback-destructive-action-guardrails`.
- **검증**: 캐스케이드/순서 테스트 6/6 · central 1681 · dashboard 519(EN/KO parity) · typecheck 양쪽 ✅. **미검증 = 브라우저 실동작**.
- **배포**: 배님 승인 대기. central-plane 변경 포함 → **central 먼저** → 스모크 → dashboard. 배포 후 라이브 QA 2종: ⓐ 체크박스 안 누르면 "영구 삭제" disabled ⓑ 정상 삭제 1건 → 목록에서 사라짐 + **새로고침 후에도 안 돌아옴**(D1 실삭제).
- **★R2 orphan 비용 누수 (후속 청소 잡 필요 — 방치 아님)**: D1→R2 순서상 D1 커밋 후 R2 삭제가 실패하면 무해한 orphan 객체가 남음(dangling 참조는 없으나 **누적 시 스토리지 비용**). 지금은 로그만 남고 재시도 없음. 후속: 주기적 청소 잡(예: cron이 `project_sources`/`workspace_visual_checks`에 참조 없는 EVIDENCE 키 목록화 → 삭제) 또는 R2 lifecycle rule. **"orphan = 무해"는 정확성 관점이지 비용 관점이 아님.**

## 6. 이월 항목 (묻히지 않게 — 이 배치에 안 담김)

- **Langfuse 키 등록**: usage 로그(`anthropic_usage`)는 쌓이는 중. UI trace는 2026-07-09 **실증됨**(#312/#313 후 배님 UI trace 2건, EU 리전). 남은 것: `docs/decision-status.md` #310 **리전 줄 기입**(배님 가입 리전).
- **latency = 출력길이 첫 쿼리**: #303 decisions 병합 배포 전후 **output_tokens 평균**이 실제 뛰었는지 Langfuse로 확인. 뛰었으면 대응 = 성능이 아니라 **spec 출력 길이 상한/요약**(비개발자에 5k 토큰 spec = 과부하, "한 화면 한 질문" 원칙과 충돌).
- **#298** (날조 mock 제거, p0-generate-no-fabrication): #303과 상보적. **rebase-merge 권고** 기록됨 — 열린 상태.
- **로그인 벽 P1 재등재**: #299 CLOSED(정직화 미반영). 백로그 정식 등재 대기.
- **R2 orphan 청소 잡**: 프로젝트 삭제(§5-B)의 D1→R2 순서상 R2 삭제 실패 시 orphan 객체 누적 → 스토리지 비용. 주기 청소 잡(cron이 미참조 EVIDENCE 키 삭제) 또는 R2 lifecycle rule. 상세 §5-B.

## 7. 정직 보류 (감사 lower-severity, 재작업 방지)

- ③ 비활성 버튼 이유 힌트(new/·items) — 필드가 버튼 바로 위라 이유 자명 → §4-2 후속.
- `CommandCenterCard` 배경 fetch `.catch(()=>{})` — 그레이스풀 degrade, 침습적 재시도 UI 대신 유지.

관련 메모리: `session-2026-07-09-prd-nondev-copy-ux`, `feedback-simsa-product-boundary`, `feedback-open-pr-is-not-shipped`, `feedback-merge-discipline`, `bae-rules-single-source`.
