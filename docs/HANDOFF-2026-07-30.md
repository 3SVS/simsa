# HANDOFF — 2026-07-30 (세션 마감: 7/20~22 연속 세션의 픽업 정본)

이전: `docs/HANDOFF-2026-07-21.md` (추가 1~4). 이 파일이 **다음 세션의 단일 픽업
지점**이다 — 7/21~22 연속 세션(#435~#465, 31 PR 전부 당일 머지·배포·실증)의
종결 상태와, Bae와 합의된 다음 작업(셀프 코퍼스 실측)을 담는다.

## 현재 상태 (전부 라이브·실증됨)

- **실행계획 5트레인 완료**: J(여정감사 v2)·U(UIUX 지시서)·E(서버 EN/KO)·
  M(해자 "왜 이 판정" 라이브)·G 앞단계(기계 게이트 전부 통과).
- **기준 10축 성문화**: `docs/simsa-standard-eval-2026-07-21.md` — Bae 교정
  이력→평가 렌즈. §3 위반 전건 집행 완료(§4 결과표).
- **find→fix→verify 원 자동 완주 실증**: 수리 PR 머지→웹훅 신호→10분 크론
  재검수→User Acceptance Required 정지(§5-4 준수). 사람 개입 0.
- **반추측 레일 행동 변화 실증**: 동일 입력에서 산탄총 CSS → 빈 edits 정직.
- 레거시 Conclave 자동리뷰는 킬스위치 오프(`LEGACY_AUTO_REVIEW="off"`,
  재가동=1줄+deploy). Simsa 자체 협의체는 유료 검수 모드로 라이브.

## 오픈 게이트 (핵심 대기)

- **잔여 = Bae 휴먼 QA뿐**: `docs/simsa-open-gate-2026-07-21.md` 체크리스트
  (시크릿 창, 30~40분, A아이디어/B기존앱/C검수→왜→고치기→공유/D횡단).
  **P0=0이면 ③④ 동시 통과 → `public launch approved.` 문구로 발효.**
- 오픈의 의미(Bae 질문에 답한 정직 판정): "완성 선언"이 아니라 **"측정 개시
  선언"** — 실전 오판율·퍼널은 오픈해야만 측정 시작됨. 기계로 잴 수 있는
  차단 결함은 0까지 소진.

## ★다음 작업 합의: 셀프 코퍼스 실측 (Bae 제안, 유효성 판정 완료)

Bae가 별도 계정으로 다양한 프로젝트를 만들어 검수·수리 루프를 실측하는 방식 —
**유효 판정**(실유저 대비 우위: **정답지 채점 가능** — 심은 결함을 알고 시작하므로
오탐/미탐 양방향 측정). 한계 2(정직): 분포는 못 잼 · 카피 이해도는 못 잼(아는
자의 저주).

**설계 확정분:**
| 축 | 다양화 |
|---|---|
| 빌더 | Lovable · v0 · Bolt · Claude Code · 직접 |
| 스택 | 정적 단일 HTML(대형 포함 — edit-mode 검증) · Next · 백엔드(Supabase) 유무 |
| 심은 결함 | 백엔드 죽음 · 버튼 무반응 · 폼 미저장 · 라우팅 깨짐 · **결함 없음(오탐 측정)** |
| 언어 | EN 1~2개 + ★**한글 데이터 필수**(파일명·상호·자유텍스트 — Rule 6: 리얼
데이터 검증. ssf2026에서 영문 placeholder만 테스트해 한글 파일명 실결함을 놓친 교훈) |

**핵심 규율: 각 앱 생성 시 정답지(심은 결함 목록)를 먼저 기록** — 검수 판정과
대조해야 진짜 오판율. 이거 없으면 그럴듯한 결과만 남는다.

**다음 세션 착수물(내 몫, 제안 상태 — Bae 확인 후):**
1. 코퍼스 설계표 + 정답지 시트 템플릿 (`docs/simsa-live-corpus-plan.md`)
2. 정답지 vs 검수 결과 자동 대조 스크립트(오탐/미탐 표)
3. 결과를 `docs/simsa-corpus-eval-2026-07-19.md`의 실전 확장판으로 기록

## 백로그 (우선순위 낮음·이월)

재검수 locale 컬럼(D1 마이그레이션 배치 — 하드게이트) · un-draft 라이브 발동
관찰 · D14 큐 리뷰(월요 크론) · EN 원어민 카피 검수 · 결과열람 대시 노출 ·
G6-2 토스 연동(Bae 가입 완료 시) · walmart PR#2 정리(산탄총 CSS 커밋 잔존).

## 주의 (재발 방지 — 이번 세션 실측 교훈)

- 체인/배포: `tools/scripts/pr-chain.sh`(main에 있음) — **안정 경로에서 실행**
  (미머지 브랜치의 스크립트는 브랜치 전환으로 소실됨, 실측). gh pr 명령은
  번호 명시, 포스푸시 후 체크 기동 확인, 체인에 git checkout 금지.
- dashboard 배포 = repo 루트 `npx vercel deploy --prod --yes --archive=tgz`
  + `vercel ls` 확인. central = deploy-central-plane 디스패치(`-f confirm=deploy`).
- 로그인-후 표면 실측 = **userKey 주입 기법**(`conclave_user_key` +
  `conclave_wf_projects:anon` 스텁) — journey-audit J5로 제도화돼 있음.
