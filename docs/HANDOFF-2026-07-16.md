# HANDOFF — 2026-07-16

> "실검증 계정 알려주고, 나머지 네가 할 수 있는 거 해봐" 세션. 계정 안내를 준비하던 중
> **T1이 계정 문제가 아니라 코드 갭임**을 밝히고, 이월 항목을 대부분 코드로 닫았다.
> 진실의 소스: `docs/simsa-prd.md`.

## 한 줄 요약

이월/열림 항목 정리 세션. **11개 PR 처리**(9 머지·1 종료·PRD 정정 1) + **central-plane 2회 배포**. 가장 큰 발견 셋: ① **T1 EN 리포트는 계정이 아니라 코드 갭**(locale 미배선 #335 + 스텝 라벨 한국어 누수 #340)이었고 **익명 라이브 스모크로 실증 완료**(#341, 10/10). ② **프로젝트 삭제가 스크린샷을 하나도 안 지웠다** — R2 키 포맷 불일치로 100% orphan, 테스트 모크가 버그를 스펙으로 굳혀놨음(#336). ③ **PRD가 "없다"던 온보딩 스펙이 실은 열린 PR #296에 갇혀 있었음**(정정 #337).

## 이 세션에서 머지·배포한 것 (main HEAD = `9077fd8`)

| PR | 내용 | 상태 |
|---|---|---|
| #335 | **inspection locale 미배선** — EN 유저에게 리포트가 항상 한국어. run 라우트→DO 페이로드→컨테이너→`runInspection` 전 구간 배선 | ✅ 머지+배포 |
| #295 | forward exits (github no-repo / ServiceMcpSetup 데드엔드). 리베이스로 사이드바 중복 effect 제거(#327/#331이 대체) | ✅ 머지 |
| #298 | P0 생성 날조 — "요약"/"linear" 단독 트리거가 무관한 회의록/Linear 스펙 생성. 트리거 좁힘 | ✅ 머지+배포 |
| #329 | HANDOFF 2026-07-12 (이력) | ✅ 머지 |
| #296 | **onboarding-feasibility-layer.md** — PRD가 "부재"로 오기록한 바로 그 파일 | ✅ 머지 |
| #336 | **R2 evidence orphan 100%** — 업로드는 전체키/D1은 상대이름 저장/삭제는 상대이름을 전체키인 양 투입→조용한 no-op. 프리픽스 스윕으로 수정. 테스트가 버그를 스펙으로 굳혀놨던 것도 정정(옛코드 대조 4실패로 실증) | ✅ 머지+배포 |
| #337 | PRD 정정 — "없다"던 P1 온보딩 스펙이 #296에 갇혀있던 것. §6.1/§15 [정정] 주석 | ✅ 머지 |
| #338 | R2 프리픽스 trailing-slash 가드 테스트 — 슬래시 지우면 proj_x가 proj_x2도 삭제. 슬래시 제거 시 이 테스트만 실패로 실증 | ✅ 머지 |
| #339 | **카나리 헛알람 구조 수정** — test/*.md도 Worker 경로로 세던 것. wrangler는 src만 번들→test-only 머지는 no-op 배포 요구였음. 10케이스 전수 검증 | ✅ 머지 |
| #340 | **EN 리포트가 한국어 스텝 라벨 인용** — 리포트 사전은 완전했으나 `planVisualFlow`/컨테이너 스텝 note가 로케일 없이 한국어 산문 생성→리포트가 그대로 인용. 라벨·note·sampleQuery(서울→Seoul) 현지화 | ✅ 머지+배포 |
| #341 | **익명 EN 리포트 라이브 스모크** — `tools/simsa-completion-loop-spike/en-report-smoke.mjs`. 10/10 pass | ✅ 머지 |
| #301 | P0 세션 인계 문서 — 목적 소멸(P0 5건 전부 라이브), 낡은 스냅샷이라 종료(사유 코멘트) | ❌ CLOSED |

**배포:** central-plane 2회 (`6e58761` → `0808b56`). 매번 `/healthz` deployedSha=main 확인. 컨테이너 이미지도 재빌드·푸시됨(SimsaInspector `a792874d`→최신).

## T1 실증 세부 (익명 스모크로 계정 불필요 확인)

**핵심:** 모든 워크스페이스 라우트는 로그인이 아니라 브라우저 로컬 `userKey`로 게이트 → 익명 스크립트로 전체 플로우 검증 가능.

`en-report-smoke.mjs` 실측(main `0808b56`): Simsa 자신의 산문(verdict/oneLine/nextSteps/notes) **한글 0**, 현지화 템플릿 토큰 **누수 0**. 남은 한글 52자 = **대상 앱의 실제 콘텐츠 인용**(한국어 CTA 라벨·콘솔 메시지) — 증거이므로 번역 금지가 정답.

## ★ 남은 Bae 액션 (여전히 계정 필요 — 익명 불가)

1. **자동수리(repair) LLM 라운드트립 실검증** — `[고치기]` 버튼(`/projects/{id}/visual-checks/{runId}`). 요구: **push 권한 있는 공개 repo + 배포됐고 지금 깨진 상태**. 스코프 `read:user public_repo`(공개 repo만·GitHub App 폴백 없음). 결과가 `mode: "auto_fix"`인지 확인(`brief_only`면 LLM 미실행=미검증). **주의:** 실패한 검수가 먼저 있어야 버튼이 뜸(=Item T1 실증과 연결).
2. **T4 스모크(repo 연결 링크 유지)** — `/projects/{id}/settings` GitHub 연결. **github.com에 그 계정으로 로그인된 상태**여야(계정 선택기 없음). read-only repo도 링크는 되지만 자동수리에서 push 실패하니 push 권한 repo 사용.
3. **카나리 Telegram DM**(선택, 1분) — `gh secret set TELEGRAM_BOT_TOKEN` + `CANARY_TELEGRAM_CHAT_ID`. 이슈 알림은 이미 라이브.

## 남은 이월 (내가 이어서 가능)

- **R2 백필** — #336 배포 이전(#322~) 삭제된 프로젝트의 증거물은 여전히 버킷에 남음. #336은 출혈만 멈춤. 일회성 스윕 잡 필요.
- **`workspace_pr_review_runs.training_r2_key`** (`events/…`) — 행은 삭제되나 R2 키 미수집→객체는 살고 포인터만 죽음. **비용 아니라 동의/보존 정책 판단**(Bae 결정 필요).
- **툼스톤 설계** — D1 batch 커밋 후 R2 스윕 죽는 잔여 부분실패 창(비용 유한).
- **Langfuse 리전** — 이미 완료(§13 EU·LIVE 실증). 7/15 핸드오프 이월 목록이 낡았던 것.
- **#340 라이브 재검증** — 배포·스모크로 완료. (닫힘)

## 교훈

- **"계정 필요" 딱지를 의심하라.** T1은 3주 내내 "인증 인스펙션 실행 필요"로 이월됐지만, 실제론 어떤 계정으로도 못 고칠 코드 갭이었다. 조사가 이걸 뒤집었고, 익명 스모크가 실증했다.
- **테스트 모크가 버그를 스펙으로 굳힐 수 있다.** #336의 삭제 테스트는 `["vis-1","vis-2"]`라는 **어떤 업로드도 안 쓰는 가짜 키**를 넣고 그게 삭제됐다고 단언 → green인 채 배포. 옛 코드+새 테스트 대조(4실패)로만 진짜를 증명했다. 회귀 테스트는 반드시 "고치기 전 코드에서 실패하는가"로 검증할 것.
- **알람 피로 = 신호 손실.** 5일 빨간불은 놓친 신호 하나가 아니라, 조치 불필요한 알람이 계속 울려 결국 아무 알람도 안 본 것. test/docs가 Worker 배포 알람을 울리던 걸 #339에서 제거.
- **누수는 사전보다 아래층에 있을 수 있다.** EN 리포트 누수의 원인은 완전했던 리포트 사전이 아니라 `planVisualFlow`의 로케일 없는 스텝 라벨이었다. 라이브 실측 없이는 못 찾았을 종류.
- 세션은 반드시 `C:\Users\seung\.conclave\conclave-ai`에서 시작.
