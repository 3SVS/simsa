# HANDOFF — 2026-06-12

## 오늘 커밋 목록

| SHA | 메시지 |
|-----|--------|
| 6609d49 | feat(workspace): Stage 10 — PR linking (list, link, view) |
| faf8d93 | feat(workspace): Stage 11 — PR code review (diff-based item check) |
| 32941a0 | feat(workspace): Stage 12 — PR fix brief + checks page integration |
| 626aa57 | feat(workspace): Stage 13 — PR comment (preview + post + list) |
| 51c1f0e | feat(workspace): Stage 14 — PR comment update mode + PATCH endpoint + usage events |
| 8d20cbf | feat(workspace): Stage 15 — PR review before/after comparison |

## 현재 상태

**PR 코드 확인 파이프라인 (Stages 10–15) 완성:**

1. **Stage 10** (`6609d49`) — PR 연결 (list, link, view), migration 0030
2. **Stage 11** (`faf8d93`) — PR diff 기반 LLM 코드 확인 (2 endpoints), migration 0031
3. **Stage 12** (`32941a0`) — 수정 지시서 생성 + /checks PR 섹션, 13 tests
4. **Stage 13** (`626aa57`) — PR comment preview + post + list, migration 0032, 22 tests
5. **Stage 14** (`51c1f0e`) — comment update mode + PATCH endpoint + usage_events, migration 0033, 23 tests
6. **Stage 15** (`8d20cbf`) — 이전/최신 비교 (ComparisonPanel, /review/compare), 28 tests

**총 테스트:** 557/557 pass  
**typecheck:** clean (central-plane + dashboard)

### Stage 15 구현 내용

- `src/workspace/pr-review-compare.ts` — 결정적 비교 (no LLM)
  - STATUS_SCORE: `passed=4, needs_decision=2, inconclusive=1, failed=0`
  - `compareRunResults` → improved / stillOpen / newlyProblematic / unchanged
  - `buildRunSummary` / `parseRunResults`
- `src/workspace/pr-review-db.ts` — `getLatestTwoPrReviewRuns` 추가
  - SQL: `WHERE status NOT IN ('running', 'queued', 'error') ORDER BY updated_at DESC LIMIT 2`
- `GET /workspace/projects/:id/github/pulls/:number/review/compare?userKey=...`
- `ComparisonPanel` (github/page.tsx) — 단일 run 시 힌트, 2개 이상 시 delta 표시
- PRCommentPanel: `hasComparison` prop → "최신 비교 결과를 바탕으로 코멘트를 업데이트할 수 있어요." 힌트
- /checks → "GitHub 탭에서 비교 결과 보기 →" 링크
- usage event: `workspace_pr_review_compared` (record-only, no billing)

### 활성 D1 migrations

| migration | 내용 |
|-----------|------|
| 0030 | workspace_linked_prs |
| 0031 | workspace_pr_review_runs |
| 0032 | workspace_pr_comments |
| 0033 | workspace_usage_events |

## 보안 제약 (계속 유효)

- autofix pipeline 연결 없음
- patch/commit/branch 생성 없음
- 자동 PR comment 작성/업데이트 없음
- billing/credit 차감 없음 (usage_events record-only)
- private repo 전체 지원 없음 (public_repo scope만)
- GitHub 토큰 → 대시보드에 절대 전달 안 됨 (central-plane만 처리)
- AES-256-GCM 암호화 (CONCLAVE_TOKEN_KEK)

## 다음 세션 픽업 포인트

### untracked 파일 커밋 필요

```
.githooks/
AGENTS.md
apps/central-plane/migrations/0031_workspace_pr_reviews.sql
apps/central-plane/src/workspace/github-pr.ts
apps/central-plane/wrangler.toml  (M — modified, not staged)
examples/github-workflows/conclave-ai.lnk
```

`0031_workspace_pr_reviews.sql` + `github-pr.ts`는 Stage 11부터 이미 적용/사용 중이므로 한 번에 커밋 권장.

### Stage 16 후보 (우선순위 순)

1. **비교 결과를 PR comment body에 포함** — `buildCommentBody` 확장, compareRunResults 결과 마크다운 섹션 추가
2. **Telegram 알림 연동** — PR 확인/비교 완료 시 DM
3. **billing/credit** — usage_events 기반 per-run 차감 구현
4. **/admin/usage-stats** — 이벤트 집계 어드민 뷰 (event_type별 일/주/월 집계)
5. **private repo 지원** — `repo` scope 확장, 유료 플랜 게이트

## 주의사항

- wrangler secret put 등 인터랙티브 명령어 → 별도 터미널에서 절대경로로 실행 (! 프롬프트 사용 금지)
- CF Containers secret 수정 → 항상 Actions workflow_dispatch 통해서 (로컬 wrangler는 staged version stuck 유발)
- Windows ESM 테스트: `../dist/...` 상대 경로 사용 (path.join + import() ERR_UNSUPPORTED_ESM_URL_SCHEME)
- `FixBriefItem = CheckableItem & { criteria? }` — id 사용, itemId 아님
- Stage 13 mock의 UPDATE handler: 7개 args `[status, ghId, ghUrl, errMsg, bodyPrev, updatedAt, id]` — bodyPrev 포함 필수
