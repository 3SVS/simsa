# HANDOFF 2026-08-20 — 전체 평가 → 갭 5건 집행 → 스택 불가지 P1 + 배포

> 픽업 정본. 직전 정본은 `docs/HANDOFF-2026-07-30.md` (7/30 이후 3주 공백이 이 세션의
> 출발점이었다).

## 오늘 한 일 (PR 9건 전부 머지 + central-plane 배포)

**1) 전체 프로젝트 평가 (4갈래 병렬 감사)** — 의도 정합 상급 / 코어 완성도 상급 /
증거 관리에 균열. 핵심 발견: 오픈게이트 근거였던 "P0=0·P1=0 KO+EN 풀런"의 실물이
미커밋 KO-only 파일이었음(커밋 기준선은 P1=3) → 당일 풀런 재실측으로 해소(아래 #471).

**2) Bae 갭 5건 지시 집행:**
| PR | 내용 |
|---|---|
| #467 | G14-b — 검수(check)·수정제안(fix)·검증패널(RC-2)·협의체(RC-3) EN locale 전면 배선 + dashboard `locale:"ko"` 하드코딩 제거 |
| #468 | migration **0065** — 재검수 locale 영속(verify-sweep v1 한계 해소). **적용 완료(remote)** |
| #469 | **journey-store 복원** — 삭제가 아니라 미머지 브랜치(e1f9f9f, 2026-07-04 beta-data-capture) 유실이었음. 원본 소스+테스트 재상륙, pr_reviewed/pr_rechecked/fix_brief_generated 배선 |
| #470 | 랜딩 Simsa 통일 — conclave-ai.dev → simsa.dev 전면 permanent redirect (**landing Vercel 재배포 필요**) |
| #471 | 무료 유지 결정 PRD §12 명문화(과금·토스 보류) + **journey-audit KO+EN 풀런(2026-08-20) 결과 커밋: P0=0·P1=0·P2=15** |
| #473 | 빌더팩 EN화(export.ts locale 배선, KO byte-identical 보장). EN 워크스루는 국제 서비스로 치환(Toss→Stripe 등) |

**3) 5분 질문 실측** — code 갈래: 가입 0초(익명) → 심사 done까지 시스템 ~1분(심사
컨테이너 48초), 총 2~3분. idea 갈래: 스펙 생성 45초/회 + 인터뷰가 지배(5분 조건부).
프로브 중 일시 LLM 503 2회 관측(이후 3연속 정상 — 순단).

**4) 스택 불가지(stack-agnostic) 트레인 개시** — Bae 지시: "어떤 조합을 사용해도
물어보고 맞춰 제공, 하드코딩 금지".
- 전수 인벤토리(B1~B10) + 설계서: `docs/simsa-stack-agnostic-design-2026-08-20.md`
- **design lock approved (2026-08-20) — D-1~D-4 LOCKED**
- #474: Phase 0 버그 — web_builder/handoff outcome 400 소실 수정 + 설계서 커밋
- #475: **Phase 1** — 호스팅/데이터 2문항(idea·code 갈래, other 자유수집) + ext 저장 +
  ExportUserProfile 운반. **소비는 Phase 2.**

**5) 배포** — `migration 0065 apply approved.` + `deploy central-plane approved.` 집행:
0065 적용 → deploy-central-plane 성공(배포 SHA=#473 시점 main) → 카나리 재판정 green
(29일 묵은 deploy-stale 해소). **#474·#475는 미배포분.**

**6) 시크릿 실태** — OPENAI·GEMINI·LANGFUSE 설정됨(RC-2/RC-3 가동 가능).
**RESEND_API_KEY 없음** → 이메일 표면(알림·재참여·검증) 전체 휴면.

## 다음 세션 픽업

1. **Phase 2** (`train stack-p2 start approved` 대기) — export/service-examples/env
   어댑터화 + 카피 레일 11곳(설계서 §3-1·2·7, 부록 A). Phase 3 = MCP/에이전트/수리팩.
2. **central-plane 재배포** (#474 outcome 수정 + #475 타입 운반 반영. 12h 넘기면 카나리
   stale 경고) + **dashboard Vercel 수동 배포** (#467 클라이언트·#473 export-api·#475
   질문 UI — repo 루트 `--archive=tgz`, exit 0이어도 `vercel ls` 확인) + **landing
   재배포**(#470 리다이렉트 발효).
3. 문서 정합 잔여(평가에서 지적, 미집행): PRD §15.5/15.6/15.7 정정 주석,
   decision-status에 Conclave EOL 행, open-gate ↔ OPEN-CHECKLIST 상호 편입.
4. Bae 액션: ① RESEND 키 발급/등록 ② 휴먼 QA(오픈게이트 잔여) ③ 배포 승인 문구들.
5. 증거 시스템 구조 개선(평가 지적): journey-audit 결과 타임스탬프 파일명(단일 슬롯
   덮어쓰기 제거), 스크린샷 트래킹 정책, lint CI 편입 — 미착수.

## 주의

- 0065 이후 visual_checks SELECT가 locale 컬럼 포함 — **다음 환경 복제 시 마이그레이션
  먼저**. verify-sweep은 원 런 locale 따름(레거시 NULL=ko).
- journey-store는 소비자(리더) 없음 — 캡처만. 동의 게이트(training consent) 뒤에서만
  저장됨.
- 오늘 LLM 순단(503×2) 같은 창에서 첫 방문자는 정직 에러+재시도 — 데드엔드 아님 확인.
- conclave-ai.dev는 머지만 된 상태 — landing 재배포 전까지 기존 Conclave 랜딩 노출 중.

## [추가 1] 스택 불가지 트레인 종결 (같은 날 후반)

- `train stack-p2 start approved` + `train stack-p3 start approved` 발효·집행 완료:
  **#477**(서버 — 빌더팩 조합 소비, 미응답=중립) · **#478**(대시 — 카탈로그 확장+
  카피 레일 11쌍, 포크 병렬) · **#479**(수리팩 web_builder + 에이전트 3종 + MCP 패널
  호스팅 소비). 게이트·집행·정정·잔여 백로그는 설계서 §5·§6이 정본.
- 이로써 인벤토리 위반 B1~B10 중 B1~B10 전부 해소 또는 정정(§6 표).
- **미배포 누적 주의**: #474·#475·#477~#479가 central-plane 미배포분,
  dashboard도 오늘 전 변경 미배포. 다음 배포 승인 시 한 번에 반영 권장.

## [추가 2] 3면 배포 완결 (같은 날 마감)

- `deploy central-plane approved.` / `deploy dashboard approved.` / `deploy landing
  approved.` 전부 집행·검증:
  - **central-plane**: deployedSha = main(#480) 일치, db up, 카나리 green.
  - **dashboard**: repo 루트 `--archive=tgz` → `vercel ls` Ready 확인(메모리 절차).
  - **landing(conclave-ai)**: ★실측 버그 — `/:path*` 리다이렉트가 **루트 `/`를
    매치하지 않음**(경로들은 308 정상, 루트만 구 랜딩 200). 명시적 `/` 규칙
    핫픽스 **#481** → 재배포 → 루트 308 → simsa.dev 라이브 확인.
    apps/landing은 Vercel 프로젝트 `conclave-ai`(root dir=apps/landing)라
    **repo 루트에서 VERCEL_ORG_ID/PROJECT_ID env 오버라이드 + --archive=tgz**로
    배포해야 한다(앱 폴더 실행은 경로 중복 에러 — 실측).
- 미배포 잔여 **0**. 오늘 세션 총 **16 PR 머지**(#467~#471, #473~#481).

## [추가 3] 이메일 표면 활성화 (RESEND 라이브)

- Bae가 RESEND_API_KEY 제공 → repo secret + `set-worker-secrets` dispatch로 Worker 등록.
- 첫 발송 403 → 원인 = Resend 계정에 trysimsa.com 미인증. **도메인 등록(ap-northeast-1)
  → Vercel DNS에 DKIM/SPF/MX 3레코드 추가(추가형) → verified → 테스트 메일 실발송
  200 sent**(seunghunbae@gmail.com 수신). 발신 = 코드 기본 `Simsa <notify@trysimsa.com>`.
- 활성화된 표면: PR 검수 완료 이메일 알림 · 이메일 인증 · reengage 크론(평생 1통 가드).
- Bae 잔여 액션은 이제 **휴먼 QA 1건**(`public launch approved.` 게이트)뿐.
