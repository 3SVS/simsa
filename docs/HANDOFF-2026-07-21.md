# HANDOFF — 2026-07-21

이전: `docs/HANDOFF-2026-07-20.md` (추가 1~3 — E-corpus 종결·auto_fix 성숙·journey-audit·Test B 완결).
오늘 흐름: **대형파일 auto_fix 트레인 완결** + **Bae 결정: 레거시 자동 리뷰 전면 오프** + 게이트웨이 신뢰성.

## 오늘 커밋 (#439~#443, 전부 당일 머지·배포·라이브 실증)

- **#439** `feat(repair)`: 대형 단일파일 auto_fix — **발췌+정밀치환(edit-mode) 사다리 단**.
  389KB 단일 index.html(vibe 클래스)이 스냅샷 상한(200KB)에 걸려 auto_fix 불가능하던
  클래스의 근본 해결. agent-worker `submit_edits`/`workEdits()`(효율게이트 경유) +
  repair-brief `buildOversizeExcerpts`(±40줄 윈도우·48KB 예산)·`applyExactEdits`
  (**정확히 1회 일치 규칙** — 잘린 커밋 원천 불가) + 컨테이너 사다리 단(full-file
  루프 빈손+oversize일 때만 — 기존 경로 바이트 동일). 테스트 15.
  **라이브: apply-walmart `mode=auto_fix`** + PR 발췌 정직 노트("큰 파일은 필요한
  부분만 고쳤어요") + `simsa-repair[bot]` 커밋.
- **#440** `fix(repair)`: repair PR 본문에 `<!-- [skip conclave] -->` — App 설치
  repo에서 Simsa repair PR을 구 협의체가 재리뷰하던 중복(실측: autofix-test PR#1
  체크런+코멘트 2건) 차단. 웹훅 정규식 락스텝 테스트.
- **#441** `feat(saas)`: ★**레거시 Conclave 자동 리뷰 전면 오프 — Bae 결정**
  ("모든 곳에서 자동 리뷰 일단 끄고 simsa 테스트하면서 진행").
  `LEGACY_AUTO_REVIEW="off"` 킬스위치(wrangler.toml [vars]) — pull_request 자동
  협의체 리뷰 + 설치 웰컴 리뷰를 **크레딧 소모 전** 스킵. 설치 기록·유저 생성·
  파운더 알림·수동 /saas/review·Simsa repair는 유지. **재가동 = [vars] 1줄
  삭제 + deploy 1회.** 테스트 3(DB 폭발 스텁으로 무접촉 증명·default-on·서명 선행).
  **라이브 실증: probe 커밋(75209ffe)에 협의체 체크런·코멘트 0** — 전면 정지 확인.
- **#442+#443** `fix(repair)`: repair 워커 LLM 호출 **CF AI Gateway 경유**.
  직행 egress 간헐 403 "Request not allowed"(#428 상세가 오늘 노출; Worker측
  2026-07-05 실측 ~90% 403과 동일 클래스). #442(헤더 배선)가 라이브에서
  `Cannot find package '@anthropic-ai/sdk'`로 실패(호출자측 clientFactory의 SDK
  import는 server.mjs 컨텍스트에서 해석 불가) → #443 **ClaudeWorker `baseURL`
  1급 옵션**으로 수정. **라이브: walmart repair `mode=auto_fix, error=null`** —
  게이트웨이 경유 완주, 403 소멸.

## 답한 질문 (Bae)

- "모든 repo에 conclave-ai-review가 붙어 충돌?" → **3SVS/simsa에는 App 미설치**
  (3SVS org 설치는 My-first-product 1개). 충돌 실체는 autofix-test에서의 중복
  리뷰였고 #440+#441로 전면 해소. 타 계정 설치(nogl-prod 120 repo 등)는 베타
  유저 — 킬스위치로 함께 정지됨(기록 보존).

## 상태

- central-plane 라이브 = main(#443까지), 컨테이너 이미지 롤아웃 완료 확인 후 실증.
- 오늘 낮 세션(#435~#438)과 합쳐 **이번 24h: #435~#443 9개 PR 전부 당일 머지·배포**.

## 다음 픽업

1. **G14-b 서버 EN화** (다음 큰 덩어리)
2. **D14 월요 크론 리뷰** — 크론 첫 실행이 오늘(월) 16:00 KST → 이후
   `deploy_trend_suggestions` 큐 리뷰(현재 테이블 빈 상태가 정상임을 확인함)
3. G6-2 토스(Bae 가입 대기) · 실유저 트래픽 · F-5 입력률 관찰
4. (선택) 검수/생성 등 Worker측 나머지 LLM 경로는 이미 게이트웨이 경유 —
   컨테이너의 /saas 구경로는 킬스위치로 휴면이라 조치 불요

## 주의

- **레거시 자동 리뷰 재가동 절차**: wrangler.toml [vars]의 `LEGACY_AUTO_REVIEW`
  줄 삭제(또는 "on") + deploy-central-plane 1회. 코드 삭제 아님 — 데이터 무손실.
- 백그라운드 체인에 `git checkout`/`--delete-branch` 금지 재확인(오늘 1회 경합
  재발 — WIP 커밋 선행으로 무손실 복구). 체인은 CI watch→merge→deploy만.

## [추가 1] 계획 수립 + Train J·U 당일 완료 (#445~#448)

Bae 지시: "전체 플로우·UIUX·기능 개발 계획 먼저 세우고 작업" + "기준선 P0부터 바로".

- **#445 실행계획 2026-07-21** — 근본 판단: 남은 문제는 기능이 아니라 **여정
  완결성**. 트레인 J(장비)→U(UIUX)∥E(서버 EN화)→M(해자 연결)→G(§14 오픈 게이트).
  07-09 계획 완료 대사 포함(T-AUTO 전부 ✅·T-VISUAL 부분·시각 검증 루프 조건 해소).
- **#446 Train J: journey-audit v2** — UX Basics·primary CTA 수(main 한정)·EN 축
  (conclave:locale 주입)·koLeak·P0/P1/P2 자동 분류·JOURNEY-AUDIT.md 런북(배포
  게이트: P0=0 필수). **기준선 P0=0**, P1 3건 판독: 실물 2(EN prep 카드 한글
  478자·개요 검수카드 CTA 플립) + 거짓양성 1(chooser 3문 설계 → 규칙 예외).
- **#447 Train U 배치1** — ①서비스 카탈로그 ko/en 병기+locale 파라미터(기본 ko),
  seedServiceSetup은 값 유지·카피만 재해석 ②검수카드 문에 "wait"(code+repo미확정
  → 카드 보류, 플립 CTA 금지) ③checks 단일 primary(prSectionVisible·
  draftHasResults·run_precheck — 하드코딩 primary를 상태기계로 편입).
- **#448 Train U 배치2** — 잔여 177자 출처=mcp-catalog(GitHub/Vercel 카드) 동일
  패턴 이중언어화.
- **실증(재감사 2회)**: EN 한글 누수 478→177→**0** · **P0=0 · P1=0**. 남은 P2
  15건 중 실물은 0(비활성 이유는 라벨/문맥 전달로 판독 기각).
- ★유의: #447에서 브랜치를 main이 아닌 #446 브랜치에서 분기해 충돌→리베이스
  복구. 또 force-push 직후 `gh pr checks --watch`가 "no checks reported"로
  즉시 통과해 머지됨 → main push CI green으로 사후 보완. **교훈: 포스푸시 후
  체인은 CI 기동 확인(sleep) 후 watch.**

### 다음 픽업
1. **Train E: 서버 EN화(G14-b)** — 서버발 유저 문자열 전수(rg)→locale 배선→EN 재감사
2. U 잔여: #3 "+N개 더" 인라인 펼침 · #2 "3초 파악" 재검 · 지시서 상태표 정정
3. Train M(해자 라이브 연결 — 설계 선행) · D14 큐(월 16시 크론 후) · Bae 확인 3건
   (A2 정의·P1 스펙 승격·prep A 보안 사후 확인)

## [추가 2] Train E·U 완결 + 미결 해소 (#450~#453, Bae 지시 반영)

- **Train E 완료**: #450 repair PR EN/KO(locale 잡 동행 — **라이브 실증:
  walmart PR#2 본문 영어**, 유저 데이터는 원문 유지) + #451 알림 표면(PR 알림
  email/telegram locale 관통 + reengage 크론은 KO/EN 병기 — 서버 저장 locale
  없음·스키마 추가는 하드게이트라 무저장 전략). central 배포 success.
- **Train U 완결(#2 재검 제외)**: #453 "+N개 더" 인라인 확장/접힘(중요도 기준).
  배포 후 KO 재감사 **P0=0·P1=0**.
- **미결 3건 해소(#452, Bae "4,5 제외 다 진행")**: §15.1 P1 스펙 승격 확정 ·
  §15.3 **A2 = Prep layer A2**(서비스/MCP 패널 export→settings 이동, 이미
  완료·라이브) · §15.4 prep A 보안 방식 승인. 지시서 #1 상태 stale 정정.
- **Train M 설계 제안 제출** — "왜 이 판정인가" 증거 체인(M-1: 검수 완료 시
  acceptance-graph 조립→deriveEvidencePack→기존 TEXT 컬럼 저장→GET evidence
  API→결과 카드 3열 체인[기준↔관찰 사실↔판정]+해석 분리+리스크 배지;
  M-2: 공유 카드+result_shared). 스키마 무변경·불변식 §5 전수 준수.
  **`design lock approved` 대기.**
- ★운영 교훈 추가: 백그라운드 체인에서 `gh pr checks/merge`는 **PR 번호 명시**
  (무번호는 현재 브랜치 상대참조 → 포그라운드 checkout과 경합, #452에서 실측).

### 상태: 이번 24h #435~#453, 19개 PR 전부 당일 머지·배포·실증
### 대기: M design lock(Bae) · 휴먼 QA(Train G 시점) · 토스 가입(Bae 진행 중)

## [추가 3] Train M 완결 (#455~#457, design lock → 당일 완결)

Bae `design lock approved` → 당일 3PR 머지·배포·라이브 실증:

- **#455 M-1a** `GET /workspace/projects/:id/evidence/:runId` — 저장된 사실
  (스펙/items·시각 런·최신 PR 리뷰 per-item 결과)을 GET 시점에
  acceptance-graph로 **결정론 조립** → deriveEvidencePack → gate.
  저장 없음·네트워크 없음·**스키마 무변경**(on-demand — 도출 로직 업그레이드
  시 결과 자동 최신). works=false+findings → crossReview 블로커 → Needs Fix;
  works=null만 미검증. 테스트 7(결정론 deepEqual·무점수 assert 포함).
- **#456 M-1b** 런 상세 "왜 이 판정인가요?" 펼침 — [확인 항목]↔[무엇으로
  확인했나]↔[상태] 3열 체인 + 브라우저 사실/Simsa 해석("측정된 사실 아님")
  분리 + DECISION_STATES 11종 KO/EN + 영어 산문은 개발자용 접힘.
- **#457 M-2** 공유 생성 시 `workspace_result_shared` 이벤트(+shareId 메타,
  비치명) — §9.4 예약 슬롯 실기록.
- **라이브 실증**: walmart·autofix-test 런 2케이스 — 무점수(score 필드 0)·
  items 없는 프로젝트는 정직한 "Needs Evidence"·해석 추출("...단계가 끝까지
  되지 않았") 확인. 풍부 경로(verified/broken 체인)는 유닛 7이 고정.

### 다음 = Train G(§14 오픈 게이트)
퍼널 계측 확인 → 기계 QA(journey-audit 풀런) 0 FAIL → 생성 진짜(Bae 입력) →
Bae UIUX P0=0 → **휴먼 QA(Bae 유저 시점) 1회 완주** → 오픈.
+ D14 큐(월 16:00 크론 후) · U #2 재검 · edit-mode 품질 관찰 · 토스(Bae).
