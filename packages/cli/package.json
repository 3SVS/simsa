{
  "name": "@simsa/cli",
  "version": "0.18.1",
  "description": "Simsa (Conclave AI) CLI — ships the `conclave` and `simsa` binaries.",
  "license": "FSL-1.1-Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "conclave": "./dist/bin/conclave.js",
    "simsa": "./dist/bin/conclave.js"
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/*.test.mjs",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "dependencies": {
    "@simsa/agent-claude": "workspace:*",
    "@simsa/agent-design": "workspace:*",
    "@simsa/agent-worker": "workspace:*",
    "@simsa/agent-gemini": "workspace:*",
    "@simsa/agent-grok": "workspace:*",
    "@simsa/agent-ollama": "workspace:*",
    "@simsa/agent-openai": "workspace:*",
    "@simsa/core": "workspace:*",
    "@simsa/integration-discord": "workspace:*",
    "@simsa/integration-email": "workspace:*",
    "@simsa/integration-slack": "workspace:*",
    "@simsa/integration-telegram": "workspace:*",
    "@simsa/observability-langfuse": "workspace:*",
    "@simsa/platform-cloudflare": "workspace:*",
    "@simsa/platform-deployment-status": "workspace:*",
    "@simsa/platform-netlify": "workspace:*",
    "@simsa/platform-railway": "workspace:*",
    "@simsa/platform-render": "workspace:*",
    "@simsa/platform-vercel": "workspace:*",
    "@simsa/scm-github": "workspace:*",
    "@simsa/failure-classifier": "workspace:*",
    "@simsa/secret-guard": "workspace:*",
    "@simsa/smoke-verifier": "workspace:*",
    "@simsa/visual-review": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@anthropic-ai/sdk": "^0.65.0",
    "cosmiconfig": "^9.0.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
