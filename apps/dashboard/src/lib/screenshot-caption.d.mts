// Train N4 — types for screenshot-caption.mjs.
export function screenshotCaption(
  name: string,
  listIndex: number,
  labels: { initial: string; afterStep: string; final: string },
): string;
export function screenshotFileName(name: string): string;
