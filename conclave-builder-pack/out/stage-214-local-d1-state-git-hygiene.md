> **SUPERSEDED** — 이 문서는 Conclave 시대 가정 하에 작성되었습니다. 제품은 Simsa로 전환되었고, 최신 방향·우선순위는 `docs/simsa-research-audit-2026-07.md` (감사 v2)를 보세요.

# Stage 214 — Local D1 State Git Hygiene (.wrangler ignore)

**Date:** 2026-06-25
**Branch:** `chore/stage-214-ignore-local-wrangler-state` (from main `cc92c9c`)
**Scope:** Git hygiene only — ignore local Wrangler/Miniflare state. No code, no package, no migration, no deploy.

---

## 1. Branch / HEAD
- `chore/stage-214-ignore-local-wrangler-state` off main `cc92c9c` (Release: Stage 211). Tracked working tree clean at branch creation.

## 2. Hygiene issue summary
- Stage 213's `--local` D1 apply created `apps/central-plane/.wrangler/` (local D1 SQLite state). It appeared as **untracked** and was **not** matched by `.gitignore` — so a future `git add .` could accidentally commit local DB state. This stage closes that gap.

## 3. Ignore structure inspected
- No `apps/central-plane/.gitignore` (absent). Root `.gitignore` is the repo convention and already ignores app-local generated state by full path (e.g. `apps/landing/public/screenshots/`). → correct location is the **root `.gitignore`** with the `apps/central-plane/.wrangler/` path (matching convention; no broad/global ignore).

## 4. Ignore rule added
Appended to root `.gitignore`:
```
# Wrangler / Miniflare local state (local D1, KV, caches). Generated by
# `wrangler dev` / `wrangler d1 execute --local`. Local-only — never commit.
apps/central-plane/.wrangler/
```
- Smallest safe rule. Does not ignore migrations, `wrangler.toml`, or any checked-in config.

## 5. Ignored-state verification
- `git check-ignore apps/central-plane/.wrangler/` → matches `apps/central-plane/.wrangler/` (now ignored).
- `git status --short` no longer lists `apps/central-plane/.wrangler/` as untracked; the only tracked change is ` M .gitignore`.
- `apps/central-plane/migrations/0047_better_auth_identity_tables.sql` and `apps/central-plane/wrangler.toml` remain tracked and unchanged.
- No local D1 state files staged.

## 6. Files changed
- `.gitignore` (one line rule + comment) — plus this report doc. No other tracked file changed.

## 7. Verification results
- central-plane build: **pass**
- auth tests (spike + route + migration): **18/18 pass**
- monorepo typecheck: **57/57** (FULL TURBO)

## 8. Safety diff review
- Tracked diff vs main = `.gitignore` only (+ report). ABSENT: package.json, pnpm-lock, source code, migration changes, wrangler.toml changes, `.env`, tracked `.wrangler` files, dashboard changes, CORS/Vercel/DNS, token/secret, deploy config, production behavior.

## 9. Production impact analysis
- **Zero.** A `.gitignore` rule has no runtime effect. Production remains `9b645af`. No deploy, no migration.

## 10. Rollback note
- Revert the one-line `.gitignore` addition (revert this PR's commit). Local `.wrangler/` state is unaffected either way (it stays on disk, just ignored).

## 11. Stage 214 decision
- **Option A — Local Wrangler state hygiene PR ready for review.** Ignore rule added, `.wrangler` now ignored and unstaged, verification green, PR opened.

## 12. Out-of-scope confirmation
No package install, no D1 runtime binding, no local migration apply, no production migration, no runtime smoke, no deploy, no production env, no `.env`, no OAuth, no Vercel rewrite, no CORS, no DNS, no dashboard UI, no token/secret. Stale dogfood PRs #121~130 untouched.

## 13. Recommended next stage
- **Stage 215 — Local Wrangler State Hygiene Merge Gate** — only after "PR #<n> merge approved."
- Then **Stage 216 — Better Auth D1 Runtime Binding Package/Version Check + Install Gate** — only after "D1 runtime binding package/version approved."
- Production migration ("Production auth migration approved.") and deploy ("Dashboard deploy approved.") remain separate gates.
